# 小团宝

面向中小地接旅行社的 B 端 SaaS 管理系统，用于散拼产品资料与班期报价、发团、行程执行、资源、财务、同行/客户、供应商及组织权限等业务的日常运营。

## Language

**Organization**:
一家使用小团宝系统的旅行社，是多租户 SaaS 中的租户单位。所有业务数据按 Organization 隔离。
_Avoid_: 租户, tenant, 公司, 账户

**SaaS**:
小团宝以多租户模式运营，多家 Organization 共用同一套部署，通过 organizationId 实现数据隔离。
_Avoid_: 私有化, 独立部署

**User**:
一家 Organization 内可登录后台的员工身份。一个 User 只属于一家 Organization，不存在跨组织任职。登录标识见 Login Username。
_Avoid_: 账户, 成员, 账号

**Login Username（登录用户名）**:
User 用于登录后台的标识字符串，在全平台唯一（含 Platform Organization 上的 Platform Admin 与各客户 Organization 的员工，同一命名空间）。持久化为规范小写（创建、编辑、开户与登录输入均先 trim 再转小写后存储或查找）；唯一性因此不区分大小写。与显示名称（name）不同。企业管理员可在员工创建与编辑时设置或修改；Platform Admin 开户时的初始企业管理员使用同一规则：不做业务前缀拼装。登录页只凭登录用户名与密码定位 User，不要求填写 Organization 标识。凡未彻底删除（`deletedAt` 为空）的 User 均占用其登录名，含停用 Employee 与所属 Organization 已停用的 User；停用或组织停用不会释放该登录名。
_Avoid_: 账号, 账户（作该字段产品文案）, 开户自动加业务前缀, 登录名按前缀改写入库, 登录页填写企业/组织 ID 消歧, 仅组织内唯一而跨组织可撞车, 停用后释放登录名供他人使用, 大小写不同视为不同登录名, 库内保留任意大小写仅靠比较时折叠

**Employee**:
与 User 同义，指 Organization 内的在职员工。
_Avoid_: 用户（易与系统登录用户混淆时）

**Employee Status**:
员工账号处于启用或停用状态。停用的 Employee 不可登录，状态可恢复；与彻底删除（`deletedAt`）不同。第一版 UI 仅提供启用/停用，不提供删除。
_Avoid_: 软删除作停用, 删除代替停用

**Employee Remark**:
Employee 的内部说明文字，可选，仅用于创建/编辑表单，不在员工列表展示。
_Avoid_: 备注作公开信息, 对外描述

**Last Login**:
Employee 最近一次成功登录的时间，在员工列表中展示为「最近活跃」；从未登录则为空。
_Avoid_: 最近活跃作在线状态, 实时在线

**Platform Organization（平台运营组织）**:
专供 Platform Admin 挂靠的 Organization，不是客户租户。其上 User 均应为 Platform Admin；登录后只进入平台区，不使用该组织的租户后台。客户 Organization 名录维护不把 Platform Organization 当作普通租户展示或停用。
_Avoid_: 把 Platform Admin 挂在客户 Organization 下, organizationId 为空的平台账号, 平台组织当客户开户

**Platform Admin**:
平台运营方的超级管理员，可跨客户 Organization 做名录维护（创建、查看档案、改名称、启用/停用）。创建客户组织时须同事务建立 Initial Organization Admin（见 Organization Onboarding）。与客户 Organization 内的 User 是不同身份，通过 User 表的 isPlatformAdmin 标志位识别，账号挂在 Platform Organization 下，共用同一套登录体系。工作台为同一 Web 应用内的独立平台区，与 Organization 后台路由及 Menu Permission 分离；不可进入租户业务页，也不可代入企业管理员操作。名录维护不提供删除 Organization；客户组织档案可只读展示 Initial Organization Admin 的登录用户名与显示名称，不因此开放平台侧员工管理。Platform Admin 账号本身由 seed/运维预置，不在平台区做账号管理 UI。
_Avoid_: 超管, 系统管理员, super admin, 企业管理员（作平台身份）, 代入租户后台, 平台区删除客户组织, 平台区管理 Platform Admin 账号, 平台区事后重置租户管理员密码, 平台档案展示等同员工管理

**Initial Organization Admin（初始企业管理员）**:
Platform Admin 开户时同事务写入的那一名企业管理员 User，是客户 Organization 的开户联系人。平台区客户 Organization **档案详情**只读展示其登录用户名与显示名称（名录列表不展示），读的是该 User 的**当前**字段（租户侧改名后档案跟随变化）；停用后仍展示这两项，不因此从档案抹去。认定规则：该组织内开户写入的那一名；实现上可用该组织最早创建的企业管理员 User 代表（第一版无员工删除 UI 时与开户那人一致）。认定不到时详情仍展示该区块并为空态（如「未设置」），不阻断改名/停用等名录操作。不是 Platform Admin，也不表示组织内只能有一名企业管理员。
_Avoid_: 平台超管, 把租户内全部企业管理员当成初始联系人, 平台档案列出全部员工, 开户快照与现网登录名长期分叉, 停用后从平台档案隐藏初始管理员, 名录列表展示初始管理员, 缺初始管理员时详情接口报错

**Organization Onboarding**:
客户 Organization 的开通交付。现行开户：Platform Admin 创建组织时同事务写入 Initial Organization Admin（登录用户名、显示名称、初始密码必填），绑定企业管理员 Role，创建后可立即登录租户后台；登录用户名规则与租户员工管理相同（见 Login Username），不拼业务前缀。邀请链接让客户自设密码仍为后置可选能力，非开户必经。Platform Admin 开户后不在平台区重置该管理员密码或管理其员工档案；平台区客户 Organization 档案可只读展示 Initial Organization Admin 的登录用户名与显示名称（不含密码，不提供改密/停用/改名等操作）。
_Avoid_: 自助注册, 开放注册, 创建组织只建壳不建管理员, 开户必经邀请链接, 平台区事后管理租户员工, 开户登录名强制加业务前缀, 平台档案展示初始密码, 平台档案做成员工管理入口

**Organization Business Prefix（组织业务前缀）**:
Organization 创建时必填、仅可设置一次的 2–4 位大写英文字母标识，用于生成发团编号及财务类业务编号。未设置前缀的 Organization 不得创建发团、收付款节点、流水或核销。前缀建议全系统唯一。
_Avoid_: 租户代码, 组织编码, 创建后补设前缀

**Organization Status（组织状态）**:
Organization 处于启用或停用状态。停用后该组织下 User 不可登录、不可新建业务单据；Platform Admin 仍可在平台区看到该组织档案并改名称或再启用。与彻底删除（`deletedAt`）不同，也不复用 Directory Profile Status。
_Avoid_: 用 deletedAt 表示停用, 目录三态套用到 Organization, 停用后对 Platform Admin 不可见, 停用后仍允许租户用户登录

**Organization Name Uniqueness（组织名称唯一）**:
客户 Organization 的名称在全平台唯一；保存重名时拒绝。Platform Organization 使用固定区分名，不参与客户开户的可选重名空间。
_Avoid_: 允许客户组织重名靠前缀区分, 仅软提示不拦截

**Workbench Action Item（工作台待处理事项）**:
从 Organization 当前业务状态实时派生、引导员工进入对应业务对象处理的行动提示；源状态解除后自动消失。它不是可领取、指派、延期或手动完成的独立任务，第一版不提供个人任务管理。
_Avoid_: 待办, 我的待办, 任务实体, 手动完成

**Coordinator Pending Account Generation（计调待生成账款）**:
计调工作台上并列的两类尚未生成收付款节点的行动提示：待生成应付与待生成应收。产品模块名 **待生成账款**；它引导计调去生成资源应付或客源应收，不是发团结清门槛，也不替代财务侧的混列「待生成账款」清单。
_Avoid_: 结算衔接, 可确认结清（作该模块名或左侧队列）, 把结清门槛当作计调待生成账款

**Pending Payable Generation（待生成应付）**:
约定金额大于零、尚无有效（未作废）资源应付节点的行程段资源；已关闭节点仍视为已生成。已结清发团上的资源不计入（不可再生成应付）；已关闭发团不排除。计调工作台按资源行计数与列表；与客源单侧的待生成应收对称。
_Avoid_: 可确认结清, 可确认结算, 按发团数冒充待生成应付, 把已结清发团上的资源计入待生成应付

**Departure Data Gap（发团资料待补充）**:
近期或进行中发团缺少可由系统客观确认的基础资料：整团无客源单、整团无行程段、已有行程段但整团无任何资源，或任一客源单的已录入客人数少于其客人人数；同一发团可同时具有多个结构化缺失项。它只表示系统资料尚待补充，不判断单个行程段是否应有资源，也不等同于业务准备未完成。
_Avoid_: 存在执行风险, 准备不完整, 准备度, 单个行程段无资源即报警, 资料齐备等同业务准备完成

## Roles

**Role Assignment**:
一个 User 在同一 Organization 内可绑定一个或多个 Role。其有效 Menu Permission 为所有已绑定 Role 的权限并集。第一版员工管理界面为单选 Role，保存时全量替换；底层 UserRole 仍支持多 Role，后续可开放多选。
_Avoid_: 单角色, 主角色

**Role Catalog**:
Role、Permission 及其映射为全平台共享的定义，不按 Organization 隔离。User 通过 UserRole 绑定 Role；Menu Permission 在该 User 所属 Organization 的后台内生效。
_Avoid_: 租户级角色表, 每组织复制角色

**企业管理员**:
Organization 的拥有者或最高管理者，拥有全部 Menu Permission。实现上通过 seed 为企业管理员 Role 绑定所有 Permission 行，权限解析无特殊分支。
_Avoid_: 老板, admin, 总经理, 系统管理员, 代码特判

**财务**:
负责应收、应付、收支流水、核销等财务相关菜单。可进入发团、产品中心、合作伙伴、供应商，但对这些业务对象**只读**：不新建/编辑发团、不改客源与执行/资源、不维护产品/合作伙伴与供应商目录（`departure:write`、`product:write`、`partner:write`、`supplier:write` 均无）；可触发生成应收/应付（生成挂在 `/departure`，不受 write action 限制）。重新打开已结清发团下的收付款节点时，财务可在明确确认联动影响后直接使发团回到待结算，无需 OP 预先审批；操作完整留痕并对 OP 可见，之后仍由 OP 重新确认已结清。
_Avoid_: 会计, 出纳, 财务编辑客源/执行/发团, 财务维护产品或合作伙伴或供应商目录, 财务不能生成应收应付

**计调**:
负责发团、产品中心、合作伙伴、供应商等运营相关菜单，对其可写（持有 `departure:write`、`product:write`、`partner:write`、`supplier:write`）；可生成应收/应付。**不进入** `/finance/*`（应收/应付/收支流水/核销）四菜单，因此登记流水、核销/撤销核销、关闭节点、调整约定金额均不可；发团详情内的收支流水、核销记录 Tab 对计调隐藏。行程管理、资源管理尚未上线，不在第一版 Menu Permission 中。
_Avoid_: 操作, 调度, planner, 行程管理, 资源管理, 计调登记流水或核销, 计调关闭或调整账款, 计调不能生成应收应付

**Preset Role**:
企业管理员、财务、计调三个 Role 由系统统一定义，每个 Organization 创建时自动 seed。第一版 Role 及其 Menu Permission 映射固定，Organization 内不可新增 Role、不可修改权限映射。
_Avoid_: 自定义角色, 角色模板, 权限配置

**System Management Access**:
组织管理、员工管理、角色权限（`/system/*`）仅企业管理员 Role 拥有。财务、计调不可访问系统管理菜单及其后端接口。
_Avoid_: 全员系统设置, HR 角色

## Permissions

**Menu Permission**:
权限的菜单级维度，决定 User 能否看到并访问某个菜单模块。菜单级之外，另有 **Action Permission**（按钮级）控制同一菜单内可写到什么程度；两者同存于 `Permission` 表，通过 `key` 区分（menuKey 如 `/finance/receivable`，action key 如 `departure:write`）。第一版仍不做数据级（只看自己的数据）权限。
_Avoid_: 数据权限, 菜单级即全部权限

**Action Permission（按钮级权限）**:
控制「进入某菜单后可写到什么程度」的操作级权限，以 action key 存于 `Permission` 表并经 `RolePermission` 全局固定映射（ADR-0001、ADR-0023）。第一版四把：`departure:write`（发团概览/状态/客源/执行/资源 编辑，及资源应付作废）、`partner:write`（合作伙伴目录 CRUD）、`supplier:write`（供应商目录 CRUD）、`product:write`（产品中心 Product/Spec/Schedule 维护），均为 企业管理员+计调=有、财务=无。生成应收/应付与 `/finance/*` 各操作**不**新增 action key：前者留在 `/departure`（计调与财务都可），后者靠是否持有对应 finance menuKey 天然区分。后端复用 `MenuPermissionGuard`，write 接口用 `@RequireMenu('departure:write')` 校验；`/auth/me` 以独立 `actionKeys` 字段下发，仅供前端按钮 gating，不参与菜单/路由过滤。
_Avoid_: 按 role 名硬编码能力, 按 Organization 自定义按钮权限, 把 action key 混进 menuKeys 参与菜单过滤, 为生成应收应付或每个 finance 操作单独建 action key

**Menu Key**:
标识一条 Menu Permission 的稳定键，与前端路由路径一致（如 `/finance/receivable`）。Permission 只对应可访问的叶子路由；分组父菜单（如 `finance`、`system`）不单独存 Permission，当前 User 任一路由子项可见时父菜单自动显示。
_Avoid_: permission code, 菜单 id

**Menu Permission Resolution**:
User 的有效 Menu Permission 由后端根据其所绑 Role 的权限并集计算，经登录与 `/auth/me` 接口以 `menuKeys` 返回（两者共用同一组装逻辑）。前端仅据此过滤菜单与路由，不维护独立的 code 映射或权限规则。页面刷新或 token 恢复时通过 `/auth/me` 重新拉取。
_Avoid_: 前端硬编码权限, 前端 code 表, 仅登录返回权限

**Menu Permission Enforcement**:
Menu Permission 同时约束菜单可见性与后端访问，不能仅靠前端隐藏菜单。第一版对系统管理类 Menu Key 强制后端校验；业务类 Menu Key 随各业务模块落地时补齐。
_Avoid_: 仅前端校验

**Supplier Management Access**:
供应商管理（`/supplier` 及其详情子路由）企业管理员、计调、财务三 Role 均可访问；其中企业管理员与计调可编辑（持有 `supplier:write`），**财务只读**（无 `supplier:write`，可查看账期规则、结算说明、更多财务信息等结算信息，用于应付/付款实务，但不维护目录 CRUD）。合作伙伴（`/partner`）同理：财务可进入（含往来账款），但目录业务信息只读（无 `partner:write`）。产品中心（`/product`）同理：财务可进入查看，但无 `product:write`，不可维护 Product/Spec/Schedule。
_Avoid_: 财务维护供应商或合作伙伴目录, 财务不进供应商名录, 详情页单独 Menu Key, 财务维护产品目录

**Product Center Access**:
产品中心（`/product` 及其详情子路由）企业管理员、计调、财务三 Role 均可访问；企业管理员与计调可编辑（持有 `product:write`），**财务只读**（无 `product:write`）。
_Avoid_: 财务维护产品/班期价, 产品中心仅计调可见

**Reference Options（参考选项）**:
仅返回 id→名称、用于渲染筛选器/下拉/标签的查找数据（如 `finance/departure-options`、`partner-options`、`supplier-options`、`source-order-options`），不是一等业务对象。访问口径为**按其所返回实体类型的业务菜单单键声明式守卫**（departure/客源单选项→`/departure`、partner→`/partner`、supplier→`/supplier`），语义为「能看见该类实体即可取其查找项」，不用 OR、不用认证裸放（ADR-0024）。当前不变量：合作伙伴/供应商「往来账款」Tab 里的发团选项按所返回实体挂 `/departure`（非当前屏幕 `/partner`），预设角色皆持 `/departure` 故不受影响。
_Avoid_: 下拉选项作业务主列表, 参考接口按当前屏幕菜单守卫, 参考接口用 /finance/* 或 OR 放行, 仅认证放行参考接口

## Product Interface

**业务主列表**:
从左侧菜单直接进入、支持独立分页的业务对象集合，包括发团、产品、应收、应付、收支流水、核销、合作伙伴、供应商与员工。详情页内嵌子表、操作履历、下拉选项及卡片集合不属于业务主列表。
_Avoid_: 所有表格, 所有集合, 详情子表

**业务对象创建时间**:
业务对象首次持久化成功的时间，创建后保持不变，不因后续编辑或关联对象变化而刷新。
_Avoid_: 首次业务发生时间, 首次活动时间

**业务对象更新时间**:
业务对象自身最后一次被人工编辑或由系统变更业务状态、金额等持久化事实的时间；下属、关联对象、派生展示值或登录活动等观测信息的变化不联动刷新该时间，例如核销不会刷新对应收付款节点，员工登录不会刷新员工档案的更新时间。
_Avoid_: 最近活动时间, 最近登录时间, 关联对象更新时间, 聚合更新时间, 派生状态更新时间

**业务显示时间**:
面向用户展示的业务时间统一按中国标准时间（`Asia/Shanghai`）解释；业务主列表、发团详情顶部，以及发团详情内嵌的客源单、应收、应付、流水与核销列表，其创建、更新时间均使用 `YYYY-MM-DD HH:mm` 格式且不显示秒。行程段、段资源、客人名单、运营表快照等其它详情子表不展示这两列。
_Avoid_: 浏览器本地时间, 客户端系统时区, 列表显示秒, 详情内所有子表都加时间列

## Core Entities

**Departure（发团）**:
一次具体出团活动的运营容器，是系统核心业务对象。计调在此录入客源、按线路规划行程、在行程段内安排自营资源或拼出，再触发关联的财务事件。**发团**是客户日常口头业务名称；发团是运营聚合根，**不是**财务结算单元——应收挂在客源单，应付挂在行程段或资源，发团头部的毛利与待收待付为跨子单元的汇总视图。
_Avoid_: 团单, tour, 团期, 团单管理, 接团单, Project

**Departure Overview（发团概览）**:
发团的基本信息：线路名称、线路类型、目的地、出发/结束日期、预计成人/儿童人数、负责人、状态等。
_Avoid_: 团期详情, 基本信息

**Departure Progress（出团进度）**:
由出团日期与结束日期自动派生的时间状态：未开始、进行中、已结束。表达这趟团在时间轴上的位置，不驱动财务锁定或只读规则。
_Avoid_: 出团状态, 团期状态, 进行中（作发团业务状态）

**Departure Status（发团状态）**:
发团业务处理阶段：编辑中、待结算、已结清、已关闭。编辑中 → 待结算由 OP 手动切换；已结清由 OP 手动确认，**不是**系统自动改状态；已结清发团下的收付款节点被明确重新打开时，经用户确认与节点一同回到待结算，不留下「发团已结清但存在开放账款」的中间状态。已结清发团上不可再生成或手工创建收付款节点；若需新增账款，须先经财务联动（如撤销核销或重开节点）回退到待结算。已关闭为归档只读，由 OP 手动触发；其下收付款节点不可直接重新打开，须先由 OP 或企业管理员带原因解除归档并使发团回到待结算。不等于财务单据是否已全部生成。
_Avoid_: 团单状态, 项目状态, 确认发团, 系统后台自动把发团标为已结清, 重新打开节点后发团仍保持已结清, 已结清发团上继续生成或手工创建应收应付

**Departure Unarchive（发团解除归档）**:
OP 或企业管理员将已关闭发团恢复为待结算的明确动作，用于再次处理该运营容器内的业务或财务事项。解除归档原因必填并保留原归档人、时间与原因；归档期间仅可查看，撤销核销、作废或反向流水、调整金额与重新打开节点等任何会改变财务事实的动作均须先解除归档，且不会自动触发解除归档。
_Avoid_: 财务操作自动解除归档, 归档期间撤销核销或作废流水, 归档期间新增反向流水, 删除原归档履历, 无原因解除归档, 发团已关闭时直接重新打开子节点

**Departure Financial Settlement Gate（发团账款结束门槛）**:
系统派生的布尔门槛：下属收付款节点全部为已结清或已关闭时为真（含未结清关闭），表示没有仍开放追收/追付的收付款计划。仅当门槛为真时，OP 才可把发团状态标为已结清；门槛本身不是发团状态。已关闭节点上仍可见的未结清不阻止该门槛，也不计入发团待收/待付。
_Avoid_: 用门槛代替发团已结清, 未结清关闭不算账款结束, 无门槛仍可标已结清, 已放弃金额

**Settlement Receivable（结算应收）**:
发团当前应向客户收取的最终团款，等于本团全部客源单优惠后金额合计。它只汇总客源团款，不包含手工创建的其他应收；生成或关闭应收节点只改变账款状态，不另造或删除结算应收金额。当前模型中客源单一律参与结算，只有删除客源单才使其金额退出汇总；「标记客源单不参与结算」是尚未建模的能力，出现真实业务需求时再引入。
_Avoid_: 实际应收（作产品指标名）, 把其他应收计入结算应收, 用已生成应收节点金额替代客源单金额, 因应收节点关闭删除客源团款, 把「参与结算」当作已存在的过滤条件

**Departure Financial Summary（发团财务摘要）**:
发团级财务概览分开表达两类事实：账款层用产品文案「已收 / 未收 / 已付 / 未付」展示核销进度（已收/已付仅指已核销到具体账款的金额）。主卡收款进度只统计由客源单生成的客户补款与游客代收路径；尚未生成应收的客源金额仍属于未收，满足结算应收＝已收＋已生成开放未收＋已关闭未收＋尚未生成应收，收款进度＝客源路径已收÷结算应收。手工应收不混入该进度，另作「其他应收」提示。主卡付款进度只统计资源应付的有效核销，以资源成本合计为分母；手工应付、其他应付及资源账款金额差异不混入该主进度，进入财务对照明细。财务明细中的全部已付仍汇总归属该发团全部非作废应付节点的有效核销，包含已结清节点与关闭前的历史核销，并满足确认应付＝全部已付＋开放未付＋已关闭未付。应收、应付的已关闭未结清均不从账上消失，而是在未收/未付中单独拆分并标记异常。资金层展示归属该发团但尚未核销的收入/支出，不以进度卡吞并。撤销核销后，金额从已收/已付退回未收/未付，对应流水则进入未核销资金；两层同时存在，不互相抵消。
_Avoid_: 用全部应付已付作资源付款进度分子, 用确认应付作主卡付款进度分母, 把手工应付混入资源付款进度, 用已收/已付同时表示资金发生与账款核销, 把手工应收混入客源结算应收进度, 把未核销资金计入已收或已付, 关闭节点后抹除历史已收或已付, 关闭节点后让剩余未收或未付消失, 确认应付不等于全部已付加未付, 撤销核销后隐藏原流水, 把未核销资金计入账款已结清, 已放弃金额

**Other Receivable（其他应收）**:
归属该发团但不是由客源单客户补款或游客代收路径生成的有效应收节点，用于承载手工补充的其他收款义务。它不进入以结算应收为分母的收款进度；概览存在该金额时单独提示并引导进入应收管理查看。
_Avoid_: 把手工应收计入结算应收, 把其他应收已收混入客源收款进度, 隐藏其他应收

**Cash Net Inflow（现金净流入）**:
发团概览资金情况指标，等于归属该发团的全部未作废收入流水减全部未作废支出流水。资金卡同时展示有效收入流水、有效支出流水与现金净流入，使用户可直接复算；另提示其中未核销收入与未核销支出。它表达已实际发生的资金净流动，包含已核销与未核销流水；退款按实际资金方向记录，客户退款作为支出、供应商退款作为收入，因此自然进入净流入计算。第一版反向流水不与原账款建立反向核销关系，不自动冲减收款或付款进度。撤销核销只改变未核销提示，不改变现金净流入。收付款节点关闭、作废或重新打开均不改写真实流水，只有流水本身作废时才退出收入或支出合计。资金按流水归属发团聚合，跨团核销不改写流水归属：存在外部核销时，资金卡另以提示展示「核销自外部流水」与「本团流水核销至他团」金额，解释资金卡与收付进度卡的合法差值；「核销自外部流水」涵盖归属他团与未归属任何发团的流水核销到本团账款，两者合并为一个口径。该提示与未核销收支提示同级，不标红、不算守恒异常。现金净流入不是利润，也不是开放账款的核销进出差。
_Avoid_: 用退款反向流水自动冲减收付款进度, 强制退款核销到反方向账款, 实际毛利, 净利润, 只展示无法复算的净流入, 概览已收减概览已付, 排除未核销流水, 撤销核销后改写现金净流入, 因节点状态改写历史现金事实, 用核销关系改写流水归属发团, 跨团核销标红作异常, 隐藏跨团核销差值, 核销自外部流水遗漏无归属发团的流水

**Cost Total（成本合计）**:
发团概览业务预计支出，等于本团全部行程段资源约定金额合计（自营与拼出均计），无论是否已生成应付。它是主卡付款进度的分母；其中尚无有效应付节点的资源金额单独汇总为「尚未生成应付」，留在成本合计中提醒，但不进入确认应付或财务明细的未付。生成或关闭应付节点不改变成本合计；当前模型中资源一律参与结算，只有删除资源才使其金额退出汇总；「标记资源不参与结算」是尚未建模的能力，出现真实业务需求时再引入。
_Avoid_: 预计成本（作主指标产品名）, 应付合计（作该指标产品名）, 用应付节点金额代替资源约定金额, 因应付节点关闭删除资源成本, 未生成资源成本计入未付, 未生成资源成本从成本合计消失, 把「参与结算」当作已存在的过滤条件

**Confirmed Payable（确认应付）**:
发团概览财务已确认的付款义务，等于归属该发团全部非作废应付节点约定金额合计，不限节点来源类型；手工应付亦计入。已结清与已关闭节点仍计入确认应付；资源应付作废后才退出确认应付。关闭只是停止后续追付，不否定原确认义务，也不改写确认毛利。与成本合计对账时必须分别暴露尚未生成应付、其他应付、已生成资源的源金额与节点金额差异，满足 `确认应付－成本合计＝其他应付＋资源账款差异－尚未生成应付`；各组成因素不得因净额相互抵消而隐藏。
_Avoid_: 应付合计, 只展示确认应付与成本合计净差额, 差异相抵后隐藏组成问题, 关闭节点后退出确认应付, 作废节点仍计入确认应付, 用资源约定金额代替节点金额

**Current Gross Profit（当前毛利）**:
发团概览的实时经营预估利润，等于结算应收减成本合计。它随有效客源团款和有效资源成本变化，不表示现金结果或财务账款已全部确认。
_Avoid_: 预估毛利（作主指标产品名）, 实际毛利, 净利润, 确认毛利

**Current Gross Margin（毛利率）**:
发团当前毛利占结算应收的比例，等于当前毛利÷结算应收×100%；结算应收为零时无可计算比例，显示「暂无数据」。负毛利保留真实负比例，不归零。
_Avoid_: 净利率, 结算应收为零时显示 0%, 负毛利率归零

**Confirmed Margin（确认毛利）**:
发团概览财务成本确认后的利润，等于结算应收减确认应付。不用「实际毛利」命名，以免与现金或核销口径混淆。
_Avoid_: 实际毛利

**Departure Overview Stats（发团概览统计）**:
发团详情「概览」Tab 内的经营与财务指标区，信息结构固定为三层：第一层展示总人数、结算应收、成本合计、当前毛利四个主指标；第二层「经营补充」展示原始团款、优惠合计、毛利率；第三层展示收款进度、付款进度、资金情况。截图只约束该信息结构，颜色、卡片、间距、图标和响应式布局遵循根 `DESIGN.md`，不照搬外部截图的渐变图标、阴影或装饰条。财务对照与异常按指标就近展开：成本合计入口解释确认应付、尚未生成应付、其他应付与资源账款差异；当前毛利入口对照确认毛利；收款进度入口解释尚未生成应收、已关闭未收与其他应收；付款进度入口解释已关闭未付及全部应付核销进度；资金情况入口解释未核销收支与外部核销（含跨团与无归属流水）。概览不新增独立财务对照大区块；这些明细能力不因主层简化而删除或合并口径。概览指标使用专用汇总口径与字段，不改写发团级已核销/未结清等全局账款字段的既有语义。毛利率、收款进度、付款进度均按四舍五入固定显示 1 位小数（如 `80.0%`）；文字保留小于 0% 或大于 100% 的真实比例，只有进度条视觉长度限制在 0–100%。比例分母为 0 时显示「暂无数据」，不显示 0%。主层与经营补充的指标金额为 0 时显示 `¥0.00`；明细入口内的提示项金额为 0 时省略该行。任何守恒关系异常均保留原始有符号金额与真实百分比，不以截断、归零或自动修正掩盖；受影响指标标红并展示差额说明。异常不阻止读取概览，后端记录结构化异常并由写操作约束避免继续扩大。不挂在详情页头部跨 Tab 常驻；头部只保留发团元数据与状态操作。
_Avoid_: 新增独立财务对照大区块, 把外部截图当视觉规范, 照搬渐变装饰, 因主层简化删除财务对照或异常能力, 把经营与确认口径合并成一套事实, 静默把负数归零, 用进度条封顶值代替真实百分比, 自动修正异常财务数据, 异常时整页不可查看, 只记录日志不在页面提示, 顶部经营概览作完整统计区, 各 Tab 重复完整统计卡, 改写全局已核销字段语义以迁就概览, 未生成应收从未收中消失, 未生成资源成本计入未付, 隐藏尚未生成应付, 手工应收混入客源收款进度, 隐藏其他应收, 隐藏已关闭未收或未付, 分母为 0 时进度显示 0%

**Departure Type（发团类型）**:
发团成团方式的展示标签，如独立团、拼团。仅用于列表筛选与识别；不约束客源单数量，不改变业务规则。独立团即恰好只有一条客源单的发团，模型与拼团相同。
_Avoid_: 团单类型, 单团, 结构性拼团

**Route Template（常用路线）**:
Organization 内常发线路的配置快照，保存行程段结构与段内资源草稿（种类、对手方、名称等）。地接社日常在固定线路上反复收客、配资源、做核算；选路线建团时一次性复制进新发团，之后模板与发团实例脱钩。模板不保存资源金额（资源金额为本团总价，随人数等变化，不作模板能力）、客源、应收应付与流水核销；亦不设段级默认执行方式。
_Avoid_: 线路模板, 产品, product, 散拼产品, 活引用, 参考价格（作模板能力）, 默认执行方式

## Product Center

**Product（散拼产品）**:
Organization 内可维护营销行程与对外报价、按班期生成发团的资料聚合根。产品承载可销售的线路说明与报价口径，不是一次具体出团，也不是执行侧行程段/资源结构模板。第一期产品类型固定为散拼；定制游等其它类型未建模。
_Avoid_: 常用路线, 线路模板, 发团, 团单（作该对象名）, Tour, Package, SKU, FIT Product（作独立类型名时）, 产品目录项

**Product Type（产品类型）**:
Product 的成团/售卖形态分类。第一期仅允许散拼产品，不提供类型切换，也不在模型中并列定制游等其它取值。
_Avoid_: 多类型并存（第一期）, 定制游作第一期取值, 发团类型（Departure Type）冒充产品类型

**Product Schedule（班期）**:
某 Product 下一条可销售的发团日期规则及其对外报价快照（如连续区间天天发、每周固定日、单独日期说明）。班期是报价与「生成发团」的口径，不是发团本身；同一班期可生成多个发团。与收付款节点（Payment Schedule）无关。
_Avoid_: 收付款节点, 应收单, 应付单, 发团, 库存, 余位, Schedule（无 Product 前缀的英文名）, Departure Slot, Departure Offering

**Product Spec（价格规格）**:
Product 上一种可报价档位及其默认销售价（如标准/舒适：成人价、儿童价、单房差等说明）。第一期一个 Product 可先只维护一个规格。规格默认价供新建班期复制；不是客源团款单价，也不驱动应收。
_Avoid_: SKU, 套餐, 房型（作规格对象名时）, 客源团款单价, 参考价格（作常用路线能力时）

**Product Schedule Price Snapshot（班期价格快照）**:
创建 Product Schedule 时从当时 Product Spec 默认价复制出的班期侧报价事实。之后修改规格默认价不回写既有班期；班期价可独立调整。文档展示与对外报价以所选班期快照为准。
_Avoid_: 班期活引用默认价, 改默认价联动未锁定班期, 第一期「跟随/锁定」双模式

**Product Itinerary（产品行程）**:
Product 上面向同行与文档的营销行程文案整体，含简版与详细版；不是发团执行安排中的行程段，也不携带资源与成本。
_Avoid_: 行程段, Itinerary Segment, 执行安排, 常用路线, 行程（作发团执行区对象名时）

**Short Itinerary（简版行程）**:
Product Itinerary 的短述，用于产品总表、列表与微信快发；可为整段文本，或拆成按日的 Product Day。
_Avoid_: 详细行程, 行程段, 执行安排

**Detailed Itinerary（详细行程）**:
Product Itinerary 的完整叙述，用于正式 Word/PDF；可为富文本或关联文档，不要求与简版字段一一强绑定。
_Avoid_: 简版行程, 行程段, 发团运营表

**Product Day（产品日行程）**:
Product Itinerary 内「第 N 天」的营销说明条目（如 D1/D2 标题与描述）。可选结构；第一期允许仅存整段简版而不拆日。不是发团行程段，不挂资源。
_Avoid_: 行程段, Itinerary Segment, Day（作发团执行单元名时）, 每日行程（作行程段同义词时）

**Product Departure Source（产品发团来源）**:
发团上仅作追溯的来源指针：来自哪个 Product、哪条 Product Schedule（及生成时所选具体出团日）。生成时一次性复制名称、日期与产品行程文案等进入发团，之后产品或班期变更不回写该发团，发团变更也不回写产品。不是活引用，也不表示财务或客源已按产品报价生成。
_Avoid_: 活引用产品, 产品价自动写入客源单价或应收, 生成时强制写入常用路线, 来源模板（作常用路线字段名套用到产品时）

**Product Status（产品状态）**:
Product 的上架生命周期：草稿、销售中、已下架。草稿可编辑与预览，不进入有效销售统计；销售中可维护班期、导出与生成发团；已下架保留历史，可再上架。已有班期或已被发团以 Product Departure Source 引用的产品不可物理删除，只能下架。
_Avoid_: 启用/停用（作产品生命周期时）, 已发布（无下架态时）, 用 deletedAt 表示下架

**Product Schedule Status（班期状态）**:
Product Schedule 的可售生命周期：销售中、已截止、已取消。销售中可用于文档与生成发团；已截止保留历史报价，新建发团须二次确认；已取消不参与有效班期与价格统计，且不可生成发团。取消不删除历史行。
_Avoid_: 草稿（作班期状态时）, 用删除代替取消, 停用（作班期状态同义词时）

**Product Feature（产品特色）**:
Product 上面向营销的卖点条目（短标题与说明，如「真心」条款）。可空；不承载年龄限制、退改、拼住等报名规则。
_Avoid_: 报名须知, 注意事项（作特色对象名时）, 把规则条文写入特色

**Booking Notice（报名须知）**:
Product 上（或组织模板提供的）报名与执行规则说明，如适用年龄、儿童标准、拼住、座位、购物与退改等。可整段维护；组织可提供常用模板，产品引用后允许单独修改。与 Product Feature 分模块，避免营销卖点与业务规则混杂。
_Avoid_: 产品特色, 把须知塞进特色条目, 仅组织模板不可被产品覆盖（第一期）

**Product Import Session（产品导入会话）**:
一次从外部总表（如试点《疆游记》xlsx）迁入产品的服务端确认过程：关联已归档的导入原件，承载解析出的结构化候选项与待确认/已接受/已废弃等状态；计调确认后批量生成草稿 Product，确认前不上架、不生成发团。候选项以服务端会话为真相，不以浏览器暂存为唯一来源。
_Avoid_: 导入批次（作本对象正式名时）, 仅前端持有候选项, 解析结果当已上架产品, 把导出文件当导入会话

**Source Order（客源单）**:
发团内的收入侧结算单元，对应一家合作旅行社输送到地接社的一批客人。记录发客客户、成人/儿童人数与对应团款单价、优惠及收款拆分。不同合作方可在同一发团下各有独立客源单；应收从客源单触发生成，而非从发团统一生成。自动生成的展示名（`displayName`）为发客客户（Partner）名称；同一发团同一伙伴多单时追加序号（如「思达典雅 2」），**不**拼出团日期或「发客」后缀（发团顶栏已有团期信息；应收「客源单」列读此展示名）。
_Avoid_: 订单, 收客单, 报名单, 客源记录, 客源批次, 展示名自动拼「X月X日发客」

**Adult Guest Count（成人人数）**:
客源单上按计价分类的成人数，由用户录入，允许为 0；与儿童人数相加得到总人数。业务上纯儿童团少见，但模型不强制必须有成人。新建客源单默认 0，保存前须使总人数 ≥ 1。
_Avoid_: 大人人数, 成人客人数

**Child Guest Count（儿童人数）**:
客源单上按计价分类的儿童数，由用户录入，允许为 0；与成人人数相加得到总人数。新建客源单默认 0。
_Avoid_: 小孩人数, 儿童客人数

**Total Guest Count（总人数）**:
客源单上的客人合计人数，等于成人人数 + 儿童人数；只读派生，不单独录入，持久化时仍可保存该派生值供列表与汇总使用。约束为总人数 ≥ 1（成人与儿童不可同时为 0）。
_Avoid_: 客人人数（作唯一录入字段时）, 总客人数

**Adult Unit Price（成人团款单价）**:
客源单上每位成人的优惠前团款单价，允许为 0（如免票）。成人人数大于 0 时必填；成人人数为 0 时可不填，计算按 0。
_Avoid_: 成人价, 大人单价

**Child Unit Price（儿童团款单价）**:
客源单上每位儿童的优惠前团款单价，允许为 0（如免票）。儿童人数大于 0 时必填；儿童人数为 0 时可不填，计算按 0。
_Avoid_: 儿童价, 小孩单价

**Original Tour Fare（原始团款）**:
客源单优惠前的整单团款基数，等于成人人数 × 成人团款单价 + 儿童人数 × 儿童团款单价；只读派生。发团概览汇总本团全部客源单的原始团款；产品抽屉在「团款与优惠」区展示为「原始团款金额」，底部预览不再重复该项，仅保留结算金额与我方代收。
_Avoid_: 原始应收（作产品指标名）, 原始团款单价（作整单汇总时）, 团款总额（与结算金额混淆时）

**Source Order Guest List（客人名单）**:
客源单下的计调备忘名单，记录出行客人姓名等便于现场对接的信息。不是客源单人数、团款或应收的事实来源；名单人数可与客源单总人数不一致，也不提供「同步人数」回写客源单。
_Avoid_: 游客档案（与应收往来对象混淆时）, 名单即人数, 报名单, 同步人数

**Guest Representative（游客代表）**:
发团运营表中代表一条客源单显示的单个现场联系人，第一版静默取该客源单客人名单中按创建时间最早的一条记录，仅展示其姓名与已有联系电话；名单为空则留空，不在客人名单界面增加规则提示。后续只有出现明确业务需求时才增加指定游客代表的能力。
_Avoid_: 导出完整客人名单, 第一版指定游客代表, 客人名单界面解释导出规则, 名单完整度

**Collection Split（收款拆分）**:
客源单上表达「钱由谁收、收多少」的录入方式。合作旅行社可能已代收了部分或全部团款；地接社也可能需直接向游客收取剩余或全部款项。收款拆分决定生成应收时走哪条路径，本身不是财务单据。
_Avoid_: 收款责任, 收款方式（作领域术语时）, 团款组成

**Customer Settlement Receivable（客户补款）**:
应收路径之一：合作旅行社已向游客收取、地接社仍需向该合作方收取的部分。往来对象为发客 Partner。仅当该路径金额大于零时生成对应收付款节点。
_Avoid_: 组团社已收, 客户已收金额（作应收名称）

**Guest Collection Receivable（游客代收）**:
应收路径之一：地接社需直接向游客收取的部分。往来对象类型为客人，稳定键为客源单 id（写入 `counterpartyId`），展示名用客源单 `displayName`。核销按类型 + 客源单 id 匹配，不要求流水名称与展示名一致。仅当该路径金额大于零时生成对应收付款节点。
_Avoid_: 地接代收, 我方代收金额（作应收名称）, 仅靠往来名称匹配游客代收

**Source Order Partner Settlement（客源单合作方结算）**:
地接社向游客收取全款后，按商业约定仍需向发客合作方支付的款项。第一版不在客源单上自动生成对 Partner 的应付；计调/财务在应付管理中手动录入。后续若业务规则稳定，再考虑从客源单结构化触发。
_Avoid_: 返佣, 佣金应付, 自动生成合作方应付

**Itinerary Segment（行程段）**:
发团内按日期、目的地切分的行程规划单元，例如「喀纳斯段｜7月1日至3日｜3天」。计调通常以「第几天到第几天在某个景点」来规划。行程段是成本侧锚点，其下挂多条资源行；段内可同时存在自营资源与拼出资源。第一版不做到每日行程明细。与 Product 上的产品日行程（Product Day）不是同一概念。
_Avoid_: 执行批次, Execution Batch, Day, 每日行程, 产品日行程, 简版行程, 详细行程, 自营段, 拼出段

**Segment Resource（行程段资源）**:
挂在行程段下的成本费用行，统一承载自营资源与拼出。计调在段内按 **资源种类** 添加费用行；种类为拼出时关联 Partner（承接方），其他种类关联 Supplier，且该种类须属于该 Supplier 的 **供应商类别** 集合。一段内可有多条、种类可混合；同一 Supplier 可被不同种类的资源行引用（如酒店供应商既挂酒店费又挂餐费）。触发生成应付时以资源行为来源，仅对手方不同。第一版不提供资源人工排序；发团运营表在行程段顺序内按资源种类固定顺序，再按资源名称与对手方稳定排列。
_Avoid_: 资源安排（作实体名或独立 Tab）, Resource Item, 拼出记录, Segment Outsource, 执行方式, 一家供应商只能对应一种资源行

**Resource Kind（资源种类）**:
一次行程段资源安排所用资源的分类，存为单值。取值与 **供应商类别** 共用同一套枚举（规范名 Resource Kind）：用车、酒店、导游、门票、餐、景区、购物店、演出、保险、拼出、其他。产品 UI label **资源种类**（不用「资源类型」「供应商类别」指代资源行字段）。拼出仅出现在资源行，表示整段或部分服务拼给同行。
_Avoid_: 资源类型, Supplier Category（作资源行字段名）, 车队/餐厅/票务（作与用车/餐/门票并列的另一套 label）, 为资源行与供应商维护两套枚举

**Execution Arrangement（执行安排）**:
发团详情中规划行程段与段内资源（含自营与拼出）的工作区；详情页 Tab 正式称「执行安排」。区内「行程段」指段导航（对应 Itinerary Segment），「资源安排」指资源主表（对应 Segment Resource 列表）——二者为工作区内称谓，不是独立 Tab。
_Avoid_: 行程段/资源安排作独立详情 Tab, 行程, 资源配置, itinerary, 每日工作台

**Departure Operations Sheet（发团运营表）**:
发团级、带导出时点的只读运营结算快照，把本团客源单、按行程段排列的资源（种类、对手方、名称、约定总价）、收付款及核销进度与各处非空备注组织成一张可核对、可预览、可导出的事实表，用于替代纸质「一趟团总账本」式手写记录。第一版使用一套固定结构，尽量覆盖系统当时能够产出的运营与财务事实，不另分运营版和结算版；它可在发团生命周期的任意阶段生成，早期主要呈现计调已录入的运营与约定金额，财务介入后逐步呈现流水及核销结果。面向普通业务与计调的主表使用应收/已收/未收和应付/已付/未付；其中已收、已付仅指已核销到具体账款的金额，未核销收支流水不混入具体客源或资源行，而在表尾逐笔列出收支方向、交易日期、往来对象、金额、通道与流水备注，并分别合计待确认收款和待确认付款。一条客源单保持一个业务主行；客户结算与游客代收作为收款路径子行，分别展示各自的应收与收款进度，不把不同付款方混成一个进度。财务流程整体尚未开始时，只在表头表达该数据阶段，不逐条标记「应收单未生成」或「应付单未生成」；各行仍显示客源或资源的约定应收/应付，已收/已付与未收/未付用「—」表示尚无进度口径，不用 0 冒充财务已确认的结果。部分客源或资源已进入财务流程时，表头标为部分开始，已生成账款的行展示实际收付进度，未生成的行继续显示「—」；进度汇总只统计已有财务口径的行。已关闭账款仍如实显示剩余未收或未付，并标为需要线下沟通处理的异常，不在导出表展开关闭原因。业务源约定金额与财务账款金额不一致时不阻止导出，须并列显示两种金额及实际收付并标为需核对；主表应收/应付仍以业务源事实为准，收付进度以财务账款为准，该异常行不进入正常未收/未付汇总。发团、客源、行程段与资源各自的非空备注跟随所属事实完整导出，不合并成无归属的大段文字；司机、导游、车牌等执行班组信息第一版不新增结构化字段，可暂随相关备注表达。缺失的后续事实保持未发生或待补充，不伪装成零或已完成，并明确标注导出时点与数据阶段。第一版交付形态为页面预览与带打印版式的 Excel（`.xlsx`）下载，PDF 后置；页面与 Excel 都使用一张表的纵向分区顺序：发团与数据阶段、客源及应收、行程段资源及应付、待确认款项、财务汇总与异常、发团级备注，不拆多工作表，也不做一张超宽同行表。Excel 默认 A4 横向、宽度缩放为一页、高度自然分页，重复关键表头并尽量避免拆开同一行程段；备注自动换行，异常同时使用文字而不只依赖颜色。拥有发团菜单权限的计调、财务与企业管理员看到同一完整内容，不按角色隐藏金额或游客代表电话。导出文件标注所属 Organization、导出人及快照时间，后端继续强制 Organization 数据隔离；每次即时生成当前快照，不在系统内保存文件副本或导出历史。客源仍属整趟发团，不嵌进行程段；第一版不引入每日行程实体。
_Avoid_: 行程日报, 日报表, 运营版与结算版并行, 仅含运营事实不含财务进度, 仅结算完成后允许导出, 用 0 代替尚未发生或待补充, 行程总账本, 客源挂行程段, 第一版含单价×数量或执行班组

**Payment Schedule（收付款节点）**:
Organization 内的计划应收或计划应付，承载约定金额、到期日、往来对象与来源追溯。从客源单或行程段资源 **显式触发** 生成；保存源事实本身不产生收付款节点。结清状态从核销分配派生，不是发团或客源单的属性。产品 UI 在应付侧称 **应付单**、应收侧称 **应收单**；编号字段展示 `scheduleNo`（ADR-0003 格式）。到期日对应收是账期事实；对应对付本版不作为账期、也不驱动逾期，产品 UI 不暴露应付到期日。
_Avoid_: 应收, 应付, 账款, Departure Receivable, 发团应收, 节点编号（作登记抽屉只读 label）, 应付到期日作账期或逾期依据（本版）

**Receivable Due Date（应收到期日）**:
应收收付款节点上的约定账期日。从客源单显式生成时，默认按所属发团 **出团日** 所在自然月的 **下一自然月 10 日** 写入（12 月出团则次年 1 月 10 日）；同一客源单下客户补款与游客代收路径共用该默认值。写入后为生成时快照，出团日变更不回写；未财务介入前可手改，介入后锁定。仅应收到期日与业务日比较可派生逾期。
_Avoid_: 用结束日作应收到期日默认, 按生成当日推算账期, 双路径不同默认到期日, 出团日变更自动改已生成到期日, 应付账期（本版）

**Payment Schedule Status（收付款节点状态）**:
后端存储的结清态枚举：`pending`（未结清且未逾期）、`overdue`（未结清且已逾期）、`settled`（已结清）、`cancelled`（已关闭）。**不**单独存 `partial`；部分核销后只要未结清金额大于零，状态仍为 `pending` 或 `overdue`。`overdue` **仅适用于应收**；应付未结清只可能是 `pending`（或已结清/已关闭），本版不因到期日进入逾期。
_Avoid_: 部分收款/部分付款（作存储状态）, 待收款/待付款（作枚举值）, 应付 overdue, 双向对称逾期

**Payment Schedule Settlement Label（收付款节点结清展示）**:
列表与详情面向财务人员的结清进度文案，由方向 + 已核销金额 **展示层派生**，不写入数据库。应收侧：待收款、部分收款、已收清；应付侧：待付款、部分付款、已付清。节点是否已关闭由 **Payment Schedule Status**（`cancelled`）单独表达，**不**用「已关闭」替代结清进度文案；已关闭且仍有未结清时，结清进度仍可为部分收款/部分付款。仅应收的 `overdue` 可在标签上叠加「已逾期」，不替代上述结清进度文案；应付本版不展示已逾期。
_Avoid_: 与 `Payment Schedule Status` 枚举一一对应, 新增 partial 枚举, cancelled 时用已关闭盖掉部分收款/部分付款, 应付已逾期标签

**Payment Schedule Close（关闭节点）**:
财务人员终止该收付款节点后续追收/追付的动作，将节点置为 `cancelled`（已关闭）。关闭 **不是** 消化或放弃未结清金额，也 **不是** 作废整条财务记录；它仅用于财务已经介入且仍有未结清金额时，因商务争议、转外部或专项处理等原因停止继续跟进。财务未介入的资源应付只能作废、不能关闭；已结清节点不可关闭。关闭不撤销、不抹除已有核销，不改写约定金额；关闭后不可新增核销，已有核销仍可查看与撤销，撤销后节点默认保持已关闭。关闭后节点上仍展示约定 / 已核销 / 未结清（未结清不归零）；该未结清 **不再计入** 发团开放待收/待付。关闭必选处置类型并必填具体说明，自动保留当时约定/已核销/未结清快照、操作人与时间；有有效核销时，确认弹窗提示将停止后续收付且已核销与未结清仍保留在节点上。不引入「已放弃金额」，不造流水、不造核销。
_Avoid_: 已放弃金额, 单一自由文本吸收所有纠错, 金额减免作关闭原因, 关闭即未结清归零, 财务未介入时关闭资源应付, 已结清仍可关闭, 关闭即撤销核销, 关闭等同删除或作废, 关闭原因可选, 关闭等同作废（整条不计入财务）, 关闭时改写约定金额, 用关闭纠正未介入的误生成

**Resource Payable Void（资源应付作废）**:
财务尚未介入时撤销误生成资源应付节点的纠错动作；仅不存在任何核销历史（包括已撤销记录）、从未明确调整约定金额且尚未关闭的资源应付节点可作废。作废与关闭严格互斥：未介入资源应付只能作废、不能关闭；财务介入后只能在仍有未结清时关闭、不能作废。作废原因必填，使用不超过 200 字的具体说明，不另设结构化作废类型；系统同时记录作废前金额、操作人与时间。作废节点不再作为有效确认应付参与汇总与进度，但仍永久保留来源、金额与完整审计事实；应付列表默认排除已作废节点，状态筛选增加「已作废」，仅用户主动筛选时进入只读审计视图。作废后对应资源恢复为未生成、金额字段解锁；计调修正源事实后可生成新节点，同时保证同一资源最多只有一个有效应付节点。第一版仅支持资源应付，不支持客源应收作废。
_Avoid_: 删除节点, 默认列表展示已作废节点, 无法筛选已作废节点, 作废原因可选, 为作废复制关闭处置类型, 用关闭代替作废, 财务介入后作废, 未介入时同时展示作废与关闭, 客源应收作废, 作废后仍计入确认应付, 作废清除审计记录, 作废后资源仍锁定, 作废后禁止重新生成, 同一资源存在多个有效应付节点

**Payment Schedule Close Disposition（节点关闭处置）**:
解释未结清仍存在时为何停止当前节点追收/追付的结构化决定，处置类型为「转外部或专项处理」「商务争议停止跟进」「其他」，并搭配必填具体说明。它不改变约定金额或证明义务消灭；金额重新议定走约定金额调整，错误核销走撤销核销，错误流水走作废或反向资金记录。
_Avoid_: 已放弃金额, 金额减免, 流水录错, 核销错配, 暂时未收/未付（保持节点开放）, 无具体说明的其他

**Payment Schedule Reopen（重新打开节点）**:
财务人员在关闭决定不再成立时，恢复原收付款节点后续追收/追付的明确动作。重新打开原因必填并保留原关闭履历；打开后根据当前有效核销与到期日重新派生待收/待付、部分收付或逾期进度，沿用原节点并恢复登记收付与匹配流水，不生成新节点。若所属发团已结清，财务须明确确认节点打开与发团回退待结算的联动影响；两者作为一个不可分割的业务动作完成，无需 OP 预先审批，但发团回退记录必须对 OP 可见。
_Avoid_: 删除关闭记录, 无原因恢复, 为恢复追收/追付而新建节点, 重新生成应收/应付, 打开后恢复普通金额编辑, 先打开节点后回退发团, 节点开放但发团仍已结清

**Payment Schedule Activity（收付款节点操作记录）**:
挂在该条收付款节点上的可读操作时间线。第一版覆盖：关闭节点（含原因与当时约定/已核销/未结清快照）、关闭后撤销核销（含已核销与未结清变化）、重新打开节点（含打开原因与打开后结清进度），以及明确调整约定金额（含调整前后金额、原因与操作人）。用于财务溯源，与全局核销列表互补，不替代核销实体本身；生成、普通编辑、登记、匹配等完整履历不在第一版范围。
_Avoid_: 仅全局核销/流水侧留痕, 第一版只存关闭字段不做节点时间线, 调整金额不记原因, 用流水详情操作记录代替节点履历, 第一版做完整节点 audit（生成/编辑/登记/匹配一并纳入）, 已放弃金额

**Payment Schedule Financial History（收付款节点财务履历）**:
表示节点是否曾发生核销（含后续已撤销）、调整或关闭等财务动作的不可逆历史事实。它与当前有效已核销金额独立：撤销最后一笔核销可使有效已核销归零，但不会清除财务履历或恢复为「从未财务介入」。
_Avoid_: 用当前已核销金额代替财务履历, 撤销最后一笔核销后履历归零, 从未财务介入

**Payment Schedule Amount Adjustment（收付款节点约定金额调整）**:
已有财务履历的源事实收付款，不因有效核销归零而恢复普通金额编辑；节点未关闭且当前有效核销为零时，可通过明确调整动作同步修正源事实与原节点，不新建节点。资源应付同步修正原资源约定金额；客源应收按节点来源定位客户补款或游客代收路径并同步该路径金额，不得静默修改或重建同一客源单的其他路径。调整原因必填并记录调整前后金额与操作人；仍有有效核销须先撤销，已关闭节点不可调整。
_Avoid_: 撤销核销后恢复普通金额编辑, 永久锁死无纠错入口, 调整时新建收付款节点, 源事实与节点金额分叉, 调整一路径静默改其他路径, 无原因调整

**Source Order Receivable Status（客源单应收状态）**:
客源单级结清进度展示态，由下属应收收付款节点聚合派生：未生成、待收（已生成且未收款）、部分收款、已收齐；若曾生成的应收节点均已关闭，展示为已关闭，**不**回退为未生成，亦**不**因关闭前曾有部分核销而展示为部分收款或已收齐。仅「未生成」允许触发财务生成；其余状态客源列表操作为「查看应收」（跳转本发团应收管理，预填往来对象筛选为该客源单发客客户/合作伙伴，并对该客源单下全部应收行——含已关闭节点——做一次高亮闪烁定位，非选中态），不可再次生成。结清进度聚合时忽略已关闭节点：仅部分路径关闭时，状态仍由剩余活跃节点派生（待收/部分收款/已收齐），不因单条关闭整单变为已关闭。已关闭时客源单不可删除、金额字段只读，非金额字段可改。
_Avoid_: 已生成（作待收的正式 label）, 关闭后等同未生成, 用节点级结清文案直接当客源单状态枚举名, 重新生成, 关闭后可删源事实或再改金额并生成, 任一路径关闭即整单已关闭, 全部关闭且曾有核销改展示为部分收款/已收齐, 查看应收隐式按 ID 过滤且筛选区空白

**Segment Resource Payable Status（资源应付状态）**:
行程段资源级结清进度展示态，由该资源关联的有效应付收付款节点派生：未生成、待付（已生成且未付款）、部分付款、已付清；撤销最后一笔有效核销且节点未关闭时，从部分付款/已付清回到待付，仍视为已生成并沿用原节点继续登记付款或匹配流水。若有效应付节点已关闭，展示为已关闭，**不**回退为未生成，亦**不**因关闭前曾有部分核销而展示为部分付款或已付清。仅「未生成」可生成应付；待付、部分付款、已付清与已关闭状态均进入「查看应付」，不可再次生成。未介入节点作废后不再视为有效节点，资源恢复为未生成并允许修正金额后生成新节点；旧作废节点仍保留审计但不参与状态聚合。已关闭时资源不可删除、金额字段只读，非金额字段可改。
_Avoid_: 已生成（作待付的正式 label）, 撤销核销后回退未生成, 撤销核销后要求新增资源, 关闭后等同未生成, 关闭后重新生成, 作废后仍视为已生成, 作废节点参与资源应付状态, 同一资源存在多个有效应付节点, 关闭后可删资源或再改金额并生成, 全部关闭且曾有核销改展示为部分付款/已付清, 查看应付隐式按 ID 过滤且筛选区空白

**Finance Generation（财务生成）**:
计调或财务在源事实确认后手动触发的动作，将客源单转为应收收付款节点、将行程段资源转为应付收付款节点。有效节点遵守一次性入口：同一来源已存在有效应收/应付（含已关闭节点）后不可再次生成；已生成且未 finance-touched 时，编辑源事实保存可同步已有节点金额，并对客源收款拆分中新出现、金额大于零且尚无有效节点的路径 **补建** 收付款节点；已 finance-touched 后源事实变更仅警示、不静默改账款，亦不补建。资源应付的未介入节点作废后不再视为有效节点，允许修正资源金额并生成一个新节点；作废历史不删除，生成 API 必须保证同一资源最多一个有效应付节点。发团级「批量生成应收」与行程段级「批量生成应付」是对「未生成」源事实的显式批量触发，内部仍走单条生成约束；不是保存即生成，也不是后台自动批量生成。
_Avoid_: 保存即生成（首次）, 批量自动生成, 团款确认（Project 旧称）, 关闭节点后再次生成, 用再次点生成补路径, 作废后仍禁止资源应付重新生成, 删除旧节点后重建, 同一来源存在多个有效节点

**Partner（合作伙伴）**:
与 Organization 有业务往来的其他旅行社或同业客户。后台菜单、表单抽屉标题、名称字段等名录 CRUD 界面统一使用 **合作伙伴**（如「合作伙伴名称」「创建合作伙伴」），不用「同行」。在 **客源单** 等业务场景中，UI 可称 **客户** 指代所选 Partner（如「发客客户」「客户数量」），与名录管理用语并存。
_Avoid_: 同业, 客户/同行, agent, 同行名称, 编辑同行, 名录 CRUD 界面用「客户」代替「合作伙伴」

**Partner Kind（合作方向）**:
Partner 在业务往来中的方向角色：仅作客户方（发客/客源）、仅作承接方（拼出承接）、或双向合作。存为 `partnerKind`；产品 UI label **合作方向**。与 Partner Type 正交，共同构成 Partner 的双维分类。合作伙伴列表页展示 **统计卡**：在 **启用** 状态下按 `partnerKind` 互斥计数（总数、客户方、承接方、双向合作）。
_Avoid_: 业务身份, 合作类型, 与 Partner Type 混为一列, 统计卡含已归档或停用

**Partner Type（合作伙伴类型）**:
Partner 作为旅行社主体的类型分类，如组团社、地接社、渠道商、综合旅行社、其他。存为 `partnerType`；产品 UI label **合作伙伴类型**（列表/表单）。与 Partner Kind 正交。Partner Epic 1 仅交付名录 CRUD；拼出/客源等业务选择器的过滤规则在对应业务 Epic 再定。
_Avoid_: 同行类型, 与 Partner Kind 共用同一枚举, wholesaler 作 UI label「批发商」, Epic 1 预埋拼出筛选

**Partner Contact Role（联系人角色）**:
Partner 主联系人的职务，如老板、计调、财务、销售、客服、其他。可选字段，仅 Partner 目录表单使用；Supplier 无对应字段。
_Avoid_: 与 Employee Role 混淆, 创建时强制填写

**Partner Directory Form（合作伙伴目录表单）**:
Partner 创建/编辑/详情只读共用的 **三段** 结构：基础信息（含名称、合作伙伴类型、合作方向、状态）、联系人信息（含联系人角色）、结算信息（结算方式、账期规则、结算说明，整段可选）。**不含** Supplier 的「更多财务信息」与独立「备注」段；Partner 无发票、银行、参考报价、业务备注字段。创建时 **合作伙伴名称、合作方向、合作伙伴类型** 三者必填；联系人、结算字段均可选。列表交互与 Supplier 一致：名称链至详情页，操作列「编辑」开列表抽屉，详情顶栏「编辑」开详情抽屉；**不**采用单抽屉 view/edit 双模式。详情页 Tab：**基本信息** + **往来账款**（见 Partner Ledger Tab）+ **合作团单**（业务事实层容器，内含客源与拼出两分段，见 Partner Source Order 与 Partner Outsource Order）。
_Avoid_: 与 Supplier 共用五段表单, 为 Partner 增加 Supplier Financial Profile, 创建时 partnerType 可选, 单抽屉查看/编辑模式, 把客源与拼出糊成同一类行

**Partner Cooperation Orders Tab（合作团单 Tab）**:
Partner 详情中的**业务事实层** Tab 容器，产品 UI label **合作团单**。内含两个分段控件：**客源**（见 Partner Source Order）与 **拼出**（见 Partner Outsource Order）。分段控件独立于筛选条（与往来账款应收/应付同构：视图选择 ≠ 列表筛选）。三类合作方向均露出两段；默认分段为纯承接方→拼出、其余→客源；切换合作伙伴时重置分段为该 Partner 默认并清空出团日期筛选。出团日期筛选在 Tab 级共享，切换分段保留日期并将分页重置为第一页。空态按**当前分段**判断与文案，不因另一分段有数据而自动跳段。客源与拼出是两类不同业务事实（收入侧客源单 vs 成本侧拼出资源），不是「同一类团单、方向相反」。
_Avoid_: 按合作方向隐藏某一分段, 整 Tab 联合空态并自动跳段, 把拼出建成反向客源单, 分段标签写成「客源团单／拼出团单」叠词, 混成一张同构表, 分段控件塞进筛选条, 切换合作伙伴后保留上一档案的分段选择

**Partner Source Order（合作团单·客源）**:
合作团单 Tab 的**客源**分段：行＝该 Partner 名下客源单（跨发团），回答「这家发了哪些团、金额怎么构成」。汇总卡六项（客源单数/总人数/原始团款合计/优惠合计/结算金额合计/游客代收合计）跟随筛选、所见即所导；按所属发团出团日期筛选，默认不限＋倒序。列上有意不放应收状态与已收/未收（归往来账款 Tab），也不放游客代表与联系电话（仅出现在导出物）。《往来账确认单》导出的全系统唯一入口在本分段工具栏（不在拼出分段）。
_Avoid_: 与往来账款合并成一个对账视图, 列上放已收/未收或应收状态, 分段内用客户习惯名, 确认单入口挂到拼出分段或别处再开

**Partner Outsource Order（合作团单·拼出）**:
合作团单 Tab 的**拼出**分段：行＝引用该 Partner 为承接方的拼出行程段资源（Segment Resource，`resourceKind=拼出`，跨发团），回答「哪些团把服务拼给了这家、约定成本多少」。列只放业务事实：出团日期、关联发团、行程段、资源名称、约定金额、备注（本段种类恒为拼出，不展示资源种类列）；**不**放应付状态/已付/未付（归往来账款 Tab）。汇总卡三项（资源行数/关联发团数/约定金额合计）跟随筛选、所见即所算；按所属发团出团日期筛选，默认不限＋出团日期倒序。列出全部该类资源行，不看应付是否已生成或作废，也不看发团状态——与供应商服务团单同理。第一版无拼出对外结算单导出（确认单仍只含客源）。
_Avoid_: 行＝客源单或反向客源, 与客源混成同构行, 列上放应付状态或已付未付, 展示冗余的资源种类列, 只列已生成应付的资源, 排除已关闭/归档发团的资源, 把拼出塞进往来账确认单

**Partner Ledger Tab（往来账款 Tab）**:
Partner 详情中的**财务账款层** Tab，行＝与该 Partner 往来的收付款节点，回答「这家欠我/我欠这家多少、收付到哪一步」。应收、应付两个子区各复用一次 PaymentScheduleWorkspace，数据按 `counterpartyType=partner + counterpartyId` 精确过滤；游客代收节点（counterparty=guest）不属于该 Partner 账款、不出现在本 Tab。汇总卡每方向三项（约定/已核销/未结清）跟随筛选，已关闭、已作废节点不计入；应收侧约定合计拆显「客户补款／其他应收」以对确认单。主时间轴为所属发团出团日期区间（与确认单周期同口径，手工其他应收随其归属发团落入区间），另保留到期日筛选做催收。Tab 内完整操作，与全局应收/应付页一致。
_Avoid_: 列上放人数/单价/优惠, keyword 模糊匹配往来对象, 游客代收计入该 Partner 账款, 汇总含已关闭或已作废节点, 只读＋跳全局页

**Partner Reconciliation Statement（往来账确认单）**:
按对账周期面向单一 Partner 生成的对外对账单据，供对方签章确认。周期口径为所属发团出团日期区间；行＝周期内该 Partner 全部客源单，明细按出团日期正序，应收节点已关闭的客源单照常列入且不做标记。只含业务事实与收款拆分，不含核销进度，也不含手工应收（其他应收），**不含**拼出资源。对外命名沿用客户习惯名，与系统规范名映射：拼入合计＝原始团款合计、原始应收＝原始团款、实际应收＝结算金额、客户已收押金＝客户补款、游客代收同名；该豁免仅限本导出物，系统 UI 仍用规范名。单价按「拼入单价（成人）／拼入单价（儿童）」拆列，与成人/儿童人数列对应，逐行可复算原始应收。保留游客代表与联系电话（信息源头即收件方），客人名单为空则留空；不设客户单号列。即时生成当前快照、不存副本，签回与确认状态走线下，系统内不跟踪。全系统唯一入口在合作团单 Tab 的**客源**分段工具栏。
_Avoid_: 客户习惯名作系统 UI 文案, 确认单含核销进度, 手工应收上确认单, 拼出资源上确认单, 剔除或标记已关闭应收的客源单, 客户单号列, 系统内跟踪签回状态, 月度对账单（作正式名）, 确认单入口挂到拼出分段

**Supplier（供应商）**:
为发团提供资源的服务商，如酒店、用车、餐、景区等。供应商档案是 Organization 内的自营资源名录，主路径是在发团执行安排中选 supplier，而非独立 ERP。一家供应商可同时提供多种资源（多选 **供应商类别**）。
_Avoid_: 资源商, vendor, 车队/餐厅（作与用车/餐对立的另一套主体称呼）

**Directory Profile Status（目录档案状态）**:
Partner 与 Supplier 目录记录的生命周期状态：启用、停用、已归档。适用于目录实体，不适用于 Employee（Employee Status 仅启用/停用）或 Organization 账号。
_Avoid_: 复用 Employee Status, 用 deletedAt 表达归档

**Directory Archive（目录归档）**:
将 Partner 或 Supplier 从默认目录列表中移除，同时保留行以供历史追溯。归档后状态为已归档；记录仍可从历史发团、资源、财务上下文中链接，但不得出现在默认目录列表或新业务的选择源中。产品 UI 可将此操作标为 **删除**，当用户意图是从日常名录工作中不再看到该档案时。默认目录列表不展示已归档行；用户可通过「显示已归档」查看并 **恢复**。
_Avoid_: 归档当作数据库硬删, 归档与停用混为一谈, 归档后不可恢复

**Directory Restore（目录恢复）**:
将已归档的 Partner 或 Supplier 恢复为非归档状态，使其重新出现在默认目录列表。恢复后状态通常为启用，是否改回停用由用户在编辑抽屉中另行调整。
_Avoid_: 恢复当作新建, 恢复后自动硬删历史引用

**Supplier Disabled（供应商停用）**:
供应商目录状态为停用。档案仍在非归档的目录管理视图中可见，但不得在新业务（如资源安排、应付对手方选择）中被选用。
_Avoid_: 停用当作归档, 停用后从列表隐藏

**Partner Disabled（合作伙伴停用）**:
Partner 目录状态为停用。档案仍在非归档的目录管理视图中可见，但不得在新业务（如客源单选客户、行程段拼出选承接方）中被选用。Partner Epic 1 仅实现名录 CRUD 与 lifecycle API，**不在**业务选择器中 enforce；enforcement 在客源/行程段 Epic 落地。
_Avoid_: 停用当作归档, Epic 1 在选择器中拦截停用或归档 Partner

**Directory Name Uniqueness（目录名称唯一）**:
同一 Organization 内，Partner 与 Supplier 各自按 **名称** 唯一；不得存在两条同名且均未归档的目录记录。保存重名时拒绝并提示用户修改名称或在名称中加入可区分后缀（如门店、区域）。
_Avoid_: 允许同名靠备注区分, 全局跨 Organization 唯一

**Directory Catalog（目录层共用）**:
Partner 与 Supplier 在名录层的共用概念与展示规则，包括三态生命周期、归档/恢复、名称唯一、结算字段 enum 与 UI label（账期规则、结算说明）。表单段数与字段集 **不必相同**：Supplier 为五段且含更多财务信息与备注；Partner 为三段且无发票/银行。Epic 先交付 Supplier，Partner 后续 Epic 复用目录层生命周期与结算 catalog，而非两套独立 CRUD。实现 Partner 时 **inline** 从 Supplier 抽出共用 catalog（如 `features/directory/`），不单独前置 refactor sprint。
_Avoid_: Partner 与 Supplier 各写一套归档规则, 目录层与 Employee 生命周期混用, 强行统一五段表单, Partner 全量 copy 后再做 directory 抽取

**Directory Payment Term Rule（账期规则）**:
Partner 与 Supplier 目录层的结算节奏，如每团结、周结、半月结、月结、按约定。Supplier 存为 settlementCycle，Partner 存为 paymentTermRule，产品 UI 统一 label **账期规则**。
_Avoid_: 结算周期, 账期, payment term 混用在目录表单

**Directory Settlement Notes（结算说明）**:
Partner 与 Supplier 目录层的自由文本，说明结算例外或对账补充（如「出团后 7 个工作日结」）。产品 UI 统一 label **结算说明**。
_Avoid_: 结算备注, 账期说明

**Supplier Reference Quote Notes（供应商参考报价说明）**:
Supplier 目录层的自由文本，描述典型报价方式（如人均价、含餐、淡旺季）。仅供计调参考，不是结构化价格事实，不驱动资源金额或应付。
_Avoid_: 默认单价, 参考价作系统计价

**Supplier Category（供应商类别）**:
Supplier 名录中对该服务商 **可提供哪些资源种类** 的分类，为 **非空多选**（无主类别）。取值类型与资源行的 **资源种类** 相同（Resource Kind），但 **不含拼出**——拼出只属于资源行。列表与执行安排选供应商时，按「类别集合包含所选资源种类」筛选。编辑类别时不得移除仍被关联资源行使用的种类；资源行保存时，非拼出的资源种类必须属于该供应商的类别集合。
_Avoid_: 单选类别, 主类别+附加类别, 与资源种类维护第二套枚举, SupplierCategory 独立枚举类型, 类别当作与资源种类无关的另一套取值, 把拼出写入供应商类别, 餐厅/车队/票务（作与餐/用车/门票并列的旧 label）

**Supplier Business Notes（供应商备注）**:
Supplier 目录层关于合作事实的自由文本（如接待上限、是否支持加单）。表单与只读区 section 标题为 **备注**，非「业务备注」。
_Avoid_: 业务备注, 用备注代替结算说明

**Supplier Financial Profile（供应商更多财务信息）**:
Supplier 目录层可选区块：是否可开票、发票类型、税率、收款银行账户。在创建/编辑表单中 **默认折叠**；收款账户可再内层折叠。轻量创建时不强制填写。
_Avoid_: 创建时强制填银行账号, 与结算信息混为一段

**Supplier Service Order Tab（供应商服务团单 Tab）**:
Supplier 详情中的**业务事实层** Tab，行＝引用该 Supplier 的行程段资源（Segment Resource，跨发团），回答「这家在哪些团、被安排了什么资源、约定成本多少」。Tab label 用 **服务团单**（**不**沿用 Partner 的「合作团单」，因供应商不发团、语义不符）。列只放业务事实：出团日期、关联发团、行程段、资源种类、资源名称、约定金额、备注；**不**放应付状态/已付/未付（归往来账款 Tab）。汇总卡三项（资源行数/关联发团数/约定金额合计）跟随筛选、所见即所算；按所属发团出团日期筛选，默认不限＋出团日期倒序。列出全部引用该 Supplier 的**非拼出**资源行，不看应付是否已生成或作废，也不看发团状态（编辑中/待结算/已结清/已关闭均含）——因「被安排提供该资源」这个业务事实已发生（与 Partner 合作团单含全部客源单同理）。第一版无对外结算单/对账单导出（后置迭代）。
_Avoid_: 沿用「合作团单」label, 行＝客源单, 行＝发团再展开子行, 列上放应付状态或已付未付, 只列已生成应付的资源, 排除已关闭/归档发团的资源, 含拼出资源, 第一版做供应商结算单导出

**Supplier Ledger Tab（供应商往来账款 Tab）**:
Supplier 详情中的**财务账款层** Tab，行＝与该 Supplier 往来的**应付**收付款节点，回答「我欠这家多少、付到哪一步」。数据按 `counterpartyType=supplier + counterpartyId` 精确过滤，复用 PaymentScheduleWorkspace（scope='supplier'）。**仅应付方向**，不设应收/应付切换——供应商结构上只有应付（供应商退款是收入流水而非应收节点）；极罕见的手工「供应商应收」只在全局应收页可见，不进本 Tab。汇总卡三项（约定/已核销/未结清），已关闭、已作废节点不计入，跟随出团日期筛选。Tab 内财务操作（登记付款/匹配流水/关闭/重开/调整约定金额/核销）按 `canMutateFinance(menuKeys)` gating：财务可操作，计调等非财务角色**只读**（可看账款但不显示操作按钮），与发团详情账款口径一致；Partner 往来账款 Tab 同步对齐该 gating。
_Avoid_: 设应收/应付切换, 把手工供应商应收纳入本 Tab, 汇总含已关闭或已作废节点, 计调可登记付款或核销, 仅靠后端 403 不做前端 gating, 暴露应付到期日或逾期

**Finance Transaction（收支流水）**:
Organization 内的资金进出记录。资金层与账款层分离：新建/编辑流水只要求金额大于零，**不**按收付款节点约定金额或客源路径金额做累计上限，因此允许单笔或合计超过约定的超额收款/超额付款（多出部分以未核销流水保留在资金卡）。登记收款/付款与核销则硬拦，核销金额不得超过节点未结清与流水未核销余额，节点不会被核销超过约定。游客代收流水表单的「建议金额 / 填入」仅作录入参考：以节点未结清（或尚未生成应收时的路径金额）为上限，并扣减同客源单上已有未核销游客代收流水后给出剩余建议，不构成后端硬拦。
_Avoid_: 流水账, 记账, 以建议金额当硬上限, 资金流水强制不超过约定账款, 用核销规则约束未关联节点的流水录入

**Departure Finance Transaction Tab（发团收支流水 Tab）**:
发团详情中的资金工作区 Tab，正式 label **收支流水**，位于应付管理与核销记录之间。仅展示 `departureId` 等于当前发团的流水；能力与全局收支流水页同级（新建、编辑、作废、去核销、详情），新建时关联发团锁死为当前发团，编辑时关联发团只读。发团 scope 不默认交易日期近窗；概览「查看流水」切本 Tab（可带收支方向），不跳全局页。发团 scope 下「查看核销」进本发团核销记录 Tab。全局 `/finance/transactions` 仍保留跨团对账与按发团筛选，不重定向。漏挂发团的流水第一版不提供认领，须在全局页编辑补关联。
_Avoid_: 团单流水 Tab, 用核销关系扩大本团流水集合, 发团 Tab 默认近 30 天, 概览查看流水跳全局, 发团内编辑可改走关联发团

**Departure Verification Tab Date Filter（发团核销 Tab 日期）**:
发团详情「核销记录」Tab 与发团收支流水 Tab 对齐：不默认核销日期近窗；深链「查看核销」仍清空日期并按流水号/节点编号筛选。
_Avoid_: 发团核销 Tab 默认近 30 天, 仅深链清日期而直接进入仍带近窗

**Transaction Direction（收支方向）**:
收支流水的资金方向，存储为 `inflow` / `outflow`。产品 UI 统一 label **收入** / **支出**（不用「流入 / 流出」）。收入流水用于应收核销；支出流水用于应付核销。
_Avoid_: 流入, 流出, income, expense（作存储枚举值）

**Transaction Writeoff Amount（流水核销金额）**:
流水上已被核销记录占用的金额及其余额，由 Verification 分配 **派生**，不单独写入流水表。产品 UI label：**已核销金额**、**未核销金额**（不用「已分配 / 未分配」）。后端字段可保持 `allocatedAmountCents` / `unallocatedAmountCents`。
_Avoid_: 已分配, 未分配, writtenOffAmount（第一版不必改 API 字段名）

**Transaction Writeoff Status（流水核销状态）**:
流水可核销进度的展示状态，由已核销金额与流水金额 **派生**，不写入数据库：未核销（已核销 = 0）、部分核销（0 < 已核销 < 流水金额）、已核销（已核销 = 流水金额）。
_Avoid_: 与收付款节点 `Payment Schedule Status` 混为一谈, 新增独立存储枚举（第一版）

**Counterparty（往来对象）**:
收付款节点与收支流水上标识资金往来一方的字段，存储为 `counterpartyType` + `counterpartyId` / `counterpartyName`。产品 UI 统一 label **往来对象**、**往来对象类型**（不用「交易对象」）。类型枚举：合作伙伴（Partner）、供应商（Supplier）、游客代收（Guest，存储值仍为 `guest`）。新建/编辑流水时：合作伙伴、供应商须从目录档案选择（写入 `counterpartyId`）；游客代收须先选关联发团再选客源单（仅列出该发团下 `guestCollectCents > 0`、即存在游客代收路径的客源单；`counterpartyId` = 客源单 id），名称可手改（如客人姓名）仅作展示。收支方向联动默认类型：收入 → 合作伙伴；支出 → 供应商。新建流水关联发团必填，表单中靠前填写。
_Avoid_: 交易对象, 客户/供应商/其他（作流水表单枚举 label，与 Partner/Guest 等领域类型混用）, 流水表单仅手输名称不接档案（含已删除的「手工录入 / manual」往来类型）, 客人流水无客源单 id 仍可核销游客代收, 新建流水关联发团可选, 往来类型 UI 仍称「客人」（与应收节点「游客代收」不一致）

**Payment Channel（收付款通道）**:
收支流水上记录资金实际经过的通道分类，固定枚举：现金、银行转账、微信、支付宝、其他（存储如 `cash` / `bank_transfer` / `wechat` / `alipay` / `other`）。凡 **新建** 收支流水（含登记收/付款与流水页手动新建）均必选；匹配已有流水时只读展示。登记抽屉 UI label：应付侧 **付款通道**、应收侧 **收款通道**（不用「付款账户/收款账户」）。仅用于分类与列表展示，第一版 Organization 不可自定义通道；不做本社账户台账、余额或多账户管理。新建流水 **不要求** 同时存在收付款节点；可先记流水，后续再通过匹配流水核销到节点。
_Avoid_: 付款账户, 收款账户（登记抽屉 UI label）, 银行卡（UI 文案，用「银行转账」）, 账户余额, 多账户 CRUD, 新建流水必须先有收付款节点

**Verification（核销）**:
收付款节点与收支流水之间的金额分配记录，将实际资金进出与计划应收/应付对齐。核销要求两侧方向、往来对象与 Organization 一致；往来对象一致性由流水与节点硬匹配保证（有 `counterpartyId` 时按 id，双方皆无 id 时按名称），不在新建核销表单上单独录入或筛选往来对象。流水与节点可分属不同发团（跨团核销）：同行一笔款项结清多个发团账款是真实业务，不得为对齐发团要求拆分真实流水；跨团组合提交前给出一次确认警示，但不阻止核销，也不视为数据异常。
_Avoid_: 结算, 对账, 新建核销顶部独立「往来对象」筛选项（与候选区搜索重复）, 游客代收仅靠名称匹配, 强制流水与节点同团, 为对齐发团拆分真实流水, 跨团核销作数据异常

**Create Verification（新建核销）**:
财务人员手工选择流水与收付款节点并建立核销关系的入口（`CreateVerificationDrawer`）。基础信息含核销方向、核销日期、可选关联发团；流水/节点候选靠各自搜索框按编号、往来对象或发团模糊过滤。不提供独立「往来对象」筛选项；选中流水后，节点候选仅保留往来对象一致者。
_Avoid_: 新建核销必填往来对象, 顶部往来对象过滤与候选区搜索双轨并存

**Cancel Verification（撤销核销）**:
解除收支流水与收付款节点之间的有效金额分配：原流水仍是已发生的资金事实，其金额回到未核销可用余额；原节点已核销金额下降、未结清金额上升。撤销核销不作废流水、不表示资金未发生或已退回；流水本身错误应另走作废或反向资金记录。
_Avoid_: 撤销付款, 撤销收款, 删除流水, 已放弃金额, 资金自动退回

**Match Transaction（匹配流水）**:
财务人员将 **已有** 收支流水与收付款节点建立核销关系的操作。登记收付款则相反：无流水时新建流水并同时核销。产品 UI、按钮与组件统称「匹配流水」；后端 HTTP 路径保持 `link-transaction`，不因产品改名而变更 API。第一版：右侧抽屉、账款/流水候选/已选流水/本次核销四块、收支方向过滤、默认核销金额取 min(流水可核销余额, 节点未结清)；**推荐标识**（同对象/同团单/金额一致）延后迭代。
_Avoid_: 关联流水（产品文案）, 匹配流水（作 API 路径名）

**Verify From Transaction（去核销）**:
财务人员从 **收支流水列表** 从某条流水发起核销的操作，与「匹配流水」互为镜像：匹配流水从收付款节点出发选流水；去核销从流水出发选收付款节点。收入流水自动进入应收核销候选；支出流水进入应付核销候选。第一版用独立抽屉 `VerifyFromTransactionDrawer`（布局镜像 `MatchTransactionDrawer`），提交复用 `createVerification`。「查看核销」跳转核销列表，经顶部筛选区按 **流水号**（`transactionNo`）过滤，不按实体 ID、不另开临时筛选条。
_Avoid_: 复用手工双下拉的核销 Modal 作第一版入口, 查看核销作独立详情抽屉（第一版）, 查看核销按 transactionId 深链, 当前筛选临时 Tag 承载深链

**Verification List Deep Link（核销列表深链筛选）**:
从流水或收付款节点「查看核销」进入核销列表时，URL 只带一侧业务编号（`transactionNo` 与 `scheduleNo` 互斥），不带实体 ID、不带 match mode、不展示「当前筛选」条；灌入顶部对应输入框，并清空日期与其余筛选项。URL 有编号时前端以精确匹配调列表 API（`*Match=exact`，大小写不敏感）；用户改任一编号输入即清掉 URL 编号，此后为普通模糊搜索（默认 `contains`）。重置恢复默认日期并清空 URL。旧 `transactionId` / `paymentScheduleId` 深链不再兼容。列表 API 删除按 ID 过滤参数。
_Avoid_: transactionId / paymentScheduleId 作核销列表可分享深链, 深链与顶部筛选双轨并存, 深链保留默认近 30 天日期, URL 暴露 match mode, 流水号与节点编号深链同时锁定

**Void Transaction（作废流水）**:
将收支流水标记为无效、不再参与新增核销。仅 **正常且未核销**（已核销金额 = 0）的流水可作废；部分或已全部核销须先撤销核销。作废 **不可恢复**；作废时 **作废原因必填**（前后端均校验）。已作废流水列表仍可查看，但不展示作废按钮。
_Avoid_: 已核销流水直接作废, 作废原因可选, 作废后可恢复（第一版）

**Edit Transaction（编辑流水）**:
修正尚未进入账务闭环的流水录入。仅 **正常且未核销**（已核销金额 = 0）可编辑；部分或已全部核销不可改核心字段，须先撤销核销。可编辑字段：收支方向、流水金额、交易日期、收付款通道、往来对象、关联发团、流水备注。未核销流水视为孤立资金记录，允许直接 UPDATE，不必作废重建。
_Avoid_: 已核销流水静默改字段, 未核销也必须作废重建（第一版）, 编辑流水号

**Finance Transaction List Filters（收支流水列表筛选）**:
第一版 P0 筛选项：交易日期范围（默认近 30 天）、收支方向、往来对象关键字、核销状态、流水号、关联发团、**流水状态**（正常/已作废）。收付款通道筛选、高级组合延后 P1。
_Avoid_: 第一版仅筛选发团, 流水状态筛选延后（实现成本低且常用）

**Finance Transaction List Columns（收支流水列表列）**:
第一版 P0 必显：流水号、收支方向、流水金额、已核销金额、未核销金额、交易日期、往来对象、收付款通道、核销状态、流水状态、操作。**创建时间**可选加列（已有 `createdAt`，零 schema 变更）。**经办人**（`createdBy`）需 schema 与创建链路改造，延后 P1。流水号点击打开详情抽屉（见下）。
_Avoid_: 第一版必显经办人, 流水号纯展示不可点

**Finance Transaction Detail（流水详情抽屉）**:
第一版 P0 实装详情抽屉（点击流水号打开）：**基础信息**、**核销信息**（已核销/未核销/笔数/最近核销时间）、**核销记录列表**。**操作记录**块延后后续迭代（当前无 audit 表；创建/编辑/作废/核销的完整操作人追溯一并后续补齐）。
_Avoid_: 第一版详情抽屉整体延后, P0 为操作记录新建 audit 表

**Register Payment / Receipt（登记付款 / 登记收款）**:
财务人员在尚无收支流水时，从收付款节点直接录入实际收付款：系统新建收支流水（含收付款通道）并同时生成核销。产品确认按钮文案为「确认付款并核销 / 确认收款并核销」；后端路径保持 `confirm-payment` / `confirm-collection`。登记抽屉：只读区含应付/应收单号、标题、关联发团、往来对象、总额/已结/未结、核销后未结金额（随输入金额实时计算）；可编辑区含收付金额、收付款通道、交易日期、**流水备注**（写入收支流水，非节点备注）。通道下拉不另附 helper 文案。底部次要说明：应付侧「确认后将生成一条支出流水，并自动完成本次核销」；应收侧「确认后将生成一条收入流水，并自动完成本次核销」。

**Finance Command Idempotency（财务写命令幂等）**:
所有可重复发生、会改变财务事实的公开 HTTP 命令必须携带 `Idempotency-Key`。同一 Organization、同一 operation、同一 key 且目标、载荷与操作者一致时，系统重放首次成功结果；同键异载荷返回冲突，缺少 key 拒绝执行。幂等记录与业务事实同事务提交，失败不保留“已处理”假象；同键并发只能有一个请求执行并分配业务编号。相同金额与往来对象仍可能是两笔真实业务，因此不得脱离 key 按 payload 或时间窗口猜测去重。客源应收/资源应付生成是来源级一次性动作，依靠来源行锁与唯一约束拒绝重复生成，不把同来源第二次请求重放成成功。
_Avoid_: 前端只禁用按钮作为防重, 相同 payload 自动合并, 业务写入成功后才单独记录幂等结果, 同键异载荷静默重放
_Avoid_: 仅确认付款（文案不清会否核销）, 登记时不填收付款通道, 通道字段下方重复枚举 helper, 备注（作登记抽屉 label，用「流水备注」）, 底部说明不区分收支方向

**Payment Schedule List Actions（收付款节点列表操作）**:
全局应收/应付列表操作列：可结算时外露「登记收款/登记付款」与「匹配流水」；「编辑」「关闭节点」「重新打开」「调整约定金额」「查看核销」收进「更多」。关闭节点仅未结清金额大于零时出现（含部分核销）；已结清与已关闭不出现关闭入口。「重新打开」仅已关闭节点出现。「调整约定金额」仅资源应付或客源应收（客户补款/游客代收）、节点开放、有效已核销为零且存在财务履历时出现；发团已归档时不出现。「查看核销」仅当有效已核销金额大于零时出现（含已关闭节点）；核销全部撤销后入口消失，审计可在核销列表按节点编号检索（含已撤销）。查看核销跳转核销列表，经顶部筛选区按 **节点编号**（`scheduleNo`）过滤，第一版不做独立核销详情抽屉。
_Avoid_: 四按钮平铺, 查看核销作独立详情页（第一版）, 查看核销按 paymentScheduleId 深链, 已结清仍露出关闭节点, 按历史含已撤销核销记录决定入口, 已关闭一律露出查看核销, 归档发团仍露出调整约定金额, 无财务履历仍露出调整约定金额, 有效核销未清零仍露出调整约定金额, 仅应付可调而应收不可调

**Payment Schedule List Filters（收付款节点列表筛选）**:
团详情与全局应收/应付用「往来对象」搜索框按 **`counterpartyName` 模糊匹配**（API：`counterpartyKeyword`）；不按合作伙伴/供应商/游客代收类型分层。查看应收/查看应付深链预填该关键字。列表 API 仍保留 `counterpartyType` / `counterpartyId` / 精确 `counterpartyName` 供后续扩展，当前 UI 不暴露。节点编号/标题、节点状态、到期日仍为客户端筛选。
_Avoid_: 往来对象类型级联作列表主交互, 团内去重 counterparties 接口

**Payment Schedule List Columns（收付款节点列表列）**:
应收/应付列表用领域化字段替代通用「标题＋往来对象」，四个 scope（发团详情内嵌、全局主列表、Partner/Supplier 往来账款 Tab）与已作废审计视图共用同一套列。**应付**列身份区为：应付单号（`scheduleNo`）、费用类别、费用项目、往来对象；**应收**为：应收单号、客源单、收款方式、往来对象。费用类别读资源种类（`SegmentResource.resourceKind`）；费用项目优先读实时资源项目名（`SegmentResource.title`），缺失回落资源种类，手工其他应付回落节点标题；收款方式由应收来源类型派生「客户补款/游客代收」，手工其他应收回落「其他」；客源单读客源单展示名（`SourceOrder.displayName`）。往来对象**列名不变**（不引入「收款对象/付款对象」，遵循 Counterparty 词条），仅展示值按方向优化：游客代收统一显示「游客」（具体客源单已在客源单列体现），其余显示对手方名称、去掉类型前缀。费用类别/费用项目/客源单靠 `sourceId+sourceType` 于列表接口实时联查源实体填充（派生字段 `resourceKind`/`resourceTitle`/`sourceOrderName`，不落库、不迁移）；源实体被删或手工来源时该派生列为空态「-」，展示随源实体实时变化。
_Avoid_: 保留通用「标题」「来源」列, 新增「收款对象/付款对象」列名, 为该列新增持久化快照字段, 游客代收展示客源单 displayName 重复客源单列, 手工行伪造资源种类/客源单
