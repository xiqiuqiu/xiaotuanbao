# 小团宝

面向中小地接旅行社的 B 端 SaaS 管理系统，用于散拼产品资料与班期报价、发团、行程执行、资源、财务、同行/客户、供应商及组织权限等业务的日常运营。

## Language

**Organization**:
一家使用小团宝系统的旅行社，是多租户 SaaS 中的租户单位。所有业务数据按 Organization 隔离。
_Avoid_: 租户, tenant, 公司, 账户

**SaaS**:
小团宝以多租户模式运营，多家 Organization 共用同一套部署，通过 organizationId 实现数据隔离。
_Avoid_: 私有化, 独立部署

**User**:
一家 Organization 内可登录后台的员工身份。一个 User 只属于一家 Organization，不存在跨组织任职。登录标识见 Login Username。
_Avoid_: 账户, 成员, 账号

**Login Username（登录用户名）**:
User 用于登录后台的标识字符串，在全平台唯一（含 Platform Organization 上的 Platform Admin 与各客户 Organization 的员工，同一命名空间）。持久化为规范小写（创建、编辑、开户与登录输入均先 trim 再转小写后存储或查找）；唯一性因此不区分大小写。与显示名称（name）不同。企业管理员可在员工创建与编辑时设置或修改；Platform Admin 开户时的初始企业管理员使用同一规则：不做业务前缀拼装。登录页只凭登录用户名与密码定位 User，不要求填写 Organization 标识。凡未彻底删除（`deletedAt` 为空）的 User 均占用其登录名，含停用 Employee 与所属 Organization 已停用的 User；停用或组织停用不会释放该登录名。
_Avoid_: 账号, 账户（作该字段产品文案）, 开户自动加业务前缀, 登录名按前缀改写入库, 登录页填写企业/组织 ID 消歧, 仅组织内唯一而跨组织可撞车, 停用后释放登录名供他人使用, 大小写不同视为不同登录名, 库内保留任意大小写仅靠比较时折叠

**Employee**:
与 User 同义，指 Organization 内的在职员工。
_Avoid_: 用户（易与系统登录用户混淆时）

**Employee Status**:
员工账号处于启用或停用状态。停用的 Employee 不可登录，状态可恢复；与彻底删除（`deletedAt`）不同。第一版 UI 仅提供启用/停用，不提供删除。
_Avoid_: 软删除作停用, 删除代替停用

**Employee Remark**:
Employee 的内部说明文字，可选，仅用于创建/编辑表单，不在员工列表展示。
_Avoid_: 备注作公开信息, 对外描述

**Last Login**:
Employee 最近一次成功登录的时间，在员工列表中展示为「最近活跃」；从未登录则为空。
_Avoid_: 最近活跃作在线状态, 实时在线

**Platform Organization（平台运营组织）**:
专供 Platform Admin 挂靠的 Organization，不是客户租户。其上 User 均应为 Platform Admin；登录后只进入平台区，不使用该组织的租户后台。客户 Organization 名录维护不把 Platform Organization 当作普通租户展示或停用。
_Avoid_: 把 Platform Admin 挂在客户 Organization 下, organizationId 为空的平台账号, 平台组织当客户开户

**Platform Admin**:
平台运营方的超级管理员，可跨客户 Organization 做名录维护（创建、查看档案、改名称、启用/停用）及 **Organization Module Entitlement** 配置。创建客户组织时须同事务建立 Initial Organization Admin（见 Organization Onboarding）。与客户 Organization 内的 User 是不同身份，通过 User 表的 isPlatformAdmin 标志位识别，账号挂在 Platform Organization 下，共用同一套登录体系。工作台为同一 Web 应用内的独立平台区，与 Organization 后台路由及 Menu Permission 分离；不可进入租户业务页，也不可代入企业管理员操作。名录维护不提供删除 Organization；客户组织档案可只读展示 Initial Organization Admin 的登录用户名与显示名称，不因此开放平台侧员工管理。Platform Admin 账号本身由 seed/运维预置，不在平台区做账号管理 UI。
_Avoid_: 超管, 系统管理员, super admin, 企业管理员（作平台身份）, 代入租户后台, 平台区删除客户组织, 平台区管理 Platform Admin 账号, 平台区事后重置租户管理员密码, 平台档案展示等同员工管理, 权限管理（作平台开通能力的产品文案）

**Organization Module Entitlement UI（模块开通）**:
平台区客户 Organization 名录操作列中的入口，产品文案为 **模块开通**（不用「权限管理」）。打开抽屉勾选该组织已开通的业务模块 Entitlement Key 并保存。不提供名录列上直接勾选；不代入租户后台。
_Avoid_: 权限管理, 功能开关, 名录行内直接改开通, 平台区改租户 RolePermission

**Initial Organization Admin（初始企业管理员）**:
Platform Admin 开户时同事务写入的那一名企业管理员 User，是客户 Organization 的开户联系人。平台区客户 Organization **档案详情**只读展示其登录用户名与显示名称（名录列表不展示），读的是该 User 的**当前**字段（租户侧改名后档案跟随变化）；停用后仍展示这两项，不因此从档案抹去。认定规则：该组织内开户写入的那一名；实现上可用该组织最早创建的企业管理员 User 代表（第一版无员工删除 UI 时与开户那人一致）。认定不到时详情仍展示该区块并为空态（如「未设置」），不阻断改名/停用等名录操作。不是 Platform Admin，也不表示组织内只能有一名企业管理员。
_Avoid_: 平台超管, 把租户内全部企业管理员当成初始联系人, 平台档案列出全部员工, 开户快照与现网登录名长期分叉, 停用后从平台档案隐藏初始管理员, 名录列表展示初始管理员, 缺初始管理员时详情接口报错

**Organization Onboarding**:
客户 Organization 的开通交付。现行开户：Platform Admin 创建组织时同事务写入 Initial Organization Admin（登录用户名、显示名称、初始密码必填），绑定企业管理员 Role，并按当时业务模块基线写入 Organization Module Entitlement，创建后可立即登录租户后台；登录用户名规则与租户员工管理相同（见 Login Username），不拼业务前缀。开户表单不勾选模块；若需收窄或加开，开户后在名录「模块开通」中配置。邀请链接让客户自设密码仍为后置可选能力，非开户必经。Platform Admin 开户后不在平台区重置该管理员密码或管理其员工档案；平台区客户 Organization 档案可只读展示 Initial Organization Admin 的登录用户名与显示名称（不含密码，不提供改密/停用/改名等操作）。
_Avoid_: 自助注册, 开放注册, 创建组织只建壳不建管理员, 开户必经邀请链接, 平台区事后管理租户员工, 开户登录名强制加业务前缀, 平台档案展示初始密码, 平台档案做成员工管理入口, 开户表单内嵌模块勾选（第一版）

**Organization Business Prefix（组织业务前缀）**:
Organization 创建时必填、仅可设置一次的 2–4 位大写英文字母标识，用于生成发团编号及财务类业务编号。未设置前缀的 Organization 不得创建发团、收付款节点、流水或核销。前缀建议全系统唯一。
_Avoid_: 租户代码, 组织编码, 创建后补设前缀

**Organization Status（组织状态）**:
Organization 处于启用或停用状态。停用后该组织下 User 不可登录、不可新建业务单据；Platform Admin 仍可在平台区看到该组织档案并改名称或再启用。与彻底删除（`deletedAt`）不同，也不复用 Directory Profile Status。
_Avoid_: 用 deletedAt 表示停用, 目录三态套用到 Organization, 停用后对 Platform Admin 不可见, 停用后仍允许租户用户登录

**Organization Name Uniqueness（组织名称唯一）**:
客户 Organization 的名称在全平台唯一；保存重名时拒绝。Platform Organization 使用固定区分名，不参与客户开户的可选重名空间。
_Avoid_: 允许客户组织重名靠前缀区分, 仅软提示不拦截

**Workbench Action Item（工作台待处理事项）**:
从 Organization 当前业务状态实时派生、引导员工进入对应业务对象处理的行动提示；源状态解除后自动消失。它不是可领取、指派、延期或手动完成的独立任务，第一版不提供个人任务管理。
_Avoid_: 待办, 我的待办, 任务实体, 手动完成

**Coordinator Pending Account Generation（计调待提交账款）**:
计调工作台上并列的两类尚未提交收付款节点的行动提示：待提交应付与待提交应收。产品模块名 **待提交账款**；它引导计调去提交资源应付或客源应收，不是发团结清门槛，也不替代财务侧的混列「待提交账款」清单。
_Avoid_: 结算衔接, 可确认结清（作该模块名或左侧队列）, 把结清门槛当作计调待提交账款

**Pending Payable Generation（待提交应付）**:
约定金额大于零、尚无有效（未作废）资源应付节点的行程段资源或发团级资源；已关闭节点仍视为已生成。已结清发团上的资源不计入（不可再提交应付）；已关闭发团不排除。计调工作台按资源行计数与列表；与客源单侧的待提交应收对称。
_Avoid_: 可确认结清, 可确认结算, 按发团数冒充待提交应付, 把已结清发团上的资源计入待提交应付, 发团级资源不计入待提交应付

**Departure Data Gap（发团资料待补充）**:
近期或进行中发团缺少可由系统客观确认的基础资料：整团无客源单、整团无行程段、已有行程段但整团无任何资源（段资源与发团级资源皆无），或任一客源单的已录入客人数少于其客人人数；同一发团可同时具有多个结构化缺失项。它只表示系统资料尚待补充，不判断单个行程段是否应有资源，也不等同于业务准备未完成。
_Avoid_: 存在执行风险, 准备不完整, 准备度, 单个行程段无资源即报警, 资料齐备等同业务准备完成

## Roles

**Role Assignment**:
一个 User 在同一 Organization 内可绑定一个或多个 Role。其有效 Menu Permission 为所有已绑定 Role 的权限并集。第一版员工管理界面为单选 Role，保存时全量替换；底层 UserRole 仍支持多 Role，后续可开放多选。
_Avoid_: 单角色, 主角色

**Role Catalog**:
Role、Permission 及其映射为全平台共享的定义，不按 Organization 隔离。User 通过 UserRole 绑定 Role；Menu Permission 在该 User 所属 Organization 的后台内生效。
_Avoid_: 租户级角色表, 每组织复制角色

**企业管理员**:
Organization 的拥有者或最高管理者，在 **Organization Module Entitlement 已开通范围内**拥有全部 Menu Permission 与对应 Action Permission。实现上通过 seed 为企业管理员 Role 绑定所有 Permission 行，再与组织开通集取交集；权限解析对企业管理员无绕过开通上限的特殊分支。
_Avoid_: 老板, admin, 总经理, 系统管理员, 代码特判, 企业管理员无视组织模块开通

**财务**:
负责应收、应付、收支流水、核销等财务相关菜单。可进入发团、产品中心、合作伙伴、供应商，但对这些业务对象**只读**：不新建/编辑发团、不改客源与执行/资源、不维护产品/合作伙伴与供应商目录（`departure:write`、`product:write`、`partner:write`、`supplier:write` 均无）；可触发提交应收/应付（生成挂在 `/departure`，不受 write action 限制）。重新打开已结清发团下的收付款节点时，财务可在明确确认联动影响后直接使发团回到待结算，无需 OP 预先审批；操作完整留痕并对 OP 可见，之后仍由 OP 重新确认已结清。
_Avoid_: 会计, 出纳, 财务编辑客源/执行/发团, 财务维护产品或合作伙伴或供应商目录, 财务不能提交应收应付

**计调**:
负责发团、产品中心、合作伙伴、供应商等运营相关菜单，对其可写（持有 `departure:write`、`product:write`、`partner:write`、`supplier:write`）；可提交应收/应付。**不进入** `/finance/*`（应收/应付/收支流水/核销）四菜单，因此登记流水、核销/撤销核销、关闭节点、调整约定金额均不可；发团详情内的收支流水、核销记录 Tab 对计调隐藏。行程管理、资源管理尚未上线，不在第一版 Menu Permission 中。
_Avoid_: 操作, 调度, planner, 行程管理, 资源管理, 计调登记流水或核销, 计调关闭或调整账款, 计调不能提交应收应付

**Preset Role**:
企业管理员、财务、计调三个 Role 由系统统一定义，每个 Organization 创建时自动 seed。第一版 Role 及其 Menu Permission 映射固定，Organization 内不可新增 Role、不可修改权限映射。
_Avoid_: 自定义角色, 角色模板, 权限配置

**System Management Access**:
组织管理、员工管理、角色权限（`/system/*`）仅企业管理员 Role 拥有。财务、计调不可访问系统管理菜单及其后端接口。
_Avoid_: 全员系统设置, HR 角色

## Permissions

**Organization Module Entitlement（组织模块开通）**:
Platform Admin 为某客户 Organization 配置的、该组织可使用能力的上限，以 **Entitlement Key** 集合表示。它不改变全局 Role Catalog，也不替代组织内 User 的 Role 权限；租户内**所有** User（含企业管理员）的有效菜单/能力均为「所绑 Role 的权限」与「该 Organization 已开通 Entitlement」的交集，无角色可绕过。第一版仅 Platform Admin 可配置；企业管理员不可在租户侧改开通集。
_Avoid_: 权限管理（作该平台能力名）, 租户级角色表, 按 Organization 改 RolePermission, 功能开关（泛称）, Feature Flag（作该领域正式名）, 企业管理员绕过组织开通

**Entitlement Key（开通键）**:
标识一项可对 Organization 开通或关闭的能力的稳定键。可对应一组 Menu Key / Action Key（模块级），也可在后续对应发团内增值等非菜单能力；同一套开通与解析机制，不另建平行体系。第一版业务模块键为：`module:departure`（发团）、`module:product`（产品中心）、`module:finance`（财务四菜单捆绑）、`module:partner`（合作伙伴）、`module:supplier`（供应商）；在出现明确可售卖/可灰度的增值点之前，不预先拆发团内部或财务叶子细键。`/system/*` 对已启用客户 Organization 默认必开，不纳入商业勾选。
_Avoid_: 把开通键等同于 Menu Key, 第一版按臆测拆 addon 细键, 为增值服务另做第二套开关体系, 第一版将财务拆成四个独立开通键

**Organization Entitlement Default（组织开通默认）**:
存量启用 Organization 在能力上线时获得**当时已有业务模块**的基线开通，使用中无感。目录中**新出现的 Entitlement Key** 默认不对任何客户 Organization 开通，须由 Platform Admin 显式开通（用于自家灰度或指定客户）。新建客户 Organization 第一版直接套用「创建当时的业务模块基线全开」，不单独做开户模板引擎。
_Avoid_: 上线时全部默认关闭再逐家勾选, 第一版先做开通模板/套餐引擎, 新模块自动对所有组织放开

**Organization Entitlement Revocation（组织开通撤销）**:
Platform Admin 取消某 Entitlement Key 后，该 Organization 内所有 User 立即失去对应菜单与能力（有效权限按交集重算）；相关 API 拒绝访问。不删除已有业务数据；再开通后数据与菜单恢复可见。第一版不做「关闭后只读」、也不因已有业务数据而禁止关闭。第一版只持久化**当前开通集**，不提供开通变更操作履历；履历为后续增强。
_Avoid_: 关闭后只读保留, 有数据则禁止关闭, 关闭时级联删除业务数据, 仅前端隐藏菜单而后端仍可访问, 第一版强制开通变更审计表

**Menu Permission**:
权限的菜单级维度，决定 User 能否看到并访问某个菜单模块。菜单级之外，另有 **Action Permission**（按钮级）控制同一菜单内可写到什么程度；两者同存于 `Permission` 表，通过 `key` 区分（menuKey 如 `/finance/receivable`，action key 如 `departure:write`）。第一版仍不做数据级（只看自己的数据）权限。有效菜单还受 Organization Module Entitlement 上限约束。
_Avoid_: 数据权限, 菜单级即全部权限

**Action Permission（按钮级权限）**:
控制「进入某菜单后可写到什么程度」的操作级权限，以 action key 存于 `Permission` 表并经 `RolePermission` 全局固定映射（ADR-0001、ADR-0023）。第一版四把：`departure:write`（发团概览/状态/客源/执行/资源 编辑，及资源应付作废）、`partner:write`（合作伙伴目录 CRUD）、`supplier:write`（供应商目录 CRUD）、`product:write`（产品中心 Product/Spec/Schedule 维护），均为 企业管理员+计调=有、财务=无。提交应收/应付与 `/finance/*` 各操作**不**新增 action key：前者留在 `/departure`（计调与财务都可），后者靠是否持有对应 finance menuKey 天然区分。后端复用 `MenuPermissionGuard`，write 接口用 `@RequireMenu('departure:write')` 校验；`/auth/me` 以独立 `actionKeys` 字段下发，仅供前端按钮 gating，不参与菜单/路由过滤。
_Avoid_: 按 role 名硬编码能力, 按 Organization 自定义按钮权限, 把 action key 混进 menuKeys 参与菜单过滤, 为提交应收应付或每个 finance 操作单独建 action key

**Menu Key**:
标识一条 Menu Permission 的稳定键，与前端路由路径一致（如 `/finance/receivable`）。Permission 只对应可访问的叶子路由；分组父菜单（如 `finance`、`system`）不单独存 Permission，当前 User 任一路由子项可见时父菜单自动显示。
_Avoid_: permission code, 菜单 id

**Menu Permission Resolution**:
User 的有效 Menu Permission 由后端根据其所绑 Role 的权限并集计算，再与所属 Organization 的 Organization Module Entitlement 取交集，经登录与 `/auth/me` 接口以 `menuKeys` 返回（两者共用同一组装逻辑）。前端仅据此过滤菜单与路由，不维护独立的 code 映射或权限规则。页面刷新或 token 恢复时通过 `/auth/me` 重新拉取。
_Avoid_: 前端硬编码权限, 前端 code 表, 仅登录返回权限, 仅按 Role 下发忽略组织开通上限

**Menu Permission Enforcement**:
Menu Permission 同时约束菜单可见性与后端访问，不能仅靠前端隐藏菜单。第一版对系统管理类 Menu Key 强制后端校验；业务类 Menu Key 随各业务模块落地时补齐。因 Organization Module Entitlement 未开通而被裁掉的菜单，租户侧表现与「无该 Menu Permission」相同，第一版不单独提示「未对贵司开通」。
_Avoid_: 仅前端校验, 第一版为未开通单独做租户侧商业文案

**Supplier Management Access**:
供应商管理（`/supplier` 及其详情子路由）企业管理员、计调、财务三 Role 均可访问；其中企业管理员与计调可编辑（持有 `supplier:write`），**财务只读**（无 `supplier:write`，可查看账期规则、结算说明、更多财务信息等结算信息，用于应付/付款实务，但不维护目录 CRUD）。合作伙伴（`/partner`）同理：财务可进入（含往来账款），但目录业务信息只读（无 `partner:write`）。产品中心（`/product`）同理：财务可进入查看，但无 `product:write`，不可维护 Product/Spec/Schedule。
_Avoid_: 财务维护供应商或合作伙伴目录, 财务不进供应商名录, 详情页单独 Menu Key, 财务维护产品目录

**Product Center Access**:
产品中心（`/product` 及其详情子路由）企业管理员、计调、财务三 Role 均可访问；企业管理员与计调可编辑（持有 `product:write`），**财务只读**（无 `product:write`）。
_Avoid_: 财务维护产品/班期价, 产品中心仅计调可见

**Reference Options（参考选项）**:
仅返回 id→名称、用于渲染筛选器/下拉/标签的查找数据（如 `finance/departure-options`、`partner-options`、`supplier-options`、`source-order-options`），不是一等业务对象。访问口径为**按其所返回实体类型的业务菜单单键声明式守卫**（departure/客源单选项→`/departure`、partner→`/partner`、supplier→`/supplier`），语义为「能看见该类实体即可取其查找项」，不用 OR、不用认证裸放（ADR-0024）。当前不变量：合作伙伴/供应商「往来账款」Tab 里的发团选项按所返回实体挂 `/departure`（非当前屏幕 `/partner`），预设角色皆持 `/departure` 故不受影响。
_Avoid_: 下拉选项作业务主列表, 参考接口按当前屏幕菜单守卫, 参考接口用 /finance/* 或 OR 放行, 仅认证放行参考接口

## Product Interface

**业务主列表**:
从左侧菜单直接进入、支持独立分页的业务对象集合，包括发团、产品、应收、应付、收支流水、核销、合作伙伴、供应商与员工。详情页内嵌子表、操作履历、下拉选项及卡片集合不属于业务主列表。
_Avoid_: 所有表格, 所有集合, 详情子表

**业务对象创建时间**:
业务对象首次持久化成功的时间，创建后保持不变，不因后续编辑或关联对象变化而刷新。
_Avoid_: 首次业务发生时间, 首次活动时间

**业务对象更新时间**:
业务对象自身最后一次被人工编辑或由系统变更业务状态、金额等持久化事实的时间；下属、关联对象、派生展示值或登录活动等观测信息的变化不联动刷新该时间，例如核销不会刷新对应收付款节点，员工登录不会刷新员工档案的更新时间。
_Avoid_: 最近活动时间, 最近登录时间, 关联对象更新时间, 聚合更新时间, 派生状态更新时间

**业务显示时间**:
面向用户展示的业务时间统一按中国标准时间（`Asia/Shanghai`）解释；业务主列表、发团详情顶部，以及发团详情内嵌的客源单、应收、应付、流水与核销列表，其创建、更新时间均使用 `YYYY-MM-DD HH:mm` 格式且不显示秒。行程段、段资源、客人名单、运营表快照等其它详情子表不展示这两列。
_Avoid_: 浏览器本地时间, 客户端系统时区, 列表显示秒, 详情内所有子表都加时间列

**AI 辅助操作模式**:
User 在不离开当前业务工作区的前提下，通过自然语言、文件或图片让 AI 理解意图并辅助录入的可选操作方式；它与表单模式长期并列，读取和改变同一份业务事实，不构成独立业务流程或独立数据来源。
_Avoid_: AI 模式, AI 助理模式, AI 自己的表单, 用聊天记录作为业务事实, 用 AI 替代现有表单

**AI 建团任务**:
User 选择交互式建团后，从收集发团基础资料开始、经逐阶段审核写入现有业务对象，直至结束辅助流程的一次可恢复任务；正式 Departure 创建前操作发团创建草稿，创建后继续以当前正式业务数据为事实源。
_Avoid_: 用单次 AI 建团会话代替整个任务, AI 自己的一套团单, 退出对话即丢失任务, 从旧聊天记录重建当前业务事实

**AI 建团任务状态**:
AI 建团任务能否继续的业务状态：进行中、已完成、已放弃；关闭页面或暂时离开不改变状态，已完成与已放弃均不可重新激活并作为只读历史保留。已完成须由 User 在完成摘要后明确结束；已放弃可由 User 主动触发，或在关联的正式 Departure 被删除后由系统带原因触发，均不撤销已经写入的正式业务数据。
_Avoid_: 暂停（作持久状态）, 关闭窗口即完成, 自动完成, 长时间未操作即放弃, 放弃任务即删除正式发团, 重新激活历史任务, 删除任务代替放弃

**AI 建团会话**:
一个 AI 建团任务内可持续追加、跨刷新与跨设备恢复的交流容器；它具有稳定服务端身份，归任务创建者所有，完整保存已发送事件，但不等同于当前模型上下文。关闭协助窗口、离开页面或另一设备加入均不结束或替换会话；User可显式新建、继续或放弃会话，重新进入任务时默认加入最近一个未完成会话。当前阶段同组织其他User即使具有`departure:write`也不能查看或接手。
_Avoid_: CopilotKit thread（作业务身份时）, 浏览器窗口会话, 每台设备一个会话, 关闭窗口即新会话, 用完整会话历史作为模型提示词, 同组织默认共享会话

**AI 会话事件**:
AI 建团会话中按服务端 sequence 追加且不可修改的事实，包含已发送User消息、Agent最终消息、关键工具结果、批次状态、审核处置和错误；多设备共同读取同一有序事件流，流式Token和模型隐藏推理不属于持久化会话事件。
_Avoid_: 可覆盖聊天数组, 客户端时间排序, 流式Token即审计事实, 保存模型隐藏推理, 多设备last-write-wins消息

**AI 输入批次**:
User一次成功发送形成的不可变Agent输入单元，原子关联User消息、必需资料依赖、发起User与服务端会话版本；发送后集合立即冻结，后续文字或文件形成新批次并按sequence排队。同一批次依次处于等待资料、可执行、Agent执行中、等待User回答、等待表单审核、已完成、失败或已取消；Agent认领时固定conversationVersion，只能读取该版本之前的有效事件。追问回复以新批次和replyToEventId明确关联，不将提问前已排队消息冒充答案。
_Avoid_: 可追加附件的运行中消息, 浏览器临时发送状态, 文件完成后空消息续跑, 多设备共同修改一个批次, 解析速度决定输入集合, 未关联的排队消息自动回答追问

**AI 批次资料依赖**:
一份发团资料档案及其固定解析结果版本参与某个AI输入批次的关系；同次发送的资料默认全部为必需依赖，齐套后才能执行Agent。Agent认领前User可从该批次移除依赖，但这不等同于撤回档案，已开始的解析可继续归档且不得自动注入已移除批次；认领后必须显式停止并重组新批次。
_Avoid_: 档案归属, 发团资料撤回, 解析完成即进入所有会话, 静默忽略失败依赖, 认领后原地替换解析版本

**AI 建团阶段**:
AI 建团任务当前建议继续处理的业务区域，例如发团基础信息、客源、游客或行程；它与任务状态分离，只用于恢复位置和引导下一步，不强制 User 按固定顺序完成。
_Avoid_: AI 建团任务状态, 不可返回的向导步骤, 打开或关闭 AI 即切换阶段

**AI 建团阶段状态**:
AI 建团阶段的处理进度：未开始、收集中、待确认、已完成、已跳过、需处理；发团基础信息不可跳过，其余阶段可由 User 明确跳过，正式业务数据后续变化可使已完成阶段转为需处理。
_Avoid_: AI 建团任务状态, 有一条数据即自动完成, 跳过等同完成, 已完成永久不变, 需处理等同发团异常

**预计人数提示**:
正式 Departure 创建前，User 为帮助 AI 推荐与规划后续提问而提供的大致人数；它不是 Departure 的正式人数，也不是创建发团的必填条件，产生客源单后以各客源单正式人数汇总为准。
_Avoid_: 发团人数, 正式客人数, 自动补齐客人, 用预计人数覆盖客源单人数, 为 AI 单独新增正式人数口径

**发团创建草稿**:
正式 Departure 创建前，由表单与 AI 辅助操作共同完善的结构化建团资料；它不预占发团编号，也不是处于编辑中的 Departure，经 User 审核确认后才用于创建正式发团。
_Avoid_: 草稿发团, 编辑中发团, 临时团单, 预占正式团号, 表单与 AI 各有一份草稿, 一开始对话就创建正式发团

**发团资料档案**:
User 在 AI 建团任务中上传并留存的图片、表格、文档或聊天截图等原始资料；每份档案始终归属于一个 AI 建团任务，正式 Departure 创建后仅增加关联而不迁移身份。原件保存后不可修改并具有完整性标识；同一任务内完全相同的SHA-256、大小与MIME复用已有逻辑档案和匹配解析版本，避免重复展示。跨任务再次使用时仍形成新的逻辑档案，权限、解析与审核历史彼此独立；底层字节或解析缓存最多在同Organization且解析器配置版本一致时复用。图片与 PDF 同属此类档案，不按扫描件与电子文档分入口或分种类。第一版由 AI 协助会话的附件入口形成档案，不另开表单上传区。它为候选变更提供来源与追溯，不因识别结果被采用而成为业务事实。
_Avoid_: 知识库（仅指原始资料存档时）, Departure附件（作为主归属时）, 同任务重复展示同一原件, 多任务共享同一逻辑档案, 可修改原件, 底层字节复用等同档案共享, 上传即录入, 识别结果等同原始资料, 未采用即无来源记录, 扫描PDF（作独立档案种类）, 按扫描/电子分入口, 表单与聊天各一套上传

**发团资料解析**:
从发团资料档案中提取文档结构与可追溯内容，并依照明确的业务解析配置形成版本化识别结果的后台处理过程；资料档案领域拥有解析任务、结果与证据关系，格式解析器提取可信的原生文档结构（含 PDF 文本层），OCR 只处理图像或不可信文本层，同一页只保留一个权威来源。AI 编排层消费结果并提出候选；解析结果不是业务事实，也不占用 AI 建团活动运行预算。
_Avoid_: User自然语言回答的字段提取, 前台对话 Agent 直接解析完整文件, 上传即完成解析, 解析结果直接写业务字段, 由模型临时决定可提取字段, 文件解析计入互动运行预算, 扫描PDF才解析, 原生文本页再走OCR, 同一页混合两套提取当权威来源

**发团资料解析配置**:
限定某类发团资料允许识别的业务用途、字段目录、输出结构、校验规则与证据粒度的系统配置；确定性文件结构与明确原值优先，OCR或模型只能补充图像内容、归类和语义判断，不能覆盖确定性原值。未被配置覆盖的内容即使能被模型识别，也不能形成该用途的结构化识别结果或 AI 候选变更。
_Avoid_: 通用提示词, 模型自由发现字段, 文件格式定义, AI 候选变更, 模型覆盖确定性原值, 用一个配置覆盖所有业务阶段

**发团资料解析结果**:
发团资料解析对某一原件形成的版本化输出，包括通用文档结构及按指定解析配置识别的业务内容；每项内容保留页、图片、文本框、Sheet或单元格等证据位置，重新解析形成新版本而不覆盖旧版本。AI输入批次必须显式固定`materialId + parseResultVersion`，新解析版本不会替换已认领批次。存在歧义的配置内内容标记需确认或保留备选，配置外内容仅作为未采用解析内容，不形成 AI 候选变更。
_Avoid_: AI候选变更, 正式业务字段, 覆盖旧解析结果, 最新版本静默替换运行中上下文, 无证据位置的识别值, 配置外内容自动生成候选

**发团资料解析运行**:
对一份发团资料档案按确定解析器、模型与解析配置执行处理的一次后台活动；瞬时故障的有限自动重试仍属于同一运行，User主动重新解析或解析器、模型、配置变化时启动新运行并形成新的发团资料解析结果版本。运行失败不改变旧版本，也不删除原件。
_Avoid_: AI建团活动运行, 每次自动重试产生新版本, 重新解析覆盖旧结果, 运行失败删除档案, 文件解析计入互动运行预算

**发团资料处理状态**:
发团资料档案从原件保存到解析结果可消费的处理进度：已上传、排队中、解析中、可用、部分可用、解析失败、已隔离；部分页面或Sheet失败时可进入部分可用，失败或隔离均不自动删除原件，也不阻止User继续手工处理业务。若它是AI输入批次的必需依赖，失败会使该批次等待User选择重试、移除依赖后继续或放弃，不允许Agent静默使用其余资料。
_Avoid_: AI建团任务状态, AI建团阶段状态, 上传即代表可用, 部分失败等同全部失败, 解析失败自动删除资料, 解析失败阻断表单, 必需资料失败后Agent自动继续

**发团资料保留规则**:
发团资料档案及其证据随 AI 建团任务和审核用途决定的留存约束；进行中、已完成或曾支持已确认业务写入的档案长期保留，已放弃且从未支持任何已确认候选的档案自放弃起保留九十天后才可进入清理。正式 Departure 删除不单独触发档案删除，组织级数据注销另由统一数据治理规则处理。
_Avoid_: 所有上传永久保留, 任务放弃立即删除, Departure删除即删除证据, 未区分已确认用途, 用文件存储成本决定是否保留审计证据

**发团资料撤回**:
User停止一份发团资料档案继续参与当前任务的业务动作；撤回后停止尚未完成的解析且不得再形成新候选。未支持任何已确认候选的档案保留三十天可恢复，期满后方可物理清理；已经成为确认写入证据的档案只从日常工作区隐藏，仍按发团资料保留规则保存。
_Avoid_: 立即物理删除, 删除StoredObject（作为User动作时）, 撤回后继续生成候选, 隐藏等同销毁, 删除已确认业务数据

**发团资料物理清理**:
发团资料档案满足保留规则后，由系统先冻结引用并标记待清理，再确认不存在候选、审核或其它业务引用，最后异步删除原件字节与可清理元数据的系统过程；任一步失败均保留可重试状态并告警，不允许User直接触发底层文件删除。迟到的解析结果必须通过档案状态版本校验，不能恢复已撤回或待清理档案。
_Avoid_: User直接物理删除, 先删原件再检查引用, 删除失败后丢失元数据, 孤儿扫描删除仍有引用的字节, 迟到解析结果恢复档案

**AI 交互记录**:
AI 建团任务中面向User展示的消息、解释与操作反馈，是AI会话事件的可读投影，用于恢复交流过程和说明历史；历史可完整分页查看，但查看历史不启动Agent，也不直接参与重建或覆盖当前业务事实。
_Avoid_: 业务数据, 当前表单值, 打开历史即运行Agent, 把完整聊天回灌模型, 用聊天记录恢复正式字段, AI 记忆即系统事实

**AI 候选变更**:
AI 根据 User 表达或资料识别提出、但尚未成为已确认业务事实的结构化变更；第一版所有 AI 候选变更均须经 User明确确认，User在审核区修正后仍保持候选状态，且不能绕过当前 User 的权限与系统确定性规则。
_Avoid_: AI 答案, AI 自动写库, 低风险字段免确认, 聊天消息即字段值, 修改候选即自动保存, AI 拥有独立业务权限

**AI 阶段审核包**:
AI 在一个建团阶段内汇总并提交给 User审核的一组结构化候选变更；审核包是跨刷新与跨设备的服务端事实，User可在其中查看来源、修正候选值，并按允许的业务对象粒度确认或拒绝，不需逐句确认，也不延迟到整个建团任务结束后统一提交。同一阶段内多份发团资料档案的识别结果汇入该阶段的一套审核包，不按档案拆包。多设备处置以审核包版本和CAS保证首个决定生效；拒绝后Agent等待User下一条明确指令，不主动重新生成。
_Avoid_: 每个字段一次弹窗, 整个任务一次性确认, 编辑聊天文本, 待确认值静默混入正式表单, 每份资料一份审核包, 两台设备重复处置, 拒绝后自动重做

**AI 审核区**:
User 查看、修正并确认或拒绝当前待确认候选的工作面；它落在对应业务表单字段上的待确认展示层，并配合整包确认动作，不是聊天记录，也不是已保存业务快照。聊天只显示“等待表单审核”等状态，不提供第二套确认/拒绝入口，也不依赖CopilotKit HITL维持业务暂停。
_Avoid_: 在对话气泡里确认, 把状态卡当操作按钮, 用聊天协同控件写入业务事实, CopilotKit respond作为审核事实, 独立审核页, 字段来源汇总区, 把候选写入快照当作审核

**AI 候选明确度**:
AI 候选变更对来源内容的理解状态：已明确、需确认、无法确定；它用于引导 User审核，不等于模型置信度，也不能让已明确候选绕过确认。
_Avoid_: 置信度百分比, 正确率, 已明确即自动写入, 无法确定时猜测填值

**AI 候选风险等级**:
由系统字段目录固定指定的审核重要程度：关键项、业务项、说明项；等级只影响审核强调与信息展开方式，所有等级仍须确认，新增字段必须明确归类。
_Avoid_: 模型临时判断风险, 低风险免确认, 未归类字段默认低风险, 风险等级代替业务权限

**AI 确认单元**:
AI 阶段审核包内可独立确认并原子写入的一组业务字段，通常对应一个完整业务对象；独立对象可部分选择，但同一对象内的联动字段须修正后整体确认。
_Avoid_: 任意逐字段写入, 一处字段错误阻止确认其他未选对象, 一次确认部分成功, 确认单元跨越不相关业务对象

**AI 候选状态**:
AI 候选变更的处置状态：待确认、已确认、已拒绝、已取代；正式业务数据变化或新候选替代旧候选时，旧候选进入已取代且不可再确认，确认写入失败则仍为待确认。
_Avoid_: 写入失败即已拒绝, 已取代仍可确认, 拒绝候选即跳过阶段, 已确认候选可再次写入

**AI 候选证据**:
支持 AI 候选变更的可追溯来源，可来自发团资料档案的具体位置、User消息或系统确定性推导规则；资料来源须能指向档案、页码与摘录，无法指出来源的内容不能形成可确认候选，来源冲突时必须标记需确认。
_Avoid_: 模型猜测, 无来源的可确认字段, 修正值伪装成原件识别值, 来源冲突时 AI 静默择一, 资料证据只存摘录不留页位置

**AI 审核记录**:
AI 阶段审核包的只读处置历史，记录候选原值、User修正与最终提交值、证据引用、操作者、时间、写入对象及结果；它用于追溯 AI 辅助过程，不取代正式业务对象自身的操作履历。
_Avoid_: 普通业务操作记录, 用聊天记录代替审核记录, 覆盖原始识别值, 只记录成功不记录失败, AI审核记录取代正式业务审计

**AI 建团业务快照**:
表单与 AI 辅助操作共同读取的某一对象当前规范化业务事实；创建前对应发团创建草稿，创建后对应 Departure 及其正式子对象，成功写入后以系统返回的新快照更新界面。
_Avoid_: 本地表单状态, AI候选值, 聊天上下文, 客户端自行推算的保存结果

**AI 建团编辑缓冲**:
User 在当前页面中尚未成功保存到业务快照的临时输入；AI不能将其视为业务事实，切入 AI、切换阶段或离开页面前须先尝试保存，保存失败时不得静默丢弃或覆盖。
_Avoid_: 服务端草稿, 已保存字段, AI可直接读取的上下文, 离开页面自动丢弃

**AI 会话文本草稿**:
同一User在一个AI建团会话输入框中尚未发送的纯文本；它按conversationId与userId保存并跨设备恢复，但在发送前不属于AI会话事件、AI交互记录或模型上下文。多设备采用服务端最后收到的完整文本为准（Last-Write-Wins），设备持续输入期间不应用远端变化，停止后同步最新文本；发送与清空草稿原子推进draftEpoch，旧epoch的延迟请求不得复活已发送内容。附件、录音和其他本地对象不属于会话文本草稿且不跨设备同步。
_Avoid_: 已发送消息, AI建团编辑缓冲, 逐字符CRDT, 草稿冲突弹窗, 附件跨设备草稿, 草稿直接进入Agent上下文, 发送后旧请求复活文本

**AI 建团对象版本**:
标识一份发团创建草稿或正式业务对象业务快照先后顺序的单调版本；写入须声明所依据版本，版本不匹配时拒绝覆盖并返回最新快照，不以业务对象更新时间替代。
_Avoid_: updatedAt, 字段级版本向量, 最后写入者直接覆盖, 版本冲突自动忽略

**AI 建团安全合并**:
对象版本变化后，系统仅在能证明本次明确修改的字段及其一致性字段组自基准版本以来均未变化时，才将修改合并到最新业务快照；重叠或无法证明安全时拒绝整次写入并交由 User处理。
_Avoid_: 客户端自行合并, 最后写入覆盖, 冲突时整页重填, 只按字段名不同判断可合并, 部分写入后再报冲突

**AI 建团一致性字段组**:
因共同参与业务校验或派生计算而必须作为整体判断冲突的一组字段；组内任一事实变化，基于旧版本对组内其它字段的修改都不得自动合并。
_Avoid_: 任意字段集合, 逐字段版本, 联动字段分别覆盖, 由模型临时决定关联关系

**AI 业务工具**:
小团宝向 AI 编排层提供的受控领域操作，用于读取任务上下文、执行确定性查询或提交待审核候选；每次调用均以当前 User 身份按既有业务能力重新授权，并由业务模块执行组织隔离、校验与审计。它不向 AI 暴露通用 CRUD、数据库结构、候选确认或绕过正式业务命令的写入能力。
_Avoid_: AI 专用 CRUD, Prisma 工具, AI 超级权限, 由模型直接确认候选, 把现有全部接口注册为工具

**AI 操作委托**:
AI 编排层在一次限定批次运行中代表发起User调用AI业务工具的短期授权关系；它绑定User、Organization、AI建团任务、输入批次与活动运行标识，但不固化或扩大权限。后台Worker每次认领均重新检查当前User、Organization与业务能力，再生成该attempt的短期委托；不保存浏览器token。
_Avoid_: AI 服务账号, 长期访问令牌, 浏览器token作后台凭证, 固定权限快照, Agent 自有业务身份, User 权限撤销后仍可继续写入

**AI 工作流作业**:
由服务端持久化、供独立workflow-worker认领的长耗时工作单元，第一阶段仅用于发团资料解析与Agent批次执行；作业只保存任务、会话、批次、档案及版本指针，通过租约、心跳、过期回收、有限重试和幂等处理获得至少一次执行语义。普通表单保存、审核命令和业务事务不进入工作流队列。
_Avoid_: API进程内fire-and-forget, 浏览器轮询触发可靠续跑, 把文件字节或完整prompt放入队列表, 所有业务操作异步化, 恰好一次的虚假承诺, Redis/BullMQ（第一阶段）

**AI 建团活动运行**:
后台Worker对一个已齐套AI输入批次执行的一次Agent推理与工具协作attempt；同一任务同一时间至多一个活动运行，多个客户端只共同观察服务端状态，后续批次按sequence等待。页面打开、关闭、刷新或第二设备加入均不创建、抢占或结束活动运行；运行结束于已完成、等待User回答、等待表单审核或失败等持久化边界。活动运行读取认领时固定的会话版本和上下文清单，不锁定表单；候选仍通过对象版本和确认时校验处理并发变化。
_Avoid_: 每个设备一个并发 Agent, 打开窗口即新运行, 关闭窗口即终止运行, CopilotKit thread作运行事实, 浏览器runAgent作为后台唤醒, 活动运行锁住表单, 多个运行同时提交同一任务候选, 用单运行代替对象版本控制, 不同 AI 建团任务禁止并行, 解析未完成仍运行Agent, 新窗口自动回灌上一窗口解析全文

**AI 活动运行预算**:
单次 AI 建团活动运行可消耗的模型迭代、工具调用、执行时间、Token、估算费用与有限重试上限；它是防止异常循环的运行保险丝，达到上限后保留已有候选并结束该次运行，不自动开启新运行。它不限制整个 AI 建团任务的正常跨阶段延续，也不包含发团资料档案后台解析任务的独立预算。
_Avoid_: AI 建团任务总额度, 达到上限即放弃任务, 无限自动重试, 用组织月度额度代替单次运行保险丝, 把文件解析预算混入互动运行

**AI 协作契约**:
表单、AI 协作界面、编排层与业务服务之间共享的稳定结构化协议，统一定义工具名称与版本、输入输出 Schema、协作状态、事件、错误及关联标识；它独立于具体前后端框架，框架侧仅负责适配，不携带业务实现。
_Avoid_: 复制多份 DTO, 由提示词约定字段, Mastra 专属类型, CopilotKit 专属业务对象, NestJS 实体直接暴露给模型

**AI 最小任务上下文**:
Context Builder在Agent attempt前按冻结输入批次和conversationVersion装配的最小必要模型输入，包括本批消息与固定资料版本、当前任务阶段、最新业务快照及对象版本、未解决审核状态、当前能力、有效滚动摘要和必要近期尾部；系统业务事实优先于摘要，历史档案只有显式引用后才按预算读取事实索引或原文片段。完整会话历史、无关阶段数据、敏感信息和数据库内部结构不进入默认上下文。
_Avoid_: 整个组织数据进入提示词, 完整数据库记录, 用旧聊天恢复业务事实, 摘要作为业务写入证据, 把所有阶段一次性注入模型, 默认暴露无关敏感字段, 把任务内历史档案全文注入新窗口, 查看历史即全量回灌

**AI 上下文清单**:
一次Agent attempt实际模型输入的可审计装配记录（Context Manifest），至少关联会话、输入批次、conversationVersion、使用的事件sequence、摘要版本、`materialId + parseResultVersion`及读取片段、业务快照版本、模型/系统提示词/工具Schema/Context Builder版本、预算裁剪原因与最终输入哈希。它只保存引用和装配规则，不复制档案全文或模型隐藏推理；同一attempt重试复用固定清单，改变输入须形成新版本。
_Avoid_: 完整prompt副本（作唯一清单时）, 模型隐藏推理, AI交互记录, 只记token总数, 重试时静默改变上下文, 无法复现输入来源

**AI 不可信业务输入**:
User 上传的文件、图片、聊天截图及其解析文本只能作为业务内容和候选证据，不能成为系统指令、权限授予或确认动作；协助会话附件进入模型上下文的是发团资料档案指针，不是原件字节或 base64。其提取结果必须符合服务端字段目录与 AI 协作契约，并由业务服务重新校验。
_Avoid_: 文件中的提示词覆盖系统规则, 上传即授权, OCR 结果直接写库, 资料内容充当 User 确认, 仅靠模型校验工具参数, 把附件原件或可抓取预览 URL 交给模型作视觉输入

**AI 运行轨迹**:
用于诊断与成本观测的模型调用、工具耗时、Token、重试和错误记录；它与 AI 交互记录、AI 审核记录及正式业务审计通过任务、运行、审核包、命令和工具调用标识关联，但异步记录失败不得回滚已经满足事务要求的业务操作。
_Avoid_: 用运行轨迹代替业务审计, 轨迹写入失败导致业务回滚, 无法关联具体命令, 只记录成功调用, 将敏感业务内容无边界写入调试日志

**AI 工具调用身份**:
调用 AI 业务工具时必须同时成立的双重身份：受信任 AI 编排服务的服务身份，以及绑定当前 User、Organization、AI 建团任务和活动运行的短期 AI 操作委托；任一缺失或失效均拒绝调用，最终业务权限仍按当前 User 实时判断。
_Avoid_: 仅信任内网来源, 仅凭服务账号, 仅凭 User 令牌不限制调用方, 长期委托, 双重身份替代业务能力检查

**Departure List View（发团视图）**:
发团管理业务主列表的默认呈现：一行一个 Departure，用于按团号/状态/进度等运营容器维度检索与进入详情。
_Avoid_: 团单列表, 把客源单行当作发团视图的行

**Route Ledger View（线路视图）**:
发团管理内与发团视图并列的只读呈现：以路线名称（`routeName` 精确归组）与出团日期为对等筛选轴，可单独或组合（交集）；至少选其一才查询，都空则空态提示、不拉全量。出团日期须起止成对；无路线时跨度最多 7 天，有路线时日期不限。呈现单位是 **发团**：每个发团一份完整日报表（表头含出团日、路线名与团号），同日多发团上下分列，不合并进同一张客源表、不做跨团或按日/按路线的金额与拼出加总；跨多天仅保留换日轻量分隔。无客源发团仍保留空壳日报。拼出挂在该发团日报汇总，不写入客源行，也不表示客源与拼出存在分配关系。列用系统规范名，可以 tooltip 附注客户习惯称呼；拼入价可以只读算式展示，金额仍以客源单字段为准。不含行级「实收业务」，不混入核销口径的已收/未收；编辑仍跳转既有发团/客源路径。筛选写入发团管理 URL，与发团视图筛选互不强绑。支持将**当前筛选命中的全部发团**导出为一个 `.xlsx`（每团一 Sheet：上客源日报含合计、下资源安排事实；空壳团仍导出）；筛选栏单一导出入口；不替代发团运营表，资源区不含应付进度（ADR-0037）。
_Avoid_: 同日多团一张表, 按日跨团合计, 路线段加总展示, 行程日报（作视图名）, 按产品实体选线（第一版）, 客源行内硬挂拼出, 行级实收业务, 视图内直接改价改人数, 无筛选即铺开全组织全历史, 未选路线时超过 7 天的宽日期全量扫读, 用线路视图导出替代发团运营表, 导出资源区带已付/未付进度

## Core Entities

**Departure（发团）**:
一次具体出团活动的运营容器，是系统核心业务对象。计调在此录入客源、按线路规划行程（含一日一段骨架）、在行程段或发团级安排自营资源或拼出、维护执行班组与团内增收记录，再显式触发关联的财务事件。**发团**是客户日常口头业务名称；发团是运营聚合根，**不是**财务结算单元——应收挂在客源单，应付挂在行程段资源或发团级资源，发团头部的毛利与待收待付为跨子单元的汇总视图；增收记录不经收付款节点。
_Avoid_: 团单, tour, 团期, 团单管理, 接团单, Project

**Departure Overview（发团概览）**:
发团的基本信息：线路名称、线路类型、目的地、出发/结束日期、预计成人/儿童人数、负责人、状态等。
_Avoid_: 团期详情, 基本信息

**Departure Progress（出团进度）**:
由出团日期与结束日期自动派生的时间状态：未开始、进行中、已结束。表达这趟团在时间轴上的位置，不驱动财务锁定或只读规则。
_Avoid_: 出团状态, 团期状态, 进行中（作发团业务状态）

**Departure Status（发团状态）**:
发团业务处理阶段：编辑中、待结算、已结清、已关闭。编辑中 → 待结算由 OP 手动切换；已结清由 OP 手动确认，**不是**系统自动改状态；已结清发团下的收付款节点被明确重新打开时，经用户确认与节点一同回到待结算，不留下「发团已结清但存在开放账款」的中间状态。已结清发团上不可再提交或手工创建收付款节点；若需新增账款，须先经财务联动（如撤销核销或重开节点）回退到待结算。已关闭为归档只读，由 OP 手动触发；其下收付款节点不可直接重新打开，须先由 OP 或企业管理员带原因解除归档并使发团回到待结算。不等于财务单据是否已全部提交。
_Avoid_: 团单状态, 项目状态, 确认发团, 系统后台自动把发团标为已结清, 重新打开节点后发团仍保持已结清, 已结清发团上继续生成或手工创建应收应付

**Departure Unarchive（发团解除归档）**:
OP 或企业管理员将已关闭发团恢复为待结算的明确动作，用于再次处理该运营容器内的业务或财务事项。解除归档原因必填并保留原归档人、时间与原因；归档期间仅可查看，撤销核销、作废或反向流水、调整金额与重新打开节点等任何会改变财务事实的动作均须先解除归档，且不会自动触发解除归档。
_Avoid_: 财务操作自动解除归档, 归档期间撤销核销或作废流水, 归档期间新增反向流水, 删除原归档履历, 无原因解除归档, 发团已关闭时直接重新打开子节点

**Departure Purge（发团误建删除）**:
将尚未产生客源单、且从未出现任何收付款节点或归属本团流水（含已作废/已关闭节点与已作废流水）的发团物理移除，并级联清除其行程段与资源草稿；仅 `编辑中` 与 `待结算` 可执行，`已结清` / `已关闭` 不可。产品文案为「删除」；持 `departure:write` 者可在列表与详情操作，不满足门槛时隐藏入口；二次确认即可、不填原因、不可恢复；发团编号不回收。它不是已关闭归档，也不是 Directory Archive。
_Avoid_: 用关闭代替误建删除, 软删除发团, 已结清或已关闭仍可删, 有客源或任何财务痕迹仍可删, 删除后回收团号, 删除原因审计表（第一版）, 目录式「显示已删除/恢复」, 仅负责人可删（第一版数据权限）

**Departure Financial Settlement Gate（发团账款结束门槛）**:
系统派生的布尔门槛：下属收付款节点全部为已结清或已关闭时为真（含未结清关闭），表示没有仍开放追收/追付的收付款计划。仅当门槛为真时，OP 才可把发团状态标为已结清；门槛本身不是发团状态。已关闭节点上仍可见的未结清不阻止该门槛，也不计入发团待收/待付。
_Avoid_: 用门槛代替发团已结清, 未结清关闭不算账款结束, 无门槛仍可标已结清, 已放弃金额

**Settlement Receivable（结算应收）**:
发团当前应向客户收取的最终团款，等于本团全部客源单优惠后金额合计。它只汇总客源团款，不包含手工创建的其他应收；生成或关闭应收节点只改变账款状态，不另造或删除结算应收金额。当前模型中客源单一律参与结算，只有删除客源单才使其金额退出汇总；「标记客源单不参与结算」是尚未建模的能力，出现真实业务需求时再引入。
_Avoid_: 实际应收（作产品指标名）, 把其他应收计入结算应收, 用已提交应收节点金额替代客源单金额, 因应收节点关闭删除客源团款, 把「参与结算」当作已存在的过滤条件

**Departure Financial Summary（发团财务摘要）**:
发团级财务域口径（概览 B 款主层展示见 ADR-0038 / Departure Overview Stats，不再强制团款收款进度、游客代收、返利、资金情况等独立主层卡）。账款层用产品文案「已收 / 未收 / 已付 / 未付」展示核销进度（已收/已付仅指已核销到具体账款的金额）。**团款收款进度**（域口径，概览主层已改用已提交应收进度）分母为结算应收（各单 S 合计）；单笔客源对分子的贡献为 `min(该单 Guest 路径已收, S) + 该单客户补款路径已收`（G约定已覆盖 S 时提交应收可不建补款节点），团级分子为各单贡献之和，进度不超过 100%。**不**把 G>S 的代收溢价或返利计入该分母/进度。**游客代收进度**（域口径）分母为各单 G约定（已生成定金/尾款节点口径），分子为这些 Guest 节点已收。返利（预估/已确认应付/已付/未付）单独统计，不并入团款收款进度。手工应收不混入团款进度，另作「其他应收」提示。团内增收的**增收净收益**（= 各条公司增收合计）计入收入合计与当前毛利，不并入结算应收或概览收款进度分母，也不等于其他应收。资源付款进度只统计资源应付的有效核销（段资源与发团级资源），以资源成本合计为分母；手工应付、其他应付、返利应付及资源账款金额差异不混入该资源付款主进度，进入财务对照明细。财务明细中的全部已付仍汇总归属该发团全部非作废应付节点的有效核销，包含已结清节点与关闭前的历史核销，并满足确认应付＝全部已付＋开放未付＋已关闭未付。应收、应付的已关闭未结清均不从账上消失，而是在未收/未付中单独拆分并标记异常。资金事实（未核销收支等）仍按 ADR-0011 分层表达，概览 B 款主层不再独立展示资金卡。撤销核销后，金额从已收/已付退回未收/未付，对应流水则进入未核销资金；两层同时存在，不互相抵消。
_Avoid_: 用全部应付已付作资源付款进度分子, 用确认应付作主卡付款进度分母, 把手工应付混入资源付款进度, 用已收/已付同时表示资金发生与账款核销, 把手工应收混入客源结算应收进度, 把未核销资金计入已收或已付, 关闭节点后抹除历史已收或已付, 关闭节点后让剩余未收或未付消失, 确认应付不等于全部已付加未付, 撤销核销后隐藏原流水, 把未核销资金计入账款已结清, 已放弃金额, 用 G>S 的游客代收抬高团款收款进度超过 100%, 把返利并入结算应收分母, 用单一「客源已收÷S」吞并代收溢价, 游客已收≥S 时团款进度仍显示 0%（只认客户补款已收）

**Other Receivable（其他应收）**:
归属该发团但不是由客源单客户补款或游客代收路径生成的有效应收节点，用于承载手工补充的其他收款义务。它不进入以结算应收为分母的收款进度；概览存在该金额时单独提示并引导进入应收管理查看。它不是团内增收记录，也不等于概览「增收净收益」。
_Avoid_: 把手工应收计入结算应收, 把其他应收已收混入客源收款进度, 隐藏其他应收, 用其他应收指代增收记录或增收净收益

**Cash Net Inflow（现金净流入）**:
发团级资金指标（概览 B 款主层不再独立展示），等于归属该发团的全部未作废收入流水减全部未作废支出流水。资金卡同时展示有效收入流水、有效支出流水与现金净流入，使用户可直接复算；另提示其中未核销收入与未核销支出。它表达已实际发生的资金净流动，包含已核销与未核销流水；退款按实际资金方向记录，客户退款作为支出、供应商退款作为收入，因此自然进入净流入计算。第一版反向流水不与原账款建立反向核销关系，不自动冲减收款或付款进度。撤销核销只改变未核销提示，不改变现金净流入。收付款节点关闭、作废或重新打开均不改写真实流水，只有流水本身作废时才退出收入或支出合计。资金按流水归属发团聚合，跨团核销不改写流水归属：存在外部核销时，资金卡另以提示展示「核销自外部流水」与「本团流水核销至他团」金额，解释资金卡与收付进度卡的合法差值；「核销自外部流水」涵盖归属他团与未归属任何发团的流水核销到本团账款，两者合并为一个口径。该提示与未核销收支提示同级，不标红、不算守恒异常。现金净流入不是利润，也不是开放账款的核销进出差。
_Avoid_: 用退款反向流水自动冲减收付款进度, 强制退款核销到反方向账款, 实际毛利, 净利润, 只展示无法复算的净流入, 概览已收减概览已付, 排除未核销流水, 撤销核销后改写现金净流入, 因节点状态改写历史现金事实, 用核销关系改写流水归属发团, 跨团核销标红作异常, 隐藏跨团核销差值, 核销自外部流水遗漏无归属发团的流水

**Cost Total（成本合计）**:
发团概览业务预计支出，等于本团全部行程段资源与发团级资源约定金额合计（自营与拼出均计），无论是否已提交应付。它是主卡付款进度的分母；其中尚无有效应付节点的资源金额单独汇总为「尚未提交应付」，留在成本合计中提醒，但不进入确认应付或财务明细的未付。生成或关闭应付节点不改变成本合计；当前模型中资源一律参与结算，只有删除资源才使其金额退出汇总；「标记资源不参与结算」是尚未建模的能力，出现真实业务需求时再引入。
_Avoid_: 预计成本（作主指标产品名）, 应付合计（作该指标产品名）, 用应付节点金额代替资源约定金额, 因应付节点关闭删除资源成本, 未生成资源成本计入未付, 未生成资源成本从成本合计消失, 把「参与结算」当作已存在的过滤条件, 发团级资源不计入成本合计

**Confirmed Payable（确认应付）**:
发团概览财务已确认的付款义务，等于归属该发团全部非作废应付节点约定金额合计，不限节点来源类型；手工应付亦计入。已结清与已关闭节点仍计入确认应付；资源应付作废后才退出确认应付。关闭只是停止后续追付，不否定原确认义务，也不改写确认毛利。与成本合计对账时必须分别暴露尚未提交应付、其他应付、已生成资源的源金额与节点金额差异，满足 `确认应付－成本合计＝其他应付＋资源账款差异－尚未提交应付`；各组成因素不得因净额相互抵消而隐藏。
_Avoid_: 应付合计, 只展示确认应付与成本合计净差额, 差异相抵后隐藏组成问题, 关闭节点后退出确认应付, 作废节点仍计入确认应付, 用资源约定金额代替节点金额

**Current Gross Profit（当前毛利）**:
发团概览的实时经营预估利润，等于收入合计减成本合计；**收入合计** = 结算应收 + 增收净收益。它随有效客源团款、有效增收记录与有效资源成本变化，不表示现金结果或财务账款已全部确认。
_Avoid_: 预估毛利（作主指标产品名）, 实际毛利, 净利润, 确认毛利, 只用结算应收减成本合计作当前毛利

**Current Gross Margin（毛利率）**:
发团当前毛利占收入合计的比例，等于当前毛利÷收入合计×100%；收入合计为零时无可计算比例，显示「暂无数据」。负毛利保留真实负比例，不归零。
_Avoid_: 净利率, 收入合计为零时显示 0%, 负毛利率归零, 用结算应收作毛利率分母

**Confirmed Margin（确认毛利）**:
发团概览财务成本确认后的利润，等于结算应收减确认应付。不用「实际毛利」命名，以免与现金或核销口径混淆。
_Avoid_: 实际毛利

**Departure Overview Stats（发团概览统计）**:
发团详情「概览」Tab 内的经营与财务指标区，信息结构固定为三层（见 ADR-0038）：**待办提醒** → **经营概况横条** → **收付款进度**。待办并行拉取客源单、行程段、账款等待办项，点击跳转对应 Tab。经营概况横条一行展示收入构成（原始团款、调整净额、优惠、结算应收、增收净收益、收入合计）、成本构成（资源成本、拼出成本、成本合计）、当前毛利与毛利率；**不展示总人数**。结算应收口径为原始团款 + 调整净额 − 优惠合计；收入合计 = 结算应收 + 增收净收益；当前毛利 = 收入合计 − 成本合计。概览主层**不再**独立展示团款收款进度、游客代收进度、返利或资金情况；收付层仅展示**收款进度**（已提交应收已核销 ÷ 已提交应收合计）与**付款进度**（资源应付有效核销 ÷ 成本合计）。颜色、卡片、间距、图标和响应式布局遵循根 `DESIGN.md`。财务对照与异常按指标就近展开：成本合计入口解释确认应付、尚未提交应付、其他应付与资源账款差异；当前毛利入口对照确认毛利；收款进度入口解释尚未提交应收、已关闭未收与其他应收；付款进度入口解释已关闭未付及全部应付核销进度。概览不新增独立财务对照大区块；明细能力不因主层简化而删除或合并口径。概览指标使用专用汇总口径与字段，不改写发团级已核销/未结清等全局账款字段的既有语义。毛利率、收款进度、付款进度均按四舍五入固定显示 1 位小数（如 `80.0%`）；文字保留小于 0% 或大于 100% 的真实比例，只有进度条视觉长度限制在 0–100%。比例分母为 0 时显示「暂无数据」，不显示 0%。主层指标金额为 0 时显示 `¥0.00`；明细入口内的提示项金额为 0 时省略该行。任何守恒关系异常均保留原始有符号金额与真实百分比，不以截断、归零或自动修正掩盖；受影响指标标红并展示差额说明。异常不阻止读取概览，后端记录结构化异常并由写操作约束避免继续扩大。不挂在详情页头部跨 Tab 常驻；头部只保留发团元数据与状态操作。
_Avoid_: 新增独立财务对照大区块, 把外部截图当视觉规范, 照搬渐变装饰, 因主层简化删除财务对照或异常能力, 把经营与确认口径合并成一套事实, 静默把负数归零, 用进度条封顶值代替真实百分比, 自动修正异常财务数据, 异常时整页不可查看, 只记录日志不在页面提示, 顶部经营概览作完整统计区, 各 Tab 重复完整统计卡, 改写全局已核销字段语义以迁就概览, 未提交应收从未收中消失, 未生成资源成本计入未付, 隐藏尚未提交应付, 手工应收混入概览收款进度, 隐藏其他应收, 隐藏已关闭未收或未付, 分母为 0 时进度显示 0%, 概览主层强制展示总人数, 概览主层独立展示团款收款进度或游客代收或返利或资金情况, 用结算应收作收款进度分母, 概览仍展示其他收入或团上收入台账录入

**Departure Type（发团类型）**:
发团成团方式的展示标签，如独立团、拼团。仅用于列表筛选与识别；不约束客源单数量，不改变业务规则。独立团即恰好只有一条客源单的发团，模型与拼团相同。
_Avoid_: 团单类型, 单团, 结构性拼团

**Route Template（常用路线）**:
Organization 内常发线路的配置快照，保存行程段结构与段内资源草稿（种类、供应商、名称等）。地接社日常在固定线路上反复收客、配资源、做核算；建团时可选用已有常用路线，一次性复制进新发团，之后模板与发团实例脱钩。填写发团的路线名称不是创建常用路线；无行程段也不得在建团当时新建常用路线。模板不保存资源金额（资源金额为本团总价，随人数等变化，不作模板能力）、客源、应收应付与流水核销；亦不设段级默认执行方式。
_Avoid_: 线路模板, 产品, product, 散拼产品, 活引用, 参考价格（作模板能力）, 默认执行方式, 历史路线模板（作实体名）, 建团时新建常用路线, 把路线名称当成常用路线实体

## Product Center

**Product（散拼产品）**:
Organization 内可维护营销行程与对外报价、按班期生成发团的资料聚合根。产品承载可销售的线路说明与报价口径，不是一次具体出团，也不是执行侧行程段/资源结构模板。第一期产品类型固定为散拼；定制游等其它类型未建模。
_Avoid_: 常用路线, 线路模板, 发团, 团单（作该对象名）, Tour, Package, SKU, FIT Product（作独立类型名时）, 产品目录项

**Product Type（产品类型）**:
Product 的成团/售卖形态分类。第一期仅允许散拼产品，不提供类型切换，也不在模型中并列定制游等其它取值。
_Avoid_: 多类型并存（第一期）, 定制游作第一期取值, 发团类型（Departure Type）冒充产品类型

**Product Schedule（班期）**:
某 Product 下一条可销售的发团日期规则及其对外报价快照（如连续区间天天发、每周固定日、单独日期说明）。班期是报价与「生成发团」的口径，不是发团本身；同一班期可生成多个发团。与收付款节点（Payment Schedule）无关。
_Avoid_: 收付款节点, 应收单, 应付单, 发团, 库存, 余位, Schedule（无 Product 前缀的英文名）, Departure Slot, Departure Offering

**Product Spec（价格规格）**:
Product 上一种可报价档位及其默认销售价（如标准/舒适：成人价、儿童价、单房差等说明）。第一期一个 Product 可先只维护一个规格。规格默认价供新建班期复制；不是客源团款单价，也不驱动应收。
_Avoid_: SKU, 套餐, 房型（作规格对象名时）, 客源团款单价, 参考价格（作常用路线能力时）

**Product Schedule Price Snapshot（班期价格快照）**:
创建 Product Schedule 时从当时 Product Spec 默认价复制出的班期侧报价事实。之后修改规格默认价不回写既有班期；班期价可独立调整。文档展示与对外报价以所选班期快照为准。
_Avoid_: 班期活引用默认价, 改默认价联动未锁定班期, 第一期「跟随/锁定」双模式

**Product Itinerary（产品行程）**:
Product 上面向同行与文档的营销行程文案整体，含简版与详细版；不是发团执行安排中的行程段，也不携带资源与成本。
_Avoid_: 行程段, Itinerary Segment, 执行安排, 常用路线, 行程（作发团执行区对象名时）

**Short Itinerary（简版行程）**:
Product Itinerary 的短述，用于产品总表、列表与微信快发；可为整段文本，或拆成按日的 Product Day。
_Avoid_: 详细行程, 行程段, 执行安排

**Detailed Itinerary（详细行程）**:
Product Itinerary 的完整叙述，用于正式 Word/PDF；可为富文本或关联文档，不要求与简版字段一一强绑定。
_Avoid_: 简版行程, 行程段, 发团运营表

**Product Day（产品日行程）**:
Product Itinerary 内「第 N 天」的营销说明条目（如 D1/D2 标题与描述）。可选结构；第一期允许仅存整段简版而不拆日。不是发团行程段，不挂资源。
_Avoid_: 行程段, Itinerary Segment, Day（作发团执行单元名时）, 每日行程（作行程段同义词时）

**Product Departure Source（产品发团来源）**:
发团上仅作追溯的来源指针：来自哪个 Product、哪条 Product Schedule（及生成时所选具体出团日）。生成时一次性复制名称、日期与产品行程文案等进入发团，之后产品或班期变更不回写该发团，发团变更也不回写产品。不是活引用，也不表示财务或客源已按产品报价生成。
_Avoid_: 活引用产品, 产品价自动写入客源单价或应收, 生成时强制写入常用路线, 来源模板（作常用路线字段名套用到产品时）

**Product Status（产品状态）**:
Product 的上架生命周期：草稿、销售中、已下架。草稿可编辑与预览，不进入有效销售统计；销售中可维护班期、导出与生成发团；已下架保留历史，可再上架。已有班期或已被发团以 Product Departure Source 引用的产品不可物理删除，只能下架。
_Avoid_: 启用/停用（作产品生命周期时）, 已发布（无下架态时）, 用 deletedAt 表示下架

**Product Schedule Status（班期状态）**:
Product Schedule 的可售生命周期：销售中、已截止、已取消。销售中可用于文档与生成发团；已截止保留历史报价，新建发团须二次确认；已取消不参与有效班期与价格统计，且不可生成发团。取消不删除历史行。
_Avoid_: 草稿（作班期状态时）, 用删除代替取消, 停用（作班期状态同义词时）

**Product Feature（产品特色）**:
Product 上面向营销的卖点条目（短标题与说明，如「真心」条款）。可空；不承载年龄限制、退改、拼住等报名规则。
_Avoid_: 报名须知, 注意事项（作特色对象名时）, 把规则条文写入特色

**Booking Notice（报名须知）**:
Product 上（或组织模板提供的）报名与执行规则说明，如适用年龄、儿童标准、拼住、座位、购物与退改等。可整段维护；组织可提供常用模板，产品引用后允许单独修改。与 Product Feature 分模块，避免营销卖点与业务规则混杂。
_Avoid_: 产品特色, 把须知塞进特色条目, 仅组织模板不可被产品覆盖（第一期）

**Product Import Session（产品导入会话）**:
一次从外部总表（如试点《疆游记》xlsx）迁入产品的服务端确认过程：关联已归档的导入原件，承载解析出的结构化候选项与待确认/已接受/已废弃等状态；计调确认后批量生成草稿 Product，确认前不上架、不生成发团。候选项以服务端会话为真相，不以浏览器暂存为唯一来源。
_Avoid_: 导入批次（作本对象正式名时）, 仅前端持有候选项, 解析结果当已上架产品, 把导出文件当导入会话

**Source Order（客源单）**:
发团内的收入侧结算单元，对应一家合作旅行社输送到地接社的一批客人。记录发客客户、成人/儿童人数与对应团款单价、团款调整项、整单优惠及收款拆分。不同合作方可在同一发团下各有独立客源单；应收从客源单触发生成，而非从发团统一生成。自动生成的展示名（`displayName`）为发客客户（Partner）名称；同一发团同一伙伴多单时追加序号（如「思达典雅 2」），**不**拼出团日期或「发客」后缀（发团顶栏已有团期信息）。实体名始终为 **客源单**；应收列表／编辑／详情中指向该实体的溯源列称 **来源客源单**（读 `displayName`），以区别于收款对象。
_Avoid_: 订单, 收客单, 报名单, 客源记录, 客源批次, 展示名自动拼「X月X日发客」, 把实体本身改名为「来源客源单」

**Adult Guest Count（成人人数）**:
客源单上按计价分类的成人数，由用户录入，允许为 0；与儿童人数相加得到总人数。业务上纯儿童团少见，但模型不强制必须有成人。新建客源单默认 0，保存前须使总人数 ≥ 1。
_Avoid_: 大人人数, 成人客人数

**Child Guest Count（儿童人数）**:
客源单上按计价分类的儿童数，由用户录入，允许为 0；与成人人数相加得到总人数。新建客源单默认 0。
_Avoid_: 小孩人数, 儿童客人数

**Total Guest Count（总人数）**:
客源单上的客人合计人数，等于成人人数 + 儿童人数；只读派生，不单独录入，持久化时仍可保存该派生值供列表与汇总使用。约束为总人数 ≥ 1（成人与儿童不可同时为 0）。
_Avoid_: 客人人数（作唯一录入字段时）, 总客人数

**Adult Unit Price（成人团款单价）**:
客源单上每位成人的优惠前团款单价，允许为 0（如免票）。成人人数大于 0 时必填；成人人数为 0 时可不填，计算按 0。
_Avoid_: 成人价, 大人单价

**Child Unit Price（儿童团款单价）**:
客源单上每位儿童的优惠前团款单价，允许为 0（如免票）。儿童人数大于 0 时必填；儿童人数为 0 时可不填，计算按 0。
_Avoid_: 儿童价, 小孩单价

**Original Tour Fare（原始团款）**:
客源单调整与优惠前的整单团款基数，等于成人人数 × 成人团款单价 + 儿童人数 × 儿童团款单价；只读派生，不含团款调整项与整单优惠。发团概览汇总本团全部客源单的原始团款；产品抽屉在基础信息区展示为「原始团款」，并在底部结算预览的团款结算行再次列出以便核对公式。
_Avoid_: 原始应收（作产品指标名）, 原始团款单价（作整单汇总时）, 团款总额（与结算金额混淆时）, 把团款调整项或整单优惠计入原始团款

**Source Order Fare Adjustment（团款调整项）**:
挂在客源单上的一条团款加减明细，用于在原始团款之外说明单房差、门票补退、住宿扣减、续住等约定。每行含调整种类、调整方向、正数整行金额（不拆数量×单价）与可选的调整说明；「其他费用调整」的调整说明必填。与既有整单优惠并存，不取代「优惠方式 / 优惠金额」。属客源单金额事实：与人数、单价、优惠、收款拆分同一套锁定与同步——未 finance-touched 时保存可重算并同步应收；已介入或客源单已关闭则只读，须走显式调整。产品抽屉内按行显式保存后再纳入待提交明细；存在未保存草稿行时不可保存客源单。
_Avoid_: 附增项（作系统规范名）, 附加费, 用团款调整项取代整单优惠, 有符号金额一行表达方向, 调整行数量×单价（第一版）, 调整项绕开金额锁定另开可编辑规则, 自定义（作独立种类名；逃生口已并入其他费用调整）, 自定义名称（作字段名；现为调整说明）, 总保存时静默吞掉未确认草稿行

**Fare Adjustment Note（调整说明）**:
团款调整项上的短文本备注，说明该行加减原因或口径。固定种类可选填；「其他费用调整」必填。取代原自定义行的「自定义名称」，不是另一套模型。
_Avoid_: 自定义名称, 把调整说明当成新的种类枚举, 固定种类强制必填说明

**Fare Adjustment Direction（调整方向）**:
团款调整项的加减属性：增项（加到结算金额）或减项（从结算金额扣除）。金额字段始终为正数，方向单独选择，不靠正负号表达。固定种类方向锁死；仅「其他费用调整」可由用户自选方向。
_Avoid_: 用正负金额代替方向, 附增（兼指减项时）

**Fare Adjustment Kind（团款调整种类）**:
团款调整项的系统内置分类；全组织同一套，第一版不可按 Organization 配置。取值六项：儿童门票补款（增）、单房差补款（增）、续住费用（增）、门票优惠退差（减）、住宿费用扣减（减）、其他费用调整（方向自选、调整说明必填、可多行）。前五项为固定种类，各自锁死方向、同一客源单每种最多一行；「其他费用调整」为逃生口，可多行。种类按需添加，未使用的不占行；已添加行金额必须大于 0。不再单列「自定义」或多项细分门票已优惠种类。
_Avoid_: 附增项枚举, Organization 级调整名录（第一版）, 自定义（作种类）, 学生/儿童半/老免门票已优惠过分列固定种, 其他补充费用（仅增的旧固定码）, 不含首晚或末晚住宿（旧规范名；现为住宿费用扣减）, 同一固定种类多行, 固定种类表单常驻零金额行, 保存金额为 0 的调整项

**Fare Adjustment Net（调整净额）**:
客源单上全部团款调整项的净影响，等于增项合计 − 减项合计；可为正、负或零。只读派生；客源列表、合作团单汇总与导出物以「调整净额」一项展示，增减明细只在客源单抽屉。发团概览不单独成卡，但结算应收计算已并入调整净额。不包含整单优惠金额；「优惠合计」口径不变。
_Avoid_: 把整单优惠并入调整净额, 列表第一版分列增项合计与减项合计, 用调整净额代替抽屉内明细, 概览经营补充再挂调整净额指标卡

**Source Order Settlement Amount（结算金额）**:
客源单调整与优惠后的最终团款，等于原始团款 + 调整净额 − 整单优惠（即原始团款 + 增项合计 − 减项合计 − 整单优惠）；只读派生，且必须 ≥ 0，否则不可保存。收款拆分与提交应收以此为准。发团「结算应收」汇总本团全部客源单的结算金额。客源单抽屉底部结算预览突出展示该项。
_Avoid_: 实际应收（作系统规范名）, 把整单优惠并进调整净额后再算结算, 结算金额含手工其他应收, 负结算金额, 静默把负结算夹成 0

**Source Order Settlement Preview（结算预览）**:
客源单抽屉底部的只读核对区，按行展示团款结算构成（原始团款、调整净额、团款优惠）、代收约定（随收款方式变化）与预计差额（客户补款 / 预计返利；全部客户结算时为无补款返利），并突出结算金额。数值均由已有派生量聚合，不新增领域事实，也不替代提交应收。
_Avoid_: 把结算预览当作可编辑录入区, 在预览里发明与收款拆分不一致的补款/返利公式, 用预览替代应收节点

**Source Order Guest List（客人名单）**:
客源单下的计调备忘名单，记录出行客人姓名等便于现场对接的信息。不是客源单人数、团款或应收的事实来源；名单人数可与客源单总人数不一致，也不提供「同步人数」回写客源单。产品抽屉内与团款调整项同构：按行显式保存后再纳入待提交名单，区块可折叠；存在未保存的名单或调整草稿行时不可保存客源单。
_Avoid_: 游客档案（与应收收款对象混淆时）, 名单即人数, 报名单, 同步人数, 总保存时静默吞掉未确认草稿行

**Guest Representative（游客代表）**:
发团运营表中代表一条客源单显示的单个现场联系人，第一版静默取该客源单客人名单中按创建时间最早的一条记录，仅展示其姓名与已有联系电话；名单为空则留空，不在客人名单界面增加规则提示。后续只有出现明确业务需求时才增加指定游客代表的能力。
_Avoid_: 导出完整客人名单, 第一版指定游客代表, 客人名单界面解释导出规则, 名单完整度

**Collection Split（收款拆分）**:
客源单上表达「谁向游客收、收多少，以及地接与合作方如何轧差」的录入方式。合作方可已向游客收定金（P）；地接可向游客收定金与/或尾款（合计 G）。**不强制** P+G=结算金额 S。地接与合作方之间的现金往来只看 G 与 S：客户补款=`max(0,S−G)`，返利=`max(0,G−S)`；P 为业务事实，不进入这两式。录单须能分别录入定金与尾款；**G约定**（录单定金/尾款派生）做计划与生成补款；**G实收**（已核销到本单定金代收与尾款代收节点的金额合计；不含未核销流水）做齐账后校正与返利落账。现行库中客源单统一按本语义，不保留旧 `P+G=S` 口径双轨。硬拦：金额不得为负；代收场景 G约定 必须 >0（否则应改选全部客户结算）；不因 P>S 或 G与 S 关系拦截。细节见 ADR-0033。
_Avoid_: 收款责任, 收款方式（作领域术语时）, 团款组成, 强制客户已收+我方代收等于结算金额, 客户补款金额等于客户已收定金, 把未核销游客收入计入 G实收, 旧口径客源单长期只读兼容模式, 强制代收两期金额均大于零, 因 P>S 拒绝保存

**Partner Collected Deposit（客户已收 / 定金）**:
合作方已向游客收取的金额 P。可大于或小于结算金额。它不是客户补款应收的金额来源；合作方代收的定金不生成地接侧「定金代收」游客应收。
_Avoid_: 客户补款（与本词混淆）, 押金（作系统规范名时；对外确认单习惯名另映射）

**Source Order Actual Collection Settlement（按实收结算）**:
代收场景下，游客代收齐账后用当前 G实收**校正**客户补款应收，并**生成或更新**返利应付。**仅当相关定金代收/尾款代收节点均已结清时**触发；核销成功后由系统自动执行，产品 UI 不提供「提前按实收结算」入口。客户补款可在提交应收时已按 G约定存在，齐账后改为 `max(0,S−G实收)`（未 touch；已覆盖则关闭）。已对补款或返利节点 finance-touch（有效核销）后不得静默重算，须先撤销核销或走明确调整。API 可在齐账后幂等重跑。
_Avoid_: 提前按实收结算, 未齐账落返利, 齐账后仍强制人工点「按实收结算」
_Avoid_: 按周自动结算, 出团后 N 天强制结算, 新核销后自动重算已 touch 的补款/返利, 半收款状态静默翻转为返利

**Customer Settlement Receivable（客户补款）**:
应收路径之一：地接代收不足以覆盖结算金额时，合作方尚需付给地接的差额。金额 = `max(0, S−G)`，收款对象为发客 Partner。代收场景下：**提交应收时**若 S−G约定>0，按 G约定同批创建节点；齐账后按 G实收校正。G约定≥S 时提交应收不建补款。全部客户结算且无地接代收时，仍在提交应收时走整笔 S 的客户结算路径。
_Avoid_: 组团社已收, 客户已收金额（作应收名称）, 客户补款等于定金 P, 代收场景把客户补款推迟到齐账后才首次创建

**Guest Collection Receivable（游客代收）**:
应收路径之一：地接需直接向游客收取的部分。可拆为 **定金代收** 与 **尾款代收** 两张收付款节点（均属 Guest；稳定键仍关联客源单 id，期次/来源子类型区分账单）。收款对象展示统一为「游客」。流水名称可手改为客人姓名仅作展示；核销按类型 + 客源单 id（及节点）匹配。仅地接实际向游客收的期次生成节点；合作方代收的定金不在此开单。各期约定合计为 G约定，可大于 S；G实收仅为这些节点上的有效已核销合计。提交应收时建这些游客节点，并在 S−G约定>0 时同批创建客户补款；**不**连带创建返利。节点尚无有效核销时，改客源单收款方式/定金/尾款须同步调整对应 Guest 与补款节点；已有有效核销则锁定。
_Avoid_: 地接代收（作应收单正式 title 时与定金代收/尾款代收分流冲突时）, 我方代收金额（作应收名称）, 仅靠往来名称匹配游客代收, 应收列表／编辑／详情收款对象再展示客源单 displayName, 一张节点用备注代替定金/尾款分账, 未核销流水计入 G实收, 改约定后与 Guest 节点长期分叉

**Source Order Rebate Payable（返利应付）**:
地接向游客代收合计 G 大于结算金额 S 时，应付给发客合作方的差额，金额 = `max(0, G−S)`。系统规范名 **返利**；属对 Partner 的应付。计划期按 G约定仅预估展示；节点仅在齐账后按实收结算且 G实收>S 时生成或更新。办理无固定系统截止日。返利预估与已确认口径仍可在明细、运营表等查阅；概览 B 款主层不再独立展示返利卡，且不并入结算应收分母。其它非返利的合作方应付仍可手工录入。见 ADR-0033。
_Avoid_: 返佣, 佣金应付, 负应收, 冲减结算金额, 把经济利润 P+G−S 当作地接须付出现金的返利, 提交应收时按 G约定先建返利应付节点

**Source Order Partner Settlement（客源单合作方结算）**:
地接与发客合作方之间除客户补款应收外的结算义务总称；其中 **返利应付** 为已结构化路径（见上）。历史表述「收全款后付合作方」现归入返利轧差模型，不再默认「第一版只能纯手工、无派生」。
_Avoid_: 返佣, 佣金应付

**Itinerary Segment（行程段）**:
发团内按日期、目的地切分的行程规划单元，可为跨多日段（如「喀纳斯段｜7月1日至3日｜3天」），也可为一键生成的 **一日一段** 执行骨架（出团日～回团日各一段，段名/目的地可后填）。行程段是按日成本侧锚点之一，其下挂多条段资源行；段内可同时存在自营资源与拼出资源。每段可录票型人数（全/半/学/免）；与客源人数合计不一致时醒目提示，默认可保存。不引入独立「行程日报」实体；与 Product 上的产品日行程（Product Day）不是同一概念。见 ADR-0034。
_Avoid_: 执行批次, Execution Batch, Day（作独立实体名）, 行程日报（作独立实体或并行工作台）, DailyReport, 产品日行程, 简版行程, 详细行程, 自营段, 拼出段, 禁止一日一段

**Segment Resource（行程段资源）**:
挂在行程段下的成本费用行，统一承载自营资源与拼出（含按日挂段的酒店、门票、拼出等）。计调在段内按 **资源种类** 添加费用行；**全部种类（含拼出）关联 Supplier**，且该种类须属于该 Supplier 的 **供应商类别** 集合。拼出对应供应商类别存值仍为 `outsource`，供应商名录 UI label 为 **旅行社**（资源种类 UI 仍为 **拼出**）。添加/编辑资源时对手方字段统一称 **供应商**（placeholder「选择供应商」）。资源安排列表、发团运营结算预览与 Excel 导出中，该列产品 UI label 亦称 **供应商**，不用「对手方」「承接方」。触发提交应付后，该主体在应付侧展示为 **付款对象**。一段内可有多条、种类可混合；同一 Supplier 可被不同种类的资源行引用（如酒店供应商既挂酒店费又挂餐费）。触发提交应付时以资源行为来源。历史拼出行可能仍挂 Partner（承接方），读路径与应付生成按已存 `counterpartyType` 处理；写路径不再接受 partnerId。第一版不提供资源人工排序；发团运营表在行程段顺序内按资源种类固定顺序，再按资源名称与供应商名稳定排列。全程费用见 **Departure Resource**；班组选人见 **Execution Crew**，不经本实体提交应付。
_Avoid_: 资源安排（作实体名或独立 Tab）, Resource Item, 拼出记录, Segment Outsource, 执行方式, 一家供应商只能对应一种资源行, 拼出表单称「承接方」或「承接同行」, 资源列表/运营结算列头称「对手方」, 新写拼出挂 Partner, 用段资源代替发团级全程费用唯一挂法, 选班组即创建段资源应付

**Resource Kind（资源种类）**:
一次行程段资源或发团级资源安排所用资源的分类，存为单值。取值与 **供应商类别** 共用同一套枚举（规范名 Resource Kind）：用车、酒店、导游、门票、用餐、景区、购物店、演出、保险、拼出、其他。产品 UI label **资源种类**（不用「资源类型」「供应商类别」指代资源行字段）。执行安排添加/编辑资源的种类下拉**不含**购物店、演出（购物店返利等走增收记录）；存量购物店/演出资源行仍可读可改；供应商类别下拉仍含购物店、演出。拼出表示整段或部分服务拼给旅行社类供应商；资源侧 label 为「拼出」，供应商类别侧同一存值 label 为「旅行社」。`meal` 两侧 UI label 均为 **用餐**。添加/编辑资源与供应商类别下拉共用「拼出／旅行社前置」的展示顺序；发团运营表资源排序仍用独立的固定种类顺序。
_Avoid_: 资源类型, Supplier Category（作资源行字段名）, 车队/餐厅/票务（作与用车/用餐/门票并列的另一套 label）, 为资源行与供应商维护两套枚举, 拼出不得写入供应商类别, 发团级资源另套种类枚举

**Departure Resource（发团级资源）**:
挂在发团上的成本费用行，用于全程用车、保险、导游等不必硬塞进某一日段的费用。Resource Kind、供应商类别校验、显式提交应付、finance-touched / 作废与重开等规则与行程段资源同一套；计入成本合计与资源付款进度分母。不是执行班组，选司机/导游不创建本实体。
_Avoid_: 强制全程费用挂到某一日段, 发团级资源另套应付规则, 班组选人即发团级资源应付, 独立日报成本行

**Execution Crew（执行班组）**:
发团级结构化执行负责人信息：司机（须选自供应商类别含用车的 Supplier）、导游（须选自类别含导游的 Supplier）、车牌（自由文本，默认可空）、联系电话（自由文本，默认可空）。与成本资源完全分离，选择或变更班组 **不** 提交应付、不草稿化导游/用车资源行。本迭代仅发团级一组；不在供应商档案上维护车牌或驾驶员字段。运营表与详情可展示班组。见 ADR-0034。
_Avoid_: 班组仅备注, 选班组自动提交应付, 段级多班组轮换（本迭代）, 供应商档案车牌/驾驶员字段（本迭代）, 用执行班组指代导游或用车成本资源

**Ticket Type Headcount（票型人数）**:
行程段（通常为一日段）上登记的全/半/学/免人数，均为非负整数。用于当日票务与人头核对；各段合计（或产品选定的执行人头口径）与本团客源单人数合计不一致时醒目提示，默认可保存，不硬拦。
_Avoid_: 票型与客源人数不一致即禁止保存, 用票型人数代替客源单人数作结算分母

**Departure Income Record（团内增收记录 / 增收记录）**:
发团下团款之外的附加收入登记行，类型为购物店返利、车销收入、自费项目返利、其他增收；含项目名称、可选合作方（Supplier）、增收金额、可选导游、可选导游提成（非必填，空则按 0）。收账状态与提成状态仅为台账标记，不生成收付款节点、不要求流水；≠ 资源成本或应付。列表展示备注。取代原团上收入台账。见 ADR-0036。
_Avoid_: 团上收入台账, Ground Income Ledger, 其他收入（作台账或概览卡名）, 增收记录自动提交应收或应付, 把增收记入资源成本或应付合计, 独立佣金/销售子系统, 与团上收入台账并存

**Company Income（公司增收）**:
单条增收记录的派生金额，等于增收金额减导游提成；行内与表单字段用此名，不单独落库为可编辑字段。
_Avoid_: 把公司增收另存为可手改字段, 公司增收与增收净收益用不同公式

**Additional Income Net（增收净收益）**:
本团全部有效增收记录的公司增收合计。概览经营概况横条与运营表该分区合计用此名；计入**收入合计**与当前毛利，但不计入结算应收或概览收款进度分母。≠ 其他应收，≠ 未核销收入流水。
_Avoid_: 其他收入（作概览卡名）, 用增收金额合计冒充净收益, 把增收净收益并入结算应收或收款进度分母, 用流水合计代替台账作增收净收益

**Execution Arrangement（执行安排）**:
发团详情中规划行程段、段内资源与发团级资源（含自营与拼出）的工作区；详情页 Tab 正式称「执行安排」。区内「行程段」指段导航（对应 Itinerary Segment），「资源安排」指资源主表——二者为工作区内称谓，不是独立 Tab。支持按出团～回团日一键生成一日一段骨架，并维护发团级资源与执行班组；不另开「行程日报」菜单或实体。资源安排标题行可展示当前行程段的资源项数、**资源金额**与 **尚未提交应付**（口径为发团概览成本合计／尚未提交应付收窄到本段；无资源时不展示；尚未提交应付为 0 时省略该项）；服务配资源与批量提交应付，不在此重复全团成本合计或确认应付对照。
_Avoid_: 行程段/资源安排作独立详情 Tab, 行程, 资源配置, itinerary, 独立行程日报工作台, 执行安排内全团统计卡, 段汇总称成本合计或确认应付或待提交应付, 段汇总用多卡 Statistic 墙

**Departure Operations Sheet（发团运营表）**:
发团级、带导出时点的只读运营结算快照，把本团客源单、按行程段排列的资源（种类、供应商、名称、约定总价）、收付款及核销进度与各处非空备注组织成一张可核对、可预览、可导出的事实表，用于替代纸质「一趟团总账本」式手写记录。第一版使用一套固定结构，尽量覆盖系统当时能够产出的运营与财务事实，不另分运营版和结算版；它可在发团生命周期的任意阶段生成，早期主要呈现计调已录入的运营与约定金额，财务介入后逐步呈现流水及核销结果。面向普通业务与计调的主表使用应收/已收/未收和应付/已付/未付；其中已收、已付仅指已核销到具体账款的金额，未核销收支流水不混入具体客源或资源行，而在表尾逐笔列出流水方向、交易日期、往来对象、金额、收付款方式与流水备注，并分别合计待确认收款和待确认付款。一条客源单保持一个业务主行，主行含调整净额（与系统内列表同口径，不展开团款调整种类明细）；客户结算与游客代收作为收款路径子行，分别展示各自的应收与收款进度，不把不同付款方混成一个进度。财务流程整体尚未开始时，只在表头表达该数据阶段，不逐条标记「应收单未生成」或「应付单未生成」；各行仍显示客源或资源的约定应收/应付，已收/已付与未收/未付用「—」表示尚无进度口径，不用 0 冒充财务已确认的结果。部分客源或资源已进入财务流程时，表头标为部分开始，已生成账款的行展示实际收付进度，未生成的行继续显示「—」；进度汇总只统计已有财务口径的行。已关闭账款仍如实显示剩余未收或未付，并标为需要线下沟通处理的异常，不在导出表展开关闭原因。业务源约定金额与财务账款金额不一致时不阻止导出，须并列显示两种金额及实际收付并标为需核对；主表应收/应付仍以业务源事实为准，收付进度以财务账款为准，该异常行不进入正常未收/未付汇总。发团、客源、行程段与资源各自的非空备注跟随所属事实完整导出，不合并成无归属的大段文字；发团级 **执行班组**（司机、导游、车牌）以结构化字段纳入发团头或班组区展示，不靠备注顶替。发团级资源与段资源一并进入资源/应付分区；增收记录有数据时分区展示（类型、项目名称、合作方、增收金额、导游提成、公司增收、综合状态；分区合计为增收净收益）。缺失的后续事实保持未发生或待补充，不伪装成零或已完成，并明确标注导出时点与数据阶段。第一版交付形态为页面预览与带打印版式的 Excel（`.xlsx`）下载，PDF 后置；页面与 Excel 都使用一张表的纵向分区顺序：发团与数据阶段（含班组）、客源及应收、行程段/发团级资源及应付、待确认款项、财务汇总与异常、发团级备注（及增收记录分区若有数据），不拆多工作表，也不做一张超宽同行表。Excel 默认 A4 横向、宽度缩放为一页、高度自然分页，重复关键表头并尽量避免拆开同一行程段；备注自动换行，异常同时使用文字而不只依赖颜色。拥有发团菜单权限的计调、财务与企业管理员看到同一完整内容，不按角色隐藏金额或游客代表电话。导出文件标注所属 Organization、导出人及快照时间，后端继续强制 Organization 数据隔离；每次即时生成当前快照，不在系统内保存文件副本或导出历史。客源仍属整趟发团，不嵌进行程段；不引入独立「行程日报」实体或与执行安排并行的第二套工作台，但允许一日一段行程段骨架与发团级资源。
_Avoid_: 独立行程日报实体, 日报表（作并行工作台或双轨实体）, 运营版与结算版并行, 仅含运营事实不含财务进度, 仅结算完成后允许导出, 用 0 代替尚未发生或待补充, 行程总账本, 客源挂行程段, 第一版含单价×数量, 禁止按日段能力, 班组仅备注不可结构化

**Payment Schedule（收付款节点）**:
Organization 内的计划应收或计划应付，承载约定金额、到期日、往来对象与来源追溯。从客源单、行程段资源或发团级资源 **显式触发** 生成；保存源事实本身不产生收付款节点。执行班组与团内增收记录不触发本实体。结清状态从核销分配派生，不是发团或客源单的属性。产品 UI 在应付侧称 **应付单**、应收侧称 **应收单**；用户可见的 `scheduleNo` label 按语境分流为 **应收单号** / **应付单号** /（核销等混列）**收付款单号**（ADR-0003 格式），不用「节点编号」。编辑/详情抽屉外壳称「编辑应收单／编辑应付单」「应收单详情／应付单详情」。到期日对应收是账期事实；对应对付本版不作为账期、也不驱动逾期，产品 UI 不暴露应付到期日。生命周期操作文案仍可用「关闭节点」「重新打开节点」等领域用语。
_Avoid_: 应收, 应付, 账款, Departure Receivable, 发团应收, 节点编号（作用户可见 scheduleNo label）, 编辑节点／应收节点详情／应付节点详情（作抽屉 title）, 应付到期日作账期或逾期依据（本版）

**Receivable Due Date（应收到期日）**:
应收收付款节点上的约定账期日。从客源单显式生成时，默认按所属发团 **出团日** 所在自然月的 **下一自然月 10 日** 写入（12 月出团则次年 1 月 10 日）；同一客源单下客户补款与游客代收路径共用该默认值。写入后为生成时快照，出团日变更不回写；未财务介入前可手改，介入后锁定。仅应收到期日与业务日比较可派生逾期。
_Avoid_: 用结束日作应收到期日默认, 按生成当日推算账期, 双路径不同默认到期日, 出团日变更自动改已生成到期日, 应付账期（本版）

**Payment Schedule Status（收付款节点状态）**:
后端存储的结清态枚举：`pending`（未结清且未逾期）、`overdue`（未结清且已逾期）、`settled`（已结清）、`cancelled`（已关闭）。**不**单独存 `partial`；部分核销后只要未结清金额大于零，状态仍为 `pending` 或 `overdue`。`overdue` **仅适用于应收**；应付未结清只可能是 `pending`（或已结清/已关闭），本版不因到期日进入逾期。
_Avoid_: 部分收款/部分付款（作存储状态）, 待收款/待付款（作枚举值）, 应付 overdue, 双向对称逾期

**Payment Schedule Settlement Label（收付款节点结清展示）**:
列表与详情面向财务人员的结清展示文案，由方向 + 已核销金额 **展示层派生**，不写入数据库。产品 UI 列头按方向分流为 **收款状态**（应收）/ **付款状态**（应付），取值仍为：应收侧待收款、部分收款、已收清；应付侧待付款、部分付款、已付清。它不是后端 **Payment Schedule Status**（含已关闭/逾期存储态）。节点是否已关闭由 Status（`cancelled`）单独表达，**不**用「已关闭」替代结清展示文案；已关闭且仍有未结清时，收款/付款状态仍可为部分收款/部分付款。仅应收的 `overdue` 可在标签上叠加「已逾期」，不替代上述结清展示文案；应付本版不展示已逾期。
_Avoid_: 与 `Payment Schedule Status` 枚举一一对应, 列头统一称「结清进度」, 把收款/付款状态当成存储枚举, 新增 partial 枚举, cancelled 时用已关闭盖掉部分收款/部分付款, 应付已逾期标签

**Payment Schedule Close（关闭节点）**:
财务人员终止该收付款节点后续追收/追付的动作，将节点置为 `cancelled`（已关闭）。关闭 **不是** 消化或放弃未结清金额，也 **不是** 作废整条财务记录；它仅用于财务已经介入且仍有未结清金额时，因商务争议、转外部或专项处理等原因停止继续跟进。财务未介入的资源应付只能作废、不能关闭；已结清节点不可关闭。关闭不撤销、不抹除已有核销，不改写约定金额；关闭后不可新增核销，已有核销仍可查看与撤销，撤销后节点默认保持已关闭。关闭后节点上仍展示约定 / 已核销 / 未结清（未结清不归零）；该未结清 **不再计入** 发团开放待收/待付。关闭必选处置类型并必填具体说明，自动保留当时约定/已核销/未结清快照、操作人与时间；有有效核销时，确认弹窗提示将停止后续收付且已核销与未结清仍保留在节点上。不引入「已放弃金额」，不造流水、不造核销。
_Avoid_: 已放弃金额, 单一自由文本吸收所有纠错, 金额减免作关闭原因, 关闭即未结清归零, 财务未介入时关闭资源应付, 已结清仍可关闭, 关闭即撤销核销, 关闭等同删除或作废, 关闭原因可选, 关闭等同作废（整条不计入财务）, 关闭时改写约定金额, 用关闭纠正未介入的误生成

**Resource Payable Void（资源应付作废）**:
财务尚未介入时撤销误生成资源应付节点的纠错动作；仅不存在任何核销历史（包括已撤销记录）、从未明确调整约定金额且尚未关闭的资源应付节点可作废。作废与关闭严格互斥：未介入资源应付只能作废、不能关闭；财务介入后只能在仍有未结清时关闭、不能作废。作废原因必填，使用不超过 200 字的具体说明，不另设结构化作废类型；系统同时记录作废前金额、操作人与时间。作废节点不再作为有效确认应付参与汇总与进度，但仍永久保留来源、金额与完整审计事实；应付列表默认排除已作废节点，状态筛选增加「已作废」，仅用户主动筛选时进入只读审计视图。作废后对应资源恢复为未提交、金额字段解锁；计调修正源事实后可提交新节点，同时保证同一资源最多只有一个有效应付节点。第一版仅支持资源应付，不支持客源应收作废。
_Avoid_: 删除节点, 默认列表展示已作废节点, 无法筛选已作废节点, 作废原因可选, 为作废复制关闭处置类型, 用关闭代替作废, 财务介入后作废, 未介入时同时展示作废与关闭, 客源应收作废, 作废后仍计入确认应付, 作废清除审计记录, 作废后资源仍锁定, 作废后禁止重新生成, 同一资源存在多个有效应付节点

**Payment Schedule Close Disposition（节点关闭处置）**:
解释未结清仍存在时为何停止当前节点追收/追付的结构化决定，处置类型为「转外部或专项处理」「商务争议停止跟进」「其他」，并搭配必填具体说明。它不改变约定金额或证明义务消灭；金额重新议定走约定金额调整，错误核销走撤销核销，错误流水走作废或反向资金记录。
_Avoid_: 已放弃金额, 金额减免, 流水录错, 核销错配, 暂时未收/未付（保持节点开放）, 无具体说明的其他

**Payment Schedule Reopen（重新打开节点）**:
财务人员在关闭决定不再成立时，恢复原收付款节点后续追收/追付的明确动作。重新打开原因必填并保留原关闭履历；打开后根据当前有效核销与到期日重新派生待收/待付、部分收付或逾期进度，沿用原节点并恢复登记收付与匹配流水，不生成新节点。若所属发团已结清，财务须明确确认节点打开与发团回退待结算的联动影响；两者作为一个不可分割的业务动作完成，无需 OP 预先审批，但发团回退记录必须对 OP 可见。
_Avoid_: 删除关闭记录, 无原因恢复, 为恢复追收/追付而新建节点, 重新提交应收/应付, 打开后恢复普通金额编辑, 先打开节点后回退发团, 节点开放但发团仍已结清

**Payment Schedule Activity（收付款节点操作记录）**:
挂在该条收付款节点上的可读操作时间线。第一版覆盖：关闭节点（含原因与当时约定/已核销/未结清快照）、关闭后撤销核销（含已核销与未结清变化）、重新打开节点（含打开原因与打开后结清进度），以及明确调整约定金额（含调整前后金额、原因与操作人）。用于财务溯源，与全局核销列表互补，不替代核销实体本身；生成、普通编辑、登记、匹配等完整履历不在第一版范围。
_Avoid_: 仅全局核销/流水侧留痕, 第一版只存关闭字段不做节点时间线, 调整金额不记原因, 用流水详情操作记录代替节点履历, 第一版做完整节点 audit（生成/编辑/登记/匹配一并纳入）, 已放弃金额

**Payment Schedule Financial History（收付款节点财务履历）**:
表示节点是否曾发生核销（含后续已撤销）、调整或关闭等财务动作的不可逆历史事实。它与当前有效已核销金额独立：撤销最后一笔核销可使有效已核销归零，但不会清除财务履历或恢复为「从未财务介入」。
_Avoid_: 用当前已核销金额代替财务履历, 撤销最后一笔核销后履历归零, 从未财务介入

**Payment Schedule Amount Adjustment（收付款节点约定金额调整）**:
已有财务履历的源事实收付款，不因有效核销归零而恢复普通金额编辑；节点未关闭且当前有效核销为零时，可通过明确调整动作同步修正源事实与原节点，不新建节点。资源应付同步修正原资源约定金额；客源应收按节点来源定位客户补款或游客代收路径并同步该路径金额，不得静默修改或重建同一客源单的其他路径。调整原因必填并记录调整前后金额与操作人；仍有有效核销须先撤销，已关闭节点不可调整。
_Avoid_: 撤销核销后恢复普通金额编辑, 永久锁死无纠错入口, 调整时新建收付款节点, 源事实与节点金额分叉, 调整一路径静默改其他路径, 无原因调整

**Source Order Receivable Status（客源单应收状态）**:
客源单级结清进度展示态，由下属应收收付款节点聚合派生：未提交、待收（已提交且未收款）、部分收款、已收齐；若曾提交的应收节点均已关闭，展示为已关闭，**不**回退为未提交，亦**不**因关闭前曾有部分核销而展示为部分收款或已收齐。仅「未提交」允许触发财务提交；其余状态客源列表操作为「查看应收」（跳转本发团应收管理，预填收款对象筛选为该客源单发客客户/合作伙伴，并对该客源单下全部应收行——含已关闭节点——做一次高亮闪烁定位，非选中态），不可再次提交。结清进度聚合时忽略已关闭节点：仅部分路径关闭时，状态仍由剩余活跃节点派生（待收/部分收款/已收齐），不因单条关闭整单变为已关闭。已关闭时客源单不可删除、金额字段只读，非金额字段可改。
_Avoid_: 已提交（作待收的正式 label）, 关闭后等同未提交, 用节点级结清文案直接当客源单状态枚举名, 重新生成, 关闭后可删源事实或再改金额并生成, 任一路径关闭即整单已关闭, 全部关闭且曾有核销改展示为部分收款/已收齐, 查看应收隐式按 ID 过滤且筛选区空白

**Segment Resource Payable Status（资源应付状态）**:
行程段资源级结清进度展示态，由该资源关联的有效应付收付款节点派生：未提交、待付（已提交且未付款）、部分付款、已付清；撤销最后一笔有效核销且节点未关闭时，从部分付款/已付清回到待付，仍视为已提交并沿用原节点继续登记付款或匹配流水。若有效应付节点已关闭，展示为已关闭，**不**回退为未提交，亦**不**因关闭前曾有部分核销而展示为部分付款或已付清。仅「未提交」可提交应付；待付、部分付款、已付清与已关闭状态均进入「查看应付」，不可再次提交。未介入节点作废后不再视为有效节点，资源恢复为未提交并允许修正金额后提交新节点；旧作废节点仍保留审计但不参与状态聚合。已关闭时资源不可删除、金额字段只读，非金额字段可改。
_Avoid_: 已提交（作待付的正式 label）, 撤销核销后回退未提交, 撤销核销后要求新增资源, 关闭后等同未提交, 关闭后重新提交, 作废后仍视为已提交, 作废节点参与资源应付状态, 同一资源存在多个有效应付节点, 关闭后可删资源或再改金额并提交, 全部关闭且曾有核销改展示为部分付款/已付清, 查看应付隐式按 ID 过滤且筛选区空白

**Finance Generation（财务生成）**:
计调或财务在源事实确认后手动触发的动作，将客源单转为应收收付款节点、将行程段资源转为应付收付款节点。有效节点遵守一次性入口：同一来源已存在有效应收/应付（含已关闭节点）后不可再次提交；已生成且未 finance-touched 时，编辑源事实保存可同步已有节点金额，并对客源收款拆分中新出现、金额大于零且尚无有效节点的路径 **补建** 收付款节点；已 finance-touched 后源事实变更仅警示、不静默改账款，亦不补建。资源应付的未介入节点作废后不再视为有效节点，允许修正资源金额并生成一个新节点；作废历史不删除，生成 API 必须保证同一资源最多一个有效应付节点。发团级「批量提交应收」与行程段级「批量提交应付」是对「未提交」源事实的显式批量触发，内部仍走单条提交约束；不是保存即提交，也不是后台自动批量提交。
_Avoid_: 保存即生成（首次）, 批量自动生成, 团款确认（Project 旧称）, 关闭节点后再次生成, 用再次点生成补路径, 作废后仍禁止资源应付重新生成, 删除旧节点后重建, 同一来源存在多个有效节点

**Partner（合作伙伴）**:
与 Organization 有业务往来的其他旅行社或同业客户。后台菜单、表单抽屉标题、名称字段等名录 CRUD 界面统一使用 **合作伙伴**（如「合作伙伴名称」「创建合作伙伴」），不用「同行」。在 **客源单** 等业务场景中，UI 可称 **客户** 指代所选 Partner（如「发客客户」「客户数量」），与名录管理用语并存。
_Avoid_: 同业, 客户/同行, agent, 同行名称, 编辑同行, 名录 CRUD 界面用「客户」代替「合作伙伴」

**Partner Kind（合作方向）**:
Partner 在业务往来中的方向角色：仅作客户方（发客/客源）、仅作承接方（拼出承接）、或双向合作。存为 `partnerKind`；产品 UI label **合作方向**。与 Partner Type 正交，共同构成 Partner 的双维分类。合作伙伴列表页展示 **统计卡**：在 **启用** 状态下按 `partnerKind` 互斥计数（总数、客户方、承接方、双向合作）。
_Avoid_: 业务身份, 合作类型, 与 Partner Type 混为一列, 统计卡含已归档或停用

**Partner Type（合作伙伴类型）**:
Partner 作为旅行社主体的类型分类，如组团社、地接社、渠道商、综合旅行社、其他。存为 `partnerType`；产品 UI label **合作伙伴类型**（列表/表单）。与 Partner Kind 正交。Partner Epic 1 仅交付名录 CRUD；拼出/客源等业务选择器的过滤规则在对应业务 Epic 再定。
_Avoid_: 同行类型, 与 Partner Kind 共用同一枚举, wholesaler 作 UI label「批发商」, Epic 1 预埋拼出筛选

**Partner Contact Role（联系人角色）**:
Partner 主联系人的职务，如老板、计调、财务、销售、客服、其他。可选字段，仅 Partner 目录表单使用；Supplier 无对应字段。
_Avoid_: 与 Employee Role 混淆, 创建时强制填写

**Partner Directory Form（合作伙伴目录表单）**:
Partner 创建/编辑/详情只读共用的 **三段** 结构：基础信息（含名称、合作伙伴类型、合作方向、状态）、联系人信息（含联系人角色）、结算信息（结算方式、账期规则、结算说明，整段可选）。**不含** Supplier 的「更多财务信息」与独立「备注」段；Partner 无发票、银行、参考报价、业务备注字段。创建时 **合作伙伴名称、合作方向、合作伙伴类型** 三者必填；联系人、结算字段均可选。列表交互与 Supplier 一致：名称链至详情页，操作列「编辑」开列表抽屉，详情顶栏「编辑」开详情抽屉；**不**采用单抽屉 view/edit 双模式。详情页 Tab：**基本信息** + **往来账款**（见 Partner Ledger Tab）+ **合作团单**（业务事实层容器，内含客源与拼出两分段，见 Partner Source Order 与 Partner Outsource Order）。
_Avoid_: 与 Supplier 共用五段表单, 为 Partner 增加 Supplier Financial Profile, 创建时 partnerType 可选, 单抽屉查看/编辑模式, 把客源与拼出糊成同一类行

**Partner Cooperation Orders Tab（合作团单 Tab）**:
Partner 详情中的**业务事实层** Tab 容器，产品 UI label **合作团单**。内含两个分段控件：**客源**（见 Partner Source Order）与 **拼出**（见 Partner Outsource Order）。分段控件独立于筛选条（与往来账款应收/应付同构：视图选择 ≠ 列表筛选）。三类合作方向均露出两段；默认分段为纯承接方→拼出、其余→客源；切换合作伙伴时重置分段为该 Partner 默认并清空出团日期筛选。出团日期筛选在 Tab 级共享，切换分段保留日期并将分页重置为第一页。空态按**当前分段**判断与文案，不因另一分段有数据而自动跳段。客源与拼出是两类不同业务事实（收入侧客源单 vs 成本侧拼出资源），不是「同一类团单、方向相反」。
_Avoid_: 按合作方向隐藏某一分段, 整 Tab 联合空态并自动跳段, 把拼出建成反向客源单, 分段标签写成「客源团单／拼出团单」叠词, 混成一张同构表, 分段控件塞进筛选条, 切换合作伙伴后保留上一档案的分段选择

**Partner Source Order（合作团单·客源）**:
合作团单 Tab 的**客源**分段：行＝该 Partner 名下客源单（跨发团），回答「这家发了哪些团、金额怎么构成」。汇总卡七项（客源单数/总人数/原始团款合计/调整净额合计/优惠合计/结算金额合计/游客代收合计）跟随筛选、所见即所导；优惠合计仍只汇总整单优惠，不含减项类团款调整；按所属发团出团日期筛选，默认不限＋倒序。列上有意不放应收状态与已收/未收（归往来账款 Tab），也不放游客代表与联系电话（仅出现在导出物）。《往来账确认单》导出的全系统唯一入口在本分段工具栏（不在拼出分段）。
_Avoid_: 与往来账款合并成一个对账视图, 列上放已收/未收或应收状态, 分段内用客户习惯名, 确认单入口挂到拼出分段或别处再开

**Partner Outsource Order（合作团单·拼出）**:
合作团单 Tab 的**拼出**分段：行＝历史引用该 Partner 为承接方的拼出行程段资源（Segment Resource，`resourceKind=拼出` 且 `counterpartyType=partner`，跨发团）。新写拼出已改挂 **供应商（旅行社类别）**，本分段仅覆盖存量 Partner 承接行；新拼出行在供应商 **服务团单** 中查看。列只放业务事实：出团日期、关联发团、行程段、资源名称、约定金额、备注（本段种类恒为拼出，不展示资源种类列）；**不**放应付状态/已付/未付（归往来账款 Tab）。汇总卡三项（资源行数/关联发团数/约定金额合计）跟随筛选、所见即所算；按所属发团出团日期筛选，默认不限＋出团日期倒序。列出全部该类资源行，不看应付是否已生成或作废，也不看发团状态。第一版无拼出对外结算单导出（确认单仍只含客源）。
_Avoid_: 行＝客源单或反向客源, 与客源混成同构行, 列上放应付状态或已付未付, 展示冗余的资源种类列, 只列已提交应付的资源, 排除已关闭/归档发团的资源, 把拼出塞进往来账确认单, 新写拼出仍挂 Partner

**Partner Ledger Tab（往来账款 Tab）**:
Partner 详情中的**财务账款层** Tab，行＝与该 Partner 往来的收付款节点，回答「这家欠我/我欠这家多少、收付到哪一步」。应收、应付两个子区各复用一次 PaymentScheduleWorkspace，数据按 `counterpartyType=partner + counterpartyId` 精确过滤；游客代收节点（counterparty=guest）不属于该 Partner 账款、不出现在本 Tab。汇总卡每方向三项（约定/已核销/未结清）跟随筛选，已关闭、已作废节点不计入；应收侧约定合计拆显「客户补款／其他应收」以对确认单。主时间轴为所属发团出团日期区间（与确认单周期同口径，手工其他应收随其归属发团落入区间），另保留到期日筛选做催收。Tab 内完整操作，与全局应收/应付页一致。
_Avoid_: 列上放人数/单价/优惠, keyword 模糊匹配往来对象, 游客代收计入该 Partner 账款, 汇总含已关闭或已作废节点, 只读＋跳全局页

**Partner Reconciliation Statement（往来账确认单）**:
按对账周期面向单一 Partner 生成的对外对账单据，供对方签章确认。周期口径为所属发团出团日期区间；行＝周期内该 Partner 全部客源单，明细按出团日期正序，应收节点已关闭的客源单照常列入且不做标记。只含业务事实与收款拆分，不含核销进度，也不含手工应收（其他应收），**不含**拼出资源。对外命名沿用客户习惯名，与系统规范名映射：拼入合计＝原始团款合计、原始应收＝原始团款、实际应收＝结算金额、客户已收押金＝客户已收（定金）P、客户补款＝轧差补款（有则列）、游客代收同名；返利列可后置。该豁免仅限本导出物，系统 UI 仍用规范名。单价按「拼入单价（成人）／拼入单价（儿童）」拆列，与成人/儿童人数列对应；客源行另列调整净额与优惠，逐行可复算：原始应收 + 调整净额 − 优惠 = 实际应收。不展开团款调整种类明细。保留游客代表与联系电话（信息源头即收件方），客人名单为空则留空；不设客户单号列。即时生成当前快照、不存副本，签回与确认状态走线下，系统内不跟踪。全系统唯一入口在合作团单 Tab 的**客源**分段工具栏。
_Avoid_: 客户习惯名作系统 UI 文案, 确认单含核销进度, 手工应收上确认单, 拼出资源上确认单, 剔除或标记已关闭应收的客源单, 客户单号列, 系统内跟踪签回状态, 月度对账单（作正式名）, 确认单入口挂到拼出分段, 客户已收押金＝客户补款（旧映射；P 与补款已分概念）

**Supplier（供应商）**:
为发团提供资源的服务商，如酒店、用车、餐、景区等。供应商档案是 Organization 内的自营资源名录，主路径是在发团执行安排中选 supplier，而非独立 ERP。一家供应商可同时提供多种资源（多选 **供应商类别**）。
_Avoid_: 资源商, vendor, 车队/餐厅（作与用车/餐对立的另一套主体称呼）

**Directory Profile Status（目录档案状态）**:
Partner 与 Supplier 目录记录的生命周期状态：启用、停用、已归档。适用于目录实体，不适用于 Employee（Employee Status 仅启用/停用）或 Organization 账号。
_Avoid_: 复用 Employee Status, 用 deletedAt 表达归档

**Directory Archive（目录归档）**:
将 Partner 或 Supplier 从默认目录列表中移除，同时保留行以供历史追溯。归档后状态为已归档；记录仍可从历史发团、资源、财务上下文中链接，但不得出现在默认目录列表或新业务的选择源中。产品 UI 可将此操作标为 **删除**，当用户意图是从日常名录工作中不再看到该档案时。默认目录列表不展示已归档行；用户可通过「显示已归档」查看并 **恢复**。
_Avoid_: 归档当作数据库硬删, 归档与停用混为一谈, 归档后不可恢复

**Directory Restore（目录恢复）**:
将已归档的 Partner 或 Supplier 恢复为非归档状态，使其重新出现在默认目录列表。恢复后状态通常为启用，是否改回停用由用户在编辑抽屉中另行调整。
_Avoid_: 恢复当作新建, 恢复后自动硬删历史引用

**Supplier Disabled（供应商停用）**:
供应商目录状态为停用。档案仍在非归档的目录管理视图中可见，但不得在新业务（如资源安排、应付对手方选择）中被选用。
_Avoid_: 停用当作归档, 停用后从列表隐藏

**Partner Disabled（合作伙伴停用）**:
Partner 目录状态为停用。档案仍在非归档的目录管理视图中可见，但不得在新业务（如客源单选客户、行程段拼出选承接方）中被选用。Partner Epic 1 仅实现名录 CRUD 与 lifecycle API，**不在**业务选择器中 enforce；enforcement 在客源/行程段 Epic 落地。
_Avoid_: 停用当作归档, Epic 1 在选择器中拦截停用或归档 Partner

**Directory Name Uniqueness（目录名称唯一）**:
同一 Organization 内，Partner 与 Supplier 各自按 **名称** 唯一；不得存在两条同名且均未归档的目录记录。保存重名时拒绝并提示用户修改名称或在名称中加入可区分后缀（如门店、区域）。
_Avoid_: 允许同名靠备注区分, 全局跨 Organization 唯一

**Directory Catalog（目录层共用）**:
Partner 与 Supplier 在名录层的共用概念与展示规则，包括三态生命周期、归档/恢复、名称唯一、结算字段 enum 与 UI label（账期规则、结算说明）。表单段数与字段集 **不必相同**：Supplier 为五段且含更多财务信息与备注；Partner 为三段且无发票/银行。Epic 先交付 Supplier，Partner 后续 Epic 复用目录层生命周期与结算 catalog，而非两套独立 CRUD。实现 Partner 时 **inline** 从 Supplier 抽出共用 catalog（如 `features/directory/`），不单独前置 refactor sprint。
_Avoid_: Partner 与 Supplier 各写一套归档规则, 目录层与 Employee 生命周期混用, 强行统一五段表单, Partner 全量 copy 后再做 directory 抽取

**Directory Payment Term Rule（账期规则）**:
Partner 与 Supplier 目录层的结算节奏，如每团结、周结、半月结、月结、按约定。Supplier 存为 settlementCycle，Partner 存为 paymentTermRule，产品 UI 统一 label **账期规则**。
_Avoid_: 结算周期, 账期, payment term 混用在目录表单

**Directory Settlement Notes（结算说明）**:
Partner 与 Supplier 目录层的自由文本，说明结算例外或对账补充（如「出团后 7 个工作日结」）。产品 UI 统一 label **结算说明**。
_Avoid_: 结算备注, 账期说明

**Supplier Reference Quote Notes（供应商参考报价说明）**:
Supplier 目录层的自由文本，描述典型报价方式（如人均价、含餐、淡旺季）。仅供计调参考，不是结构化价格事实，不驱动资源金额或应付。
_Avoid_: 默认单价, 参考价作系统计价

**Supplier Category（供应商类别）**:
Supplier 名录中对该服务商 **可提供哪些资源种类** 的分类，为 **非空多选**（无主类别）。取值类型与资源行的 **资源种类** 相同（Resource Kind），**含拼出**（存值 `outsource`，供应商 UI label **旅行社**）；`meal` UI label **用餐**（与资源种类一致）。供应商类别下拉与添加资源种类下拉共用展示顺序（旅行社／拼出置于前五）；发团运营表资源排序另有固定顺序。列表与执行安排选供应商时，按「类别集合包含所选资源种类」筛选（选资源种类「拼出」时筛出类别含旅行社的供应商）。编辑类别时不得移除仍被关联资源行使用的种类；资源行保存时，资源种类必须属于该供应商的类别集合。
_Avoid_: 单选类别, 主类别+附加类别, 与资源种类维护第二套枚举, SupplierCategory 独立枚举类型, 类别当作与资源种类无关的另一套取值, 拼出不得作为供应商类别, 餐厅/车队/票务（作与用餐/用车/门票并列的旧 label）

**Supplier Business Notes（供应商备注）**:
Supplier 目录层关于合作事实的自由文本（如接待上限、是否支持加单）。表单与只读区 section 标题为 **备注**，非「业务备注」。
_Avoid_: 业务备注, 用备注代替结算说明

**Supplier Financial Profile（供应商更多财务信息）**:
Supplier 目录层可选区块：是否可开票、发票类型、税率、收款银行账户。在创建/编辑表单中 **默认折叠**；收款账户可再内层折叠。轻量创建时不强制填写。
_Avoid_: 创建时强制填银行账号, 与结算信息混为一段

**Supplier Service Order Tab（供应商服务团单 Tab）**:
Supplier 详情中的**业务事实层** Tab，行＝引用该 Supplier 的行程段资源（Segment Resource，跨发团，**含拼出／旅行社**），回答「这家在哪些团、被安排了什么资源、约定成本多少」。Tab label 用 **服务团单**（**不**沿用 Partner 的「合作团单」，因供应商不发团、语义不符）。列只放业务事实：出团日期、关联发团、行程段、资源种类、资源名称、约定金额、备注；**不**放应付状态/已付/未付（归往来账款 Tab）。汇总卡三项（资源行数/关联发团数/约定金额合计）跟随筛选、所见即所算；按所属发团出团日期筛选，默认不限＋出团日期倒序。列出全部引用该 Supplier 的资源行，不看应付是否已生成或作废，也不看发团状态（编辑中/待结算/已结清/已关闭均含）——因「被安排提供该资源」这个业务事实已发生（与 Partner 合作团单含全部客源单同理）。第一版无对外结算单/对账单导出（后置迭代）。
_Avoid_: 沿用「合作团单」label, 行＝客源单, 行＝发团再展开子行, 列上放应付状态或已付未付, 只列已提交应付的资源, 排除已关闭/归档发团的资源, 排除拼出资源, 第一版做供应商结算单导出

**Supplier Ledger Tab（供应商往来账款 Tab）**:
Supplier 详情中的**财务账款层** Tab，行＝与该 Supplier 往来的**应付**收付款节点，回答「我欠这家多少、付到哪一步」。数据按 `counterpartyType=supplier + counterpartyId` 精确过滤，复用 PaymentScheduleWorkspace（scope='supplier'）。**仅应付方向**，不设应收/应付切换——供应商结构上只有应付（供应商退款是收入流水而非应收节点）；极罕见的手工「供应商应收」只在全局应收页可见，不进本 Tab。汇总卡三项（约定/已核销/未结清），已关闭、已作废节点不计入，跟随出团日期筛选。Tab 内财务操作（登记付款/匹配流水/关闭/重开/调整约定金额/核销）按 `canMutateFinance(menuKeys)` gating：财务可操作，计调等非财务角色**只读**（可看账款但不显示操作按钮），与发团详情账款口径一致；Partner 往来账款 Tab 同步对齐该 gating。
_Avoid_: 设应收/应付切换, 把手工供应商应收纳入本 Tab, 汇总含已关闭或已作废节点, 计调可登记付款或核销, 仅靠后端 403 不做前端 gating, 暴露应付到期日或逾期

**Finance Transaction（收支流水）**:
Organization 内的资金进出记录。资金层与账款层分离：新建/编辑流水只要求金额大于零，**不**按收付款节点约定金额或客源路径金额做累计上限，因此允许单笔或合计超过约定的超额收款/超额付款（多出部分以未核销流水保留在资金卡）。登记收款/付款与核销则硬拦，核销金额不得超过节点未结清与流水未核销余额，节点不会被核销超过约定。游客代收流水表单的「建议金额 / 填入」仅作录入参考：以节点未结清（或尚未提交应收时的路径金额）为上限，并扣减同客源单上已有未核销游客代收流水后给出剩余建议，不构成后端硬拦。
_Avoid_: 流水账, 记账, 以建议金额当硬上限, 资金流水强制不超过约定账款, 用核销规则约束未关联节点的流水录入

**Departure Finance Transaction Tab（发团收支流水 Tab）**:
发团详情中的资金工作区 Tab，正式 label **收支流水**，位于应付管理与核销记录之间。仅展示 `departureId` 等于当前发团的流水；能力与全局收支流水页同级（新建、编辑、作废、去核销、详情），新建时关联发团锁死为当前发团，编辑时关联发团只读。发团 scope 不默认交易日期近窗；概览「查看流水」切本 Tab（可带流水方向），不跳全局页。发团 scope 下「查看核销」进本发团核销记录 Tab。全局 `/finance/transactions` 仍保留跨团对账与按发团筛选，不重定向。漏挂发团的流水第一版不提供认领，须在全局页编辑补关联。
_Avoid_: 团单流水 Tab, 用核销关系扩大本团流水集合, 发团 Tab 默认近 30 天, 概览查看流水跳全局, 发团内编辑可改走关联发团

**Departure Verification Tab Date Filter（发团核销 Tab 日期）**:
发团详情「核销记录」Tab 与发团收支流水 Tab 对齐：不默认核销日期近窗；深链「查看核销」仍清空日期并按流水单号/收付款单号筛选。
_Avoid_: 发团核销 Tab 默认近 30 天, 仅深链清日期而直接进入仍带近窗

**Transaction Direction（流水方向）**:
收支流水的资金方向，存储为 `inflow` / `outflow`。产品 UI 字段 label **流水方向**；取值统一 **收入** / **支出**（不用「流入 / 流出」）。收入流水用于应收核销；支出流水用于应付核销。
_Avoid_: 收支方向（作字段 label）, 流入, 流出, income, expense（作存储枚举值）

**Transaction Writeoff Amount（流水核销金额）**:
流水上已被核销记录占用的金额及其余额，由 Verification 分配 **派生**，不单独写入流水表。产品 UI label：**已核销金额**、**待核销金额**（不用「已分配 / 未分配」；不用「未核销金额」作金额列 label）。后端字段可保持 `allocatedAmountCents` / `unallocatedAmountCents`。
_Avoid_: 已分配, 未分配, 未核销金额（作金额列 label）, writtenOffAmount（第一版不必改 API 字段名）

**Transaction Writeoff Status（流水核销状态）**:
流水可核销进度的展示状态，由已核销金额与交易金额 **派生**，不写入数据库：未核销（已核销 = 0）、部分核销（0 < 已核销 < 交易金额）、已核销（已核销 = 交易金额）。状态取值文案仍为「未核销 / 部分核销 / 已核销」，与金额列 label「待核销金额 / 已核销金额」区分。
_Avoid_: 与收付款节点 `Payment Schedule Status` 混为一谈, 新增独立存储枚举（第一版）, 用状态文案替代金额列 label

**Financial Party Naming（财务主体命名）**:
业务来源、合作主体与财务结算对象分词：客源单关联 **客户**（Partner，名录仍称合作伙伴）；资源（含拼出）关联 **供应商**；应收展示 **收款对象**、应付展示 **付款对象**；流水与核销展示 **往来对象**。三者不是同一字段的随意别名，而是场景分流的产品语言；存储层对手方仍为 Counterparty。
_Avoid_: 用「往来对象」覆盖应收/应付单向页, 用「客户/供应商」直接当应收/应付列表对手方列名, 把业务来源列与结算对象列混成一列, 资源列表列头称「对手方」, 拼出表单称「承接方」

**Counterparty（往来对象）**:
收付款节点与收支流水上标识资金往来一方的字段，存储为 `counterpartyType` + `counterpartyId` / `counterpartyName`。领域名仍为 Counterparty；**产品 UI label 按页面方向分流**：应收单侧称 **收款对象**，应付单侧称 **付款对象**，收支流水与核销等同时含流水方向的页面仍称 **往来对象**（类型字段同理：收款对象类型 / 付款对象类型 / 往来对象类型）。类型枚举：合作伙伴（Partner）、供应商（Supplier）、游客代收（Guest，存储值仍为 `guest`）。第一版收款/付款对象与业务对手方**不可分叉**：客源提交应收时写入发客客户（客户补款）或游客代收（Guest）；资源提交应付时写入该资源供应商（含拼出／旅行社；历史 Partner 承接拼出行仍按已存 counterparty）；登记与核销沿用，不提供改成「另一家实际收付主体」的能力。新建/编辑流水时：合作伙伴、供应商须从目录档案选择（写入 `counterpartyId`）；游客代收须先选关联发团再选客源单（仅列出该发团下 `guestCollectCents > 0`、即存在游客代收路径的客源单；`counterpartyId` = 客源单 id），名称可手改（如客人姓名）仅作展示。流水方向联动默认类型：收入 → 合作伙伴；支出 → 供应商。新建流水关联发团必填，表单中靠前填写。
_Avoid_: 交易对象, 在单向的应收/应付页统一称「往来对象」, 第一版允许收款/付款对象与客源客户或资源供应商分叉, 客户/供应商/其他（作流水表单枚举 label，与 Partner/Guest 等领域类型混用）, 流水表单仅手输名称不接档案（含已删除的「手工录入 / manual」往来类型）, 客人流水无客源单 id 仍可核销游客代收, 新建流水关联发团可选, 往来类型 UI 仍称「客人」（与应收节点「游客代收」不一致）

**Payment Channel（收付款方式）**:
收支流水上记录资金实际经过的方式分类，固定枚举：现金、银行转账、微信、支付宝、其他（存储如 `cash` / `bank_transfer` / `wechat` / `alipay` / `other`）。凡 **新建** 收支流水（含登记收/付款与流水页手动新建）均必选；匹配已有流水时只读展示。产品 UI 字段 label：**收付款方式**（不用「收付款通道」）。登记抽屉 UI label：应付侧 **付款通道**、应收侧 **收款通道**（不用「付款账户/收款账户」）。仅用于分类与列表展示，第一版 Organization 不可自定义方式；不做本社账户台账、余额或多账户管理。新建流水 **不要求** 同时存在收付款节点；可先记流水，后续再通过匹配流水核销到节点。
_Avoid_: 收付款通道（作字段 label）, 付款账户, 收款账户（登记抽屉 UI label）, 银行卡（UI 文案，用「银行转账」）, 账户余额, 多账户 CRUD, 新建流水必须先有收付款节点

**Verification（核销）**:
收付款节点与收支流水之间的金额分配记录，将实际资金进出与计划应收/应付对齐。核销要求两侧方向、往来对象与 Organization 一致；往来对象一致性由流水与节点硬匹配保证（有 `counterpartyId` 时按 id，双方皆无 id 时按名称），不在新建核销表单上单独录入或筛选往来对象。流水与节点可分属不同发团（跨团核销）：同行一笔款项结清多个发团账款是真实业务，不得为对齐发团要求拆分真实流水；跨团组合提交前给出一次确认警示，但不阻止核销，也不视为数据异常。
_Avoid_: 结算, 对账, 新建核销顶部独立「往来对象」筛选项（与候选区搜索重复）, 游客代收仅靠名称匹配, 强制流水与节点同团, 为对齐发团拆分真实流水, 跨团核销作数据异常

**Create Verification（新建核销）**:
财务人员手工选择流水与收付款节点并建立核销关系的入口（`CreateVerificationDrawer`）。基础信息含核销方向、核销日期、可选关联发团；流水/节点候选靠各自搜索框按编号、往来对象或发团模糊过滤。不提供独立「往来对象」筛选项；选中流水后，节点候选仅保留往来对象一致者。
_Avoid_: 新建核销必填往来对象, 顶部往来对象过滤与候选区搜索双轨并存

**Cancel Verification（撤销核销）**:
解除收支流水与收付款节点之间的有效金额分配：原流水仍是已发生的资金事实，其金额回到未核销可用余额；原节点已核销金额下降、未结清金额上升。撤销核销不作废流水、不表示资金未发生或已退回；流水本身错误应另走作废或反向资金记录。
_Avoid_: 撤销付款, 撤销收款, 删除流水, 已放弃金额, 资金自动退回

**Match Transaction（匹配流水）**:
财务人员将 **已有** 收支流水与收付款节点建立核销关系的操作。登记收付款则相反：无流水时新建流水并同时核销。产品 UI、按钮与组件统称「匹配流水」；后端 HTTP 路径保持 `link-transaction`，不因产品改名而变更 API。第一版：右侧抽屉、账款/流水候选/已选流水/本次核销四块、流水方向过滤、默认核销金额取 min(流水可核销余额, 节点未结清)；**推荐标识**（同对象/同团单/金额一致）延后迭代。
_Avoid_: 关联流水（产品文案）, 匹配流水（作 API 路径名）

**Verify From Transaction（去核销）**:
财务人员从 **收支流水列表** 从某条流水发起核销的操作，与「匹配流水」互为镜像：匹配流水从收付款节点出发选流水；去核销从流水出发选收付款节点。收入流水自动进入应收核销候选；支出流水进入应付核销候选。第一版用独立抽屉 `VerifyFromTransactionDrawer`（布局镜像 `MatchTransactionDrawer`），提交复用 `createVerification`。「查看核销」跳转核销列表，经顶部筛选区按 **流水单号**（`transactionNo`）过滤，不按实体 ID、不另开临时筛选条。
_Avoid_: 复用手工双下拉的核销 Modal 作第一版入口, 查看核销作独立详情抽屉（第一版）, 查看核销按 transactionId 深链, 当前筛选临时 Tag 承载深链

**Verification List Deep Link（核销列表深链筛选）**:
从流水或收付款节点「查看核销」进入核销列表时，URL 只带一侧业务编号（`transactionNo` 与 `scheduleNo` 互斥），不带实体 ID、不带 match mode、不展示「当前筛选」条；灌入顶部对应输入框，并清空日期与其余筛选项。URL 有编号时前端以精确匹配调列表 API（`*Match=exact`，大小写不敏感）；用户改任一编号输入即清掉 URL 编号，此后为普通模糊搜索（默认 `contains`）。重置恢复默认日期并清空 URL。旧 `transactionId` / `paymentScheduleId` 深链不再兼容。列表 API 删除按 ID 过滤参数。
_Avoid_: transactionId / paymentScheduleId 作核销列表可分享深链, 深链与顶部筛选双轨并存, 深链保留默认近 30 天日期, URL 暴露 match mode, 流水单号与收付款单号深链同时锁定

**Void Transaction（作废流水）**:
将收支流水标记为无效、不再参与新增核销。仅 **正常且未核销**（已核销金额 = 0）的流水可作废；部分或已全部核销须先撤销核销。作废 **不可恢复**；作废时 **作废原因必填**（前后端均校验）。已作废流水列表仍可查看，但不展示作废按钮。
_Avoid_: 已核销流水直接作废, 作废原因可选, 作废后可恢复（第一版）

**Edit Transaction（编辑流水）**:
修正尚未进入账务闭环的流水录入。仅 **正常且未核销**（已核销金额 = 0）可编辑；部分或已全部核销不可改核心字段，须先撤销核销。可编辑字段：流水方向、交易金额、交易日期、收付款方式、往来对象、关联发团、流水备注。未核销流水视为孤立资金记录，允许直接 UPDATE，不必作废重建。
_Avoid_: 已核销流水静默改字段, 未核销也必须作废重建（第一版）, 编辑流水单号

**Finance Transaction List Filters（收支流水列表筛选）**:
第一版 P0 筛选项：交易日期范围（默认近 30 天）、流水方向、往来对象关键字、核销状态、流水单号、关联发团、**流水状态**（正常/已作废）。收付款方式筛选、高级组合延后 P1。
_Avoid_: 第一版仅筛选发团, 流水状态筛选延后（实现成本低且常用）

**Finance Transaction List Columns（收支流水列表列）**:
第一版 P0 必显：流水单号、流水方向、交易金额、已核销金额、待核销金额、交易日期、往来对象、收付款方式、核销状态、流水状态、操作。**创建时间**可选加列（已有 `createdAt`，零 schema 变更）。**经办人**（`createdBy`）需 schema 与创建链路改造，延后 P1。流水单号点击打开详情抽屉（见下）。
_Avoid_: 第一版必显经办人, 流水单号纯展示不可点, 流水号/收支方向/流水金额/未核销/收付款通道（作列 label）

**Finance Transaction Detail（流水详情抽屉）**:
第一版 P0 实装详情抽屉（点击流水单号打开）：**基础信息**、**核销信息**（已核销金额/待核销金额/笔数/最近核销时间）、**核销记录列表**。**操作记录**块延后后续迭代（当前无 audit 表；创建/编辑/作废/核销的完整操作人追溯一并后续补齐）。
_Avoid_: 第一版详情抽屉整体延后, P0 为操作记录新建 audit 表

**Register Payment / Receipt（登记付款 / 登记收款）**:
财务人员在尚无收支流水时，从收付款节点直接录入实际收付款：系统新建收支流水（含收付款方式）并同时生成核销。产品确认按钮文案为「确认付款并核销 / 确认收款并核销」；后端路径保持 `confirm-payment` / `confirm-collection`。登记抽屉：只读区含应付/应收单号、标题、关联发团、收款对象或付款对象（随方向）、总额/已结/未结、核销后未结金额（随输入金额实时计算）；可编辑区含收付金额、付款通道或收款通道（随方向）、交易日期、**流水备注**（写入收支流水，非节点备注）。通道下拉不另附 helper 文案。底部次要说明：应付侧「确认后将生成一条支出流水，并自动完成本次核销」；应收侧「确认后将生成一条收入流水，并自动完成本次核销」。

**Finance Command Idempotency（财务写命令幂等）**:
所有可重复发生、会改变财务事实的公开 HTTP 命令必须携带 `Idempotency-Key`。同一 Organization、同一 operation、同一 key 且目标、载荷与操作者一致时，系统重放首次成功结果；同键异载荷返回冲突，缺少 key 拒绝执行。幂等记录与业务事实同事务提交，失败不保留“已处理”假象；同键并发只能有一个请求执行并分配业务编号。相同金额与往来对象仍可能是两笔真实业务，因此不得脱离 key 按 payload 或时间窗口猜测去重。客源应收/资源应付生成是来源级一次性动作，依靠来源行锁与唯一约束拒绝重复生成，不把同来源第二次请求重放成成功。
_Avoid_: 前端只禁用按钮作为防重, 相同 payload 自动合并, 业务写入成功后才单独记录幂等结果, 同键异载荷静默重放
_Avoid_: 仅确认付款（文案不清会否核销）, 登记时不填收付款方式, 通道字段下方重复枚举 helper, 备注（作登记抽屉 label，用「流水备注」）, 底部说明不区分流水方向

**Payment Schedule List Actions（收付款节点列表操作）**:
全局应收/应付列表操作列：可结算时外露「登记收款/登记付款」与「匹配流水」；「编辑」「关闭节点」「重新打开」「调整约定金额」「查看核销」收进「更多」。关闭节点仅未结清金额大于零时出现（含部分核销）；已结清与已关闭不出现关闭入口。「重新打开」仅已关闭节点出现。「调整约定金额」仅资源应付或客源应收（客户补款/游客代收）、节点开放、有效已核销为零且存在财务履历时出现；发团已归档时不出现。「查看核销」仅当有效已核销金额大于零时出现（含已关闭节点）；核销全部撤销后入口消失，审计可在核销列表按收付款单号检索（含已撤销）。查看核销跳转核销列表，经顶部筛选区按 **收付款单号**（`scheduleNo`）过滤，第一版不做独立核销详情抽屉。
_Avoid_: 四按钮平铺, 查看核销作独立详情页（第一版）, 查看核销按 paymentScheduleId 深链, 已结清仍露出关闭节点, 按历史含已撤销核销记录决定入口, 已关闭一律露出查看核销, 归档发团仍露出调整约定金额, 无财务履历仍露出调整约定金额, 有效核销未清零仍露出调整约定金额, 仅应付可调而应收不可调

**Payment Schedule List Filters（收付款节点列表筛选）**:
团详情与全局应收/应付的对手方搜索框按 **`counterpartyName` 模糊匹配**（API：`counterpartyKeyword`）；产品 UI label 随方向为 **收款对象** / **付款对象**（不用「往来对象」）。不按合作伙伴/供应商/游客代收类型分层。查看应收/查看应付深链预填该关键字。列表 API 仍保留 `counterpartyType` / `counterpartyId` / 精确 `counterpartyName` 供后续扩展，当前 UI 不暴露。单号／派生身份文案、节点状态、到期日仍为客户端筛选；单向页搜索框 placeholder 为「搜索应收单号 / 收款方式」「搜索应付单号 / 费用项目」（底层仍可命中存储 `title`）。发团内嵌、Partner/Supplier 往来账款 Tab、登记/详情抽屉等凡单向应收/应付表面，对手方文案一律方向化；流水与核销页仍用往来对象，混列单号筛选用「收付款单号」。
_Avoid_: 应收/应付筛选或抽屉仍称「往来对象」, 往来对象类型级联作列表主交互, 团内去重 counterparties 接口, 搜索节点编号／标题（作单向页 placeholder）

**Payment Schedule List Columns（收付款节点列表列）**:
应收/应付列表用领域化字段替代通用「标题＋往来对象」，四个 scope（发团详情内嵌、全局主列表、Partner/Supplier 往来账款 Tab）与已作废审计视图共用同一套列；**编辑抽屉与详情抽屉身份区与列表同序同取值**，不单开「标题」字段（提交编辑时沿用原 `title`）。**应付**身份区为：应付单号（`scheduleNo`）、费用类别、费用项目、付款对象；**应收**为：应收单号、来源客源单、收款方式、收款对象。费用类别读资源种类（`SegmentResource.resourceKind`）；费用项目优先读实时资源项目名（`SegmentResource.title`），缺失回落资源种类，手工其他应付回落节点标题；收款方式由应收来源类型派生「客户补款/游客代收」，手工其他应收回落「其他」；来源客源单读客源单展示名（`SourceOrder.displayName`）。收款对象 / 付款对象均为 Counterparty 字段的方向化 UI label（不新增持久化字段）：游客代收统一显示「游客」（具体客源单已在来源客源单列体现），其余显示对手方名称、去掉类型前缀。费用类别/费用项目/来源客源单靠 `sourceId+sourceType` 于列表接口实时联查源实体填充（派生字段 `resourceKind`/`resourceTitle`/`sourceOrderName`，不落库、不迁移）；源实体被删或手工来源时该派生列为空态「-」，展示随源实体实时变化。
_Avoid_: 保留或加回通用「标题」「来源」列（含编辑／详情）, 应收溯源列仍称「客源单」（易与收款对象混淆）, 应收/应付列表仍统一称「往来对象」, 为收款/付款对象新增持久化快照字段, 游客代收展示客源单 displayName 重复来源客源单列, 手工行伪造资源种类/客源单, 编辑／详情与列表身份字段取值分叉
