# 业务与 Agent 融合原型 QA

> 历史验证记录：本文及关联截图只证明当时版本，不能作为当前业务校验或验收规则。最新设计以 [#483](../../../prd/2026-09-07-business-object-agent-collaboration.md) 和 [本轮对齐记录](current-design-qa.md) 为准；年龄/电话类型强制核实、容量阻断、全局聊天及原收款简化已失效。

日期：2026-09-05

## 已验证

- 默认业务列表，无原型顶部切换器；Agent 初始收起。
- 从业务页面发起建团，在明确点击后进入审核；创建成功后显式打开正式发团。
- 同一关西团、同一会话继续客源任务，确认后业务页显示 8 成人 / 2 儿童、¥60,800 应收建议。
- 酒店和团餐独立确认后，正式资源页显示两组、总额 ¥10,400；巴士仍待审核。
- 手工新增 ¥600 资源后，财务建议和 Agent 查询均读取 3 条资源、¥11,000；查询不触发写入。
- 从财务页“待我处理”恢复资源现场，再返回保留财务业务位置。
- 1560 × 894 DOM 检查：页面无横向溢出，审核 footer 下边界为 894，底栏保持在视口内。
- 桌面业务/助手并列已视觉检查。

## 检查范围

build、6 项 Node 检查、antd lint 与 diff 空白检查。不是生产集成验证，未运行生产全量测试。

## 已知限制

有限关键词驱动的演示；不连接真实 CopilotKit/Agent 服务。原型共享消息与输入状态，不代表生产会话架构实现完成。手工录入与精确编辑仍为简化表单。全局跨对象任务、财务核销、完整移动布局未验证。

## 2026-09-05：对齐真实业务对象后的检查

范围仅 `docs/design/prototypes/wf-03-create-departure`；真实 5173 业务没有修改。

- 浏览器从已有发团进入，八分区与主导航可见。
- 客源管理 → 整理名单 → 三段式审核 → 写入 → 正式客源单：10 人、原始应收 62,800、优惠 2,000、结算金额 60,800；游客抽屉显示 10 位。
- 单独提交应收后出现 AR-SOURCE 60,800；登记收款后账款仍未结清；核销确认后未结清为 0、登记收款入口禁用。
- 撤销核销后可重新匹配，原流水保留。
- 酒店与团餐独立写入合计 10,400；28 座车保持不可勾选和待处理。
- D2 下展开同一会话再返回仍为 D2；客源/财务/资源历史保留。
- 资源应付审核显示 04-02 酒店 7,200 和 04-03 团餐 3,200，确认后应付列表按对应供应商显示两笔。
- 三段式截图检查：左侧对象 220px，中间会话、右侧审核；审核内容独立滚动，确认按钮吸底。修复隐藏全局侧栏后主内容误落入 0px 网格列的问题。
- 修复客源数据 `children` 数字字段被 antd Table 当树形子行而导致的渲染错误；客源表只传展示行对象，再次实际确认写入通过。
- 构建、7 个模型/打包检查通过；Ant Design 主页面检查无问题。

不能从这些定向检查推导生产财务、权限或全部系统功能已验证。完整范围见 business-coverage.md。

### 对象范围协作补充验证

- 已移除全局展开入口；工作台不展示聊天按钮或会话面板。
- 客源页生成提案后，新建协作保持新消息独立；再次请求相同提案不会夺取旧提案的来源。
- 工作台待审核入口恢复原客源页面、来源会话和三段式审核，左栏可见两次相关协作。
- 供应商范围发起查询，从“查看关联资源”进入执行安排仍保留同一对话，关联范围显示供应商管理与本团。
- 切换普通业务导航与新建协作不会自动进入审核。构建、既有 7 项模型/打包检查、Ant Design 检查及 diff 检查通过。
