# WF-05 日程资源原型 QA

> 历史验证记录：本文及关联截图只证明当时版本，不能作为当前业务校验或验收规则。最新设计以 [#483](../../../prd/2026-09-07-business-object-agent-collaboration.md) 和 [本轮对齐记录](current-design-qa.md) 为准；年龄/电话类型强制核实、容量阻断、全局聊天及原收款简化已失效。

日期：2026-09-05

final result: passed

## 验证范围

本结论只针对内存交互原型，不代表生产资源或财务链路通过验证。

- 参考：WF-01 三栏结构与 ADR-0055 后续决策；参考图里的关系条已被明确废弃，没有复刻。
- In-app Browser 在 1560 × 894 桌面视口检查：三栏完整，中栏无关系条；右侧内容独立滚动时，金额摘要与确认底栏保持可见。
- 酒店和团餐勾选后合计 ¥10,400；28 座巴士不足 32 人，无法勾选。
- 首批确认显示 2 组写入结果，剩余 1 组；不自动跳转正式记录。
- 补充车队确认单只将巴士更新为 35 座、¥2,800、v2；已写入两组仍为 v1 且不可重复提交。
- 精确编辑巴士金额至 ¥2,900 后返回审核、版本升为 v3，选择清空，需要重新勾选。
- 第二批确认 1 组后显式进入正式资源列表，呈现全部 3 组，状态为待提交应付。
- 修复窄窗口下沿用旧最小宽度导致裁切的问题，WF-05 在 900px 以下纵向排列。
- 修复会话列表图标意外占据弹性宽度造成文本右移的问题。

## 自动检查

- npm run build：通过，保留现有 Vite 大 chunk 警告。
- node --test tests/*.test.mjs：5/5 通过（4 项 Sites 包装与路由检查、1 项分组确认/修订保护检查）。
- antd lint ResourcePrototype.jsx：0 问题。
- git diff --check：通过。

## 已知边界

- 内置材料按钮演示增量更新；自由文本只留在会话，不接真实模型与附件解析。
- 每组仅含一条资源，未验证组内多条资源的提交失败和回滚。
- 容量是原型审核字段；真实人员冲突、重复预订、权限、并发及应付写入不在本次验证范围。
- 尚未验证精确编辑未保存离开保护；取消会放弃本次编辑。
- 未提交 Git，未修改生产业务实现。
