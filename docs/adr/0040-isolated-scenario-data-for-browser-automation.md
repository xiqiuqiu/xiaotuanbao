---
status: accepted
---

# 浏览器自动化使用隔离的场景数据

CI 中每次浏览器自动化运行使用迁移和固定种子构建的临时数据库，本地默认使用独立 E2E 数据库，禁止自动重置开发者日常数据。Playwright 用例由场景工厂通过正式 API 或领域命令建立目标行为所需的隔离前置状态，再由真实浏览器执行目标行为；只有正常接口无法产生的边界状态才允许受限测试代码直接操作数据库，且测试能力不得成为生产接口。
