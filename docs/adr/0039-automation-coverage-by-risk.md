---
status: accepted
---

# 前端自动化以业务行为目录作为覆盖契约

项目以版本化、机器可读并由人工确认的业务行为目录作为前端自动化覆盖权威标准，登记所有可识别行为以暴露未验证路径，再按失败影响、发生可能和人工可发现性分级。目录最小单位是一次有业务结果的状态变化，每个行为指定一个主验证层；关键行为必须全部自动化并通过，不用代码行覆盖率或单一总分掩盖风险，也不把每个界面细节都升级为浏览器门禁。
