# 服务端统一生成发团运营表 Excel

发团运营表采用后端统一快照模型：同一份结构化快照同时供页面预览与 Excel 导出使用，避免运营与财务口径在前后端重复拼装后产生差异。第一版由 NestJS 在服务端使用封装后的 ExcelJS 渲染器生成带打印版式的 `.xlsx`，前端只负责预览、请求与下载；Excel 文件写入导出时点的静态事实，不用公式重算业务金额。ExcelJS 只存在于可替换的文件渲染边界，领域快照不依赖其类型，以控制该库维护活跃度一般所带来的替换成本；PDF 暂不纳入第一版。
