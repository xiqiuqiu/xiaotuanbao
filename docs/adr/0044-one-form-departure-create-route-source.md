---
status: accepted
---

# 新建发团用一张表选择路线来源，不再分两步向导

新建发团曾按「先选路线、再填团期」拆成两步向导。叠上 AI 辅助后，打开协助会跳过路线步，草稿却仍默认 `mode=template` 且没有 `templateId`，创建被空常用路线拦住。选用常用路线与填写路线名称之后要填的发团字段相同，第一步并不是独立业务步骤。因此改为**一张表、一次创建**：路线来源是表上的控件（默认填写路线名称；选用常用路线在抽屉里用卡片网格），AI 挂在同一张表上。复制骨架语义不变（ADR-0008）。两扇门入口与「建团当时新建常用路线」均拒绝。
