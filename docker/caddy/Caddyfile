{$DOMAIN} {
    root * /srv/web
    encode gzip zstd

    handle /api/* {
        reverse_proxy api:3000
    }

    handle {
        try_files {path} /index.html
        file_server
    }
}
