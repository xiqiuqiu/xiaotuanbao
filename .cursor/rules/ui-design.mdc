---
description: 小团宝 Web UI 须遵循根目录 DESIGN.md（Ant Design 视觉语言）
globs: apps/web/**/*.{tsx,ts,css}
alwaysApply: false
---

# UI 设计约束

编写或修改 `apps/web` 界面前，阅读并遵守仓库根目录 [`DESIGN.md`](../../DESIGN.md)。

要点：

- 对齐 Ant Design v6 默认浅色主题；主题只改 `AppProviders` 的 seed / `theme.components`
- 一屏一个 `primary` 按钮；间距走 4px 网格（常用 8/16/24）
- 列表页骨架：页头 + 筛选 Card + 表格 Card + Drawer/Modal
- 禁止硬编码主色/表面色、平行设计系统、胶囊主按钮、装饰性炫光动画
- 文案术语遵守 `CONTEXT.md`
