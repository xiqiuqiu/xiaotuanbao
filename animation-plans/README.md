# Animation improvement plans

Generated by `improve-animations` against commit `2894e53` (001–011); plan **012** stamped at `c2e94ec`.  
Execution target: `apps/web` only. Do **not** invent a parallel CSS token system (`DESIGN.md`); prefer Ant Design `cssVar` / `theme.token` motion fields.

## Status

| # | Title | Severity | Status | Depends on |
| --- | --- | --- | --- | --- |
| 001 | Gate overview metric-card enter to first visit | HIGH | DONE | — |
| 002 | Soften global prefers-reduced-motion | MEDIUM | DONE | — |
| 003 | Keep softFetching opacity under reduced motion | MEDIUM | DONE | 002 (optional; safer after) |
| 004 | Progress reveal via transform, not clip-path | MEDIUM | DONE | 001 (same CSS file — merge carefully) |
| 005 | Unify press feedback scale/duration | MEDIUM | DONE | 007 (optional) |
| 006 | Login submit color uses `ease` | LOW | DONE | 005 (overlap on `.submit`) |
| 007 | Consolidate ease-out via ant CSS vars | LOW | DONE | — |
| 008 | Departure tab opacity bridge | LOW | DONE | **001** (required) |
| 009 | Execution segment pane opacity | LOW | DONE | 005 (optional) |
| 010 | Lock antd motion durations to DESIGN | LOW | DONE | 007 (optional merge) |
| 011 | Rare success feedback without celebration | LOW | DONE (no-op) | — |
| 012 | Create-departure wizard step opacity bridge | LOW | DONE | — (imitate **008**) |

## Recommended execution order

1. **001** — highest leverage; removes daily tab-noise.
2. **004** — same overview CSS; do in the same PR as 001 if possible.
3. **002** then **003** — accessibility pair.
4. **007** then **005** then **006** — cohesion / easing sweep (one PR is fine).
5. **010** — can merge with 007’s `AppProviders` edit.
6. **008** then **009** — additive bridges after corrective work.
7. **011** last among the original set — polish or no-op.
8. **012** — additive; safe anytime after 008’s pattern exists (already DONE). No dependency on unfinished work.

## Notes for executors

- Invoke with `improve-animations execute <plan>` or hand any agent the single markdown file; plans are self-contained.
- After implementation, re-check with `review-animations` standards (no ease-in, no `scale(0)`, reduced-motion keeps comprehension cues).
- Custom motion surfaces today: `global.css`, `LoginPage.module.css`, `DepartureOverviewStatsCards.module.css`, `PaymentScheduleWorkspace.module.css`, `ExecutionTab.module.css`, `list-query-ux.module.css`, `TableNameLink.module.css`, `DepartureDetailPage.module.css`（+ plan 012 将扩展 `CreateDepartureWizard.module.css`）。
