# bloub engine (vendored)

Source: https://github.com/jeremy-prt/bloub (`src/bot`, plus `lookTarget` from `src/ui/gaze.ts`)

License: MIT (see `LICENSE`). Not affiliated with x.ai. The visual design of the reference avatar is not covered by the MIT license; only the measured recreation code is.

Do not rewrite measured geometry or timing tables in `profiles.ts` / `states.ts`.
